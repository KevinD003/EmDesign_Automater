# 🧵 STITCHIQ — Project Status & Handoff

> **Single source of truth for project state.** Read this first, then [`README.md`](./README.md),
> [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md), and the master spec
> [`AI-Embroidery-Software-Prompt.md`](./AI-Embroidery-Software-Prompt.md).

| Field | Value |
|---|---|
| **Project** | STITCHIQ — AI-powered embroidery design & digitizing platform |
| **Document version** | **v140** |
| **Times updated** | **140** |
| **Last updated** | 2026-08-09 |
| **Current phase** | **PHASE B in progress (B1 done, B1.5 partial) · Phase A complete+verified** · LAUNCH-READY (supervised LAN, ~100 users) — after a NO_GO was earned and fixed.** v2 Parts 0–17 complete · Parts 19–21 = swarm integration + adversarial review: authorization, session TTL, rate-limit keying and credential-leak blockers found by the critic layer and closed. |
| **Git branch** | `claude/code-quality-improvements-hyu6dg` |
| **Latest code commit** | see the changelog below — one commit per CTO Phase-A item since v106 |
| **Working tree** | clean |
| **Tracked files** | ~1,300 |
| **Location** | GitHub: `KevinD003/EmDesign_Automater`, branch `claude/code-quality-improvements-hyu6dg` |

---

## 📌 For the next model / session — READ THIS FIRST

1. **Current reality:** The app is a working (minimal) embroidery studio. Three input paths:
   **Open** a real `.DST`/`.PES`, **Digitize a PNG/JPG image**, or **Text** (lettering) — all → stitches +
   **real vector objects** (with holes/counters + underlay). Then: render → click-to-select →
   recolor/rename/reorder → undo/redo → export any format → worksheet PDF. Digitized/lettered objects are
   **editable**: select one, change density/angle/underlay, Apply → server rebuilds the stitches.
   Export to .DST/.PES/.JEF/.EXP/.VP3, convert via API, or download a **full production package ZIP**
   (machine file + master + worksheet + color card + preview + summary). A **2D/TrueView-3D** toggle shows a
   lit 3D thread preview; snap any color to the nearest catalog thread; edit density/angle/underlay/**pull-comp**.
   A **Check** button surfaces pre-export validation (hoop-fit blocks); **Save/Saved** persists designs in-browser;
   **Open** re-imports a master `.stiq.json`; the worksheet shows thread length per color; a **Studio ⇄ Dashboard**
   nav toggle adds a **real metrics page** (My designs / stitches / colors from the signed-in cloud account).
   **Cloud persistence is LIVE** — Save/list/open designs to a real Supabase Postgres with **per-user auth
   (signup/login)** — cloud Save/Open in the UI. **Phase 8 v1**: **Optimize** (cut stitch-path travel) +
   **Quality** (score + findings) buttons; **manual digitizing** (draw Run/Satin/Fill). Verified: **pytest 81/81,
   vitest 57/57**, e2e via Vite proxy, in-browser render confirmed in Chrome (§12), **+ live auth/cloud round-trip
   with multi-user isolation + manual-draw + adversarial-review hardening**.
2. **Chosen scope (by the user):** build **vertically**, one phase at a time ([§14](#-14-full-project-roadmap-phases-010)).
3. **Next task (pick one):** Everything buildable with this stack is now built — Phases 0–8(v1) + manual
   digitizing, all verified in Chrome. Remaining is either **gated** or **outward**: (a) **neural AI** (Phases
   8-neural/9 — learned digitizing, text-to-design; needs GPU + training data); (b) **push to GitHub** to exercise
   the CI config (needs explicit user authorization — data-publishing action); (c) polish: **satin-stroke lettering
   v1.1**, password reset, Supabase Storage for master/preview URLs, per-stroke satin.
4. **⚠️ MANDATORY — every change is logged in THIS FILE.** Before finishing any task: bump **Document
   version** + **Times updated**; update **Last updated** + **Latest code commit**; add a
   [§2](#-2-update-history--changelog) row (**newest on top**); flip [§5](#-5-feature-status-matrix) rows;
   move [§7](#-7-whats-remaining)→[§6](#-6-whats-done-verified); tick [§14](#-14-full-project-roadmap-phases-010);
   commit the doc with the code.

---

## 🗂 Table of Contents
1. [TL;DR](#-1-tldr) · 2. [Changelog](#-2-update-history--changelog) · 3. [Tech Stack](#-3-tech-stack-exact-installed-versions) ·
4. [Repo](#-4-repository-structure) · 5. [Feature Matrix](#-5-feature-status-matrix) · 6. [DONE](#-6-whats-done-verified) ·
7. [REMAINING](#-7-whats-remaining) · 8. [Decisions](#-8-key-decisions--rationale) · 9. [Gotchas](#-9-environment--gotchas) ·
10. [Next Steps](#-10-next-steps-do-these-in-order) · 11. [Run & Verify](#-11-how-to-run--verification-baseline) ·
12. [Risks](#-12-known-risks--unverified-claims) · 13. [Data Model](#-13-data-model-reference) ·
14. [Roadmap](#-14-full-project-roadmap-phases-010) · 15. [Phase 1 Deep-Dive](#-15-phase-1-deep-dive--file-io--canvas)

---

## ✅ 1. TL;DR

- **Stack:** TypeScript (React + Vite) frontend · Python (FastAPI) backend · PostgreSQL/Supabase (schema **applied & live** — designs CRUD).
- **Built:** **Phases 0–5 essentially done**: open/parse, **image auto-digitize** (TATAMI + SATIN + underlay + **holes/counters**), **text lettering**, **object-level editing** (→ server rebuild), Konva render, full color-stop editing, **export any format + convert + full production package ZIP**, worksheet PDF.
- **Measured (new):** a digitizer quality bench now exists — **v1 baseline scores 1.2/5, 0/10 fixtures customer-acceptable** ([`docs/benchmarks/v1-baseline-audit.md`](./docs/benchmarks/v1-baseline-audit.md)). Fast and crash-free, but background separation, color-layer retention, and satin-vs-tatami choice all fail.
- **Verified:** **pytest 90/90 (both segmentation tiers)** · **vitest 57/57** · typecheck/build · e2e lettering→digitize→edit→rebuild→validate→export/convert/package/thread-match/**optimize+quality+manual-draw** through the Vite proxy · **live Supabase auth + per-user cloud CRUD + real Dashboard + Phase 8 path-opt + manual digitizing (multi-user isolation) verified in Chrome**.
- **Still stubbed:** *neural* AI/ML (learned digitizing, text-to-design — Phases 8/9, need GPU/data), satin-stroke lettering, password reset. (Auth, cloud sync, path-opt + quality scoring are live.)
- **Next:** Phase 6 polish (password reset / Storage URLs), lettering v1.1, or push to GitHub (CI unverified).

---

## 🔄 2. Update History / Changelog

> Add a new row every time the project changes. **Newest at top.**

| # | Date | Author | Type | Summary |
|---|------|--------|------|---------|
| 141 | 2026-08-10 | Claude | 🐛 Fix | **CTO 1b — boustrophedon CELL DECOMPOSITION in `_scanline_fill`. The badge goes 47.0 → 22.65 machine-minutes and the corpus 97,590 → 65,004 stitches (−33.4%) with NO coverage loss on any fixture.** The CTO's attribution was right: `_scanline_fill` walked every scan row and emitted each of its runs back to back, so on an annulus the row above the counter is one run and every row beside it is two — the fill hopped left-run to right-run STRAIGHT ACROSS THE COUNTER, once per row, and `_route_travel` dutifully sewed a detour around the rim each time. Measured on badge Satin 3: 113 such connections at a 59.3mm median, which is the counter's diameter, manufacturing 16,989 stitches — 80% of the object. Part 13 fixed the equivalent hop BETWEEN components; the hop WITHIN one concave component was never addressed. Rows are now split into monotone CELLS at the critical rows where the run count changes, each cell is sewn to completion, and cells are visited nearest-first entering at whichever of their four corners is closest. **Isolated on a 300px annulus: 2,151 points before AND after — pure reordering, no coverage removed — with total jump distance 13,059.6px → 233.9px (−98.2%) and hole crossings 68 → 0.** Satin 3: 12,354 → 3,216 stitches, router jumps 171 → 68 at a 42.31mm → 3.64mm median, travel manufacturing 155 stitches instead of 16,989. Badge trims 76 → 28. Nine of ten CTO acceptance criteria met; **the jump COUNT target (10–20) is missed at 68 and is reported as missed** — the five remaining over-20mm jumps are the 11 disconnected tatami islands inside that "satin" object, a classification smell reported as a finding. Renders eyeballed: 07 and 03 clear the 0.995 SSIM gate against their PRE-1b baselines (0.997401, 0.995318) — a third of the stitches gone and the picture unchanged, because they were travel laid on covered ground; badge counter ink 12,550 → 12,699px, marginally MORE thread. **Three self-corrections: (1) keying the stagger phase to absolute canvas y reintroduced the STEP 3a position-dependence through `_scanline_angled`'s uncropped shallow-angle shortcut — caught by its own test, now anchored at the region's first scanned row; (2) cell entry scored by run midpoint when traversal always starts at a run END, worth 388 stitches and 4 trims on the badge; (3) the first regression test asserted on raw jump count and conflated intra-cell serpentine turns with inter-cell hops.** Also, on its own merits and NOT as part of 1b: `_skeleton_branches` built `pts = set(pt_list)` under a comment claiming "raster insertion order", which a set does not have — discovery ran in tuple-hash order against `_skeleton_adjacency`'s explicit instruction. Now `dict.fromkeys` + a raster-ordered node list; **measured effect −0.1% corpus stitches, coverage unchanged — a determinism fix, not a travel one.** 31 initial failures: 11 expected re-pins, **20 investigated rather than re-pinned**. The STEP 0 crossing negative control had lost its teeth (98 → 2) because 1b removed the defect upstream, so it now disables BOTH defenses instead of being quietly retired. **Quality bands RE-CUT, not carried over: the badge's 20%/4%/55min would have waved the entire pre-1b regression back through — 47 minutes passes a 55-minute band. Now 8%/1.5%/28.0.** New `tests/test_boustrophedon_cells.py`, 12 cases. One new OPEN divergence recorded: badge edited-rebuild trims vs digitize went 57/57 → 19/25 — rebuild improved 57 → 25, but 1b removed the noise hiding a +6 gap; raster-difference hypothesis UNMEASURED, deferred to 1c/3e-i. Both lanes green: **1,186 passed / 2 skipped** · **1,180 passed / 8 skipped**. Report: `docs/CTO-1B-BOUSTROPHEDON.md`. |
| 140 | 2026-08-09 | Claude | ✨ Feat | **Atelier P2 — ⌘K command palette and the mobile tab bar, mounted on both the Studio and the dashboard. Found and fixed a cascade bug in the shipped design file: THE TAB BAR COULD NEVER APPEAR AT ANY WIDTH.** `.sq-tabbar { display: none }` sat AFTER the `max-width: 64rem` query that sets `display: flex`, and both have the same specificity — so the later rule won unconditionally. Confirmed in a browser at 390px: four buttons in the DOM, nothing on screen. Base rules moved above the query, with the reason recorded there so nobody “tidies” them back down. **Verified by driving a real browser, twelve checks:** palette closed initially · Ctrl-K opens · `role="dialog" aria-modal="true"` present · focus lands in the input · 5 commands with no design loaded (the four design-scoped tool rows correctly absent) · typing filters 5 → 1 · Enter RUNS the command (theme light → dark) · palette closes after running · Esc closes · zero console errors · tab bar visible at 390px · 4 buttons. Both components mounted in the dashboard branch as well as the Studio — the palette is global and the tab bar is how you leave a dashboard section on a phone. tsc clean · vitest **186 passed** · build clean. |
| 139 | 2026-08-09 | Claude | 💄 UI | **Atelier P1 — the whole app moves to the new design language, and the STUDIO IS THEMABLE for the first time. 74 hard-coded colour literals in `index.css` down to 8.** Design handoff received (`docs/design/ATELIER-HANDOFF.md`, reference comp archived beside it): warm paper, Instrument Serif display, antique-brass accent, mono numerals — replacing the violet-on-near-black scheme the handoff calls generic. Four files shipped as-is per the brief: `styles/atelier.css` (token layer), `lib/useTheme.ts`, `components/command/CommandPalette.tsx`, `components/nav/MobileTabBar.tsx` (the last two mounted in P2). **The restyle is one pass over token DEFINITIONS, not 74 edits.** `index.css` already had three token blocks, so `--bg/--panel/--text/--accent` and `--dz-*` are now aliases onto `--sq-*`; every rule still reads `var(--bg)` and no markup changed. Remaining literals: seven `#fff` on saturated accent fills (correct in both themes) and one categorical series hue that must stay distinguishable. **Two things that would have broken silently and were caught by reading, not luck.** (1) `data-theme` moved to `<html>`, which means `.dz-root[data-theme='light']` in `index.css` could NEVER match again — the dashboard would have been stuck dark forever. That block is deleted; atelier.css supplies both surfaces and the aliases follow. (2) `useTheme()` only runs while the dashboard is mounted, so landing straight on the Studio would leave `<html>` bare and fall through to atelier's LIGHT `:root`. The resolved theme is now stamped once in `main.tsx` before first paint. **`theme.test.ts` inverted:** it asserted every `.dz-root` carries `data-theme`; that rule is now exactly backwards, so it asserts NONE does, plus that `<html>` is stamped at startup and that DashShell routes changes through the one hook. Same defect class either way — a surface whose theme disagrees with the rest of the app. Verified in a browser on both surfaces: `html[data-theme]` resolves, body background `rgb(19,18,17)` dark / `rgb(247,244,238)` light, **zero console errors**. tsc clean · vitest **186 passed** · build clean. P2–P5 (palette/tab bar, Studio restyle, dashboard expansion, analytics + landing) tracked and open. |
| 138 | 2026-08-09 | Claude | 🧪 Test | **STEP 3d — the last verdict item. The hash lock now REFUSES to re-pin a stream that fails its quality bands, and records the measurements beside each hash so a re-pin is auditable in the diff.** The verdict said “gate on quality bands not hashes”, and the sharper reading is that **the hash was never the problem** — an exact hash catches ANY change, which is exactly right for a performance optimization that should be byte-identical. What it cannot do is tell a good change from a bad one. **The weak point is the moment of re-pinning:** one command, no questions asked. That is how a travel resampler with a 2-pixel floor got locked in as the new baseline at 70.8% manufactured stitches, 44.4% sub-floor penetrations and 21.3 machine-minutes with every gate green. `STITCH_LOCK_WRITE=1` now computes sub-0.5mm share, sub-0.3mm share, max stitch length and machine time (stitches **plus** 2.5s per trim), checks them against per-fixture bands and **asserts rather than writing** if any fails. **Verified end to end, not assumed:** with the badge's machine-time band temporarily set to an impossible 1.0, the re-pin was refused with “47.0 machine-minutes (35,091 stitches + 75 trims), band 1.0” and `stitch-hashes.json` was left byte-identical. The lock file now carries `{hash, stitches, trims, sub_floor_share, sub_penetration_share, max_stitch_mm, machine_minutes}` per fixture, the committed numbers are re-checked on **every** run (so a band tightened after a re-pin still applies to the stream already locked), and a negative control feeds the STEP −1 regression's real numbers to the checker and requires ≥3 objections. Bands measured for THIS lock's configuration, which is not the bench's: 04 0.0%/0.0%/2.7min · 05 0.4%/0.2%/2.7 · 06 6.2%/0.9%/2.6 · 07 14.6%/1.1%/47.0. The badge's 20% band is set to catch a RETURN of the pixel floor (29.9% here), not to bless 14.6%. Old bare-string lock entries still gate on the hash rather than erroring. Hashes unchanged — no output change in this commit. Both lanes green: **1,174 passed / 2 skipped** · **1,168 passed / 8 skipped**. **STEP −1 → 3d complete; B2 is unblocked.** |
| 137 | 2026-08-09 | Claude | ♻️ Refactor | **STEP 3c — the (c) ruling, applied to the satin sweep: ONE implementation, two callers. Byte-identical output on both paths across four fixtures, verified by stream hash before and after.** The verdict ruled one generation core with digitize and rebuild as thin wrappers, and the reason was empirical: both paths called the same generators, each computed the arguments itself, and **the arguments drifted ten times in this series** — `route_pad` 8px vs 2px · the coalesce clamp · the penetration floor · the border sweep width (duplicated, waiting to drift) · the satin underlay axis · pull compensation applied twice · the wide-remainder pitch · the wide-remainder angle · the PARALLEL underlay's base angle · the trim gate. **None catchable by a stitch hash**, because the hash moves with the regression. New `generation.py` (layer between `underlay` and `routing`) holds `spine_satin` → a frozen `SatinAttempt` reporting points, the skeleton's own `median_w`, the uncovered share, the medial axis and a `reason`. It REPORTS and does not decide: digitize turns `reason` into a classification, rebuild uses `viable` to choose spine vs bounding-rect sweep for an object the user already declared SATIN. **rebuild no longer reads `SATIN_MAX_W_MM` or `SATIN_MAX_UNCOVERED` at all.** Three of the ten divergences lived in this sweep and all three were shipped by `250e850`; there is no second copy left to drift from. **Scoped honestly:** classification stays in `pipeline.py` — deciding from pixels whether a region should be satin, which holes to knock out, whether a band wants a contour fill, which underlay a stroke's width earns is digitize's job and rebuild deliberately does not do it (the object is the source of truth). What both must agree on is what a *decided* object turns into, and that is what moved. The fill path remains classification-bound and is called out rather than half-extracted. **12 new core tests** including a negative control that the caps are not read outside the core, an AST check that neither caller reaches past it to `_skeleton_satin_hires`, and one pinning both halves of R2 — pull must widen the emitted COLUMNS but must NOT widen the width MEASUREMENT, since that measurement is the classification gate. **Three earlier structural tests failed and were right to** — they noticed the code had moved; repointed at `generation.py` and STRENGTHENED to assert both ends of each chain (the core's parameter *and* what each caller feeds it). Both lanes green: **1,164 passed / 2 skipped** · **1,158 passed / 8 skipped**; no re-pin. |
| 136 | 2026-08-09 | Claude | ✨ Feat | **STEP 3b — rebuild now works on the raster digitize actually used. Mean worst-object loss 16.6% → 12.0%, mean |total ratio − 1| 0.042 → 0.028; fixture 01's worst object goes −4.4% → +0.5% and 04/06 land exactly on 1.00.** Every generator in the package is a raster operation — thinning, distance transforms, scanlines, contour tracing — so the grid is part of what produced a design's stitches, and rebuild had no way to know it. digitize runs at **0.075 mm/px (13.3 px/mm)** on the bench corpus; rebuild picked **0.1 mm/px** from the object bounding box, a third coarser, so the two paths measured different stroke widths and laid rows at a different pitch on the same shape. New `Design.source_mm_per_px` records it at digitize and rebuild honours it, still bounded by the 2000px canvas cap so a stored value cannot make an edit allocate an enormous raster. `None` (an older save, or an imported stitch file that never had a raster) falls back to exactly the previous behaviour, and `model_copy(update=…)` carries the stamp through a rebuild so a second edit lands on the same grid. **Measured, rebuild @ own grid → rebuild @ digitize's grid** (ratio, worst object): 01 1.08/−4.4% → **1.06/+0.5%** · 04 0.94/−10.1% → **1.00/−5.1%** · 05 1.00/−7.1% → 1.00/**−8.3%** · 06 0.98/−18.9% → **1.00/−13.5%** · 07 0.96/−36.2% → **1.03/−25.5%** · 02 0.95/−23.1% → **1.08**/−20.0%. **Not uniform, and said so:** 05's worst object goes slightly the wrong way and 02's total ratio gets worse even as its worst object improves. P6 bands re-tightened to where each fixture actually landed. Badge −25.5% and fine-text −20.0% remain the real gap, outstanding rather than certified. **Correction:** earlier notes said digitize rasters at “~18px/mm” — that was read off the constants, not measured. The measured value is **13.3 px/mm**; four comments corrected. Both lanes green: **1,151 passed / 2 skipped** · **1,145 passed / 8 skipped**; no re-pin (digitize output unchanged — only a field added). |
| 135 | 2026-08-09 | Claude | 🐛 Fix | **STEP 3a — a fill was a property of WHERE THE OBJECT SAT, not of the object. Translating a shape 100px re-phased its rows by up to 35.16px; growing the canvas around an untouched region moved every stitch by 0.83px. Now 0.0px and ~3e-14 respectively.** `_scanline_angled` rotated the mask about the CANVAS centre (`_warp_fit(region, (w/2, h/2), angle)`), and `_warp_fit` then offsets the fitted output by `(nw − w)/2`, which depends on the canvas dimensions too — both terms leak the canvas into the row grid. Three consequences in ascending order of seriousness: digitize rasters at ~18px/mm on the source image while rebuild uses 10px/mm on the bounding box of every object, so **the two paths could never agree on the grid even in principle** (part of the residual P6 gap); adding or deleting an unrelated object changes the design bbox and **re-phases the fill of every object left alone**; and **B2 is move/scale/rotate** — moving one object would have silently restitched it, the opposite of what a transform means. The fix crops to the region's own foreground window and rotates about the centre of that, so fitted size, rotation centre and row phase depend on nothing but the region's own pixels — the way `_skeleton_satin_hires` already worked. **Corpus impact is small, as expected** (row phase moves, row count barely): 01 8,749 → 8,809 st · sub-0.5mm 3.8% → 4.0% · 13.73 → 13.8 min; 07 34,665 → 34,817 · 14.9% → 14.7% · 46.54 → 46.6; 05 1,864 → 1,811 · 1.0% → 0.3%. All inside the existing bands, no band widened. Re-pinned 4 stream locks and 10 visual baselines. **21 new tests** sweep six angles for canvas-invariance, translation-equivariance and jump-flag stability, plus an empty-mask case and a negative control that reproduces the old rotation inline and proves the probes reject it. **Flagged, not fixed:** the badge's arc lettering is illegible and its upper-left ring has gaps — verified PRE-EXISTING (only 2,573 of 630,436 px changed, mean abs diff 0.201), and it is B3/B4 work. Both lanes green: **1,146 passed / 2 skipped** (default) · **1,140 passed / 8 skipped** (no-pass-through). |
| 134 | 2026-08-09 | Claude | 🧪 Test | **STEP 2 — a second CI lane runs the whole suite with rebuild's pass-through DISABLED. Switching it on the first time failed exactly SEVEN tests; the CTO predicted ≥5.** Rather than read for the hollowed-out regressions I found them empirically: `STITCHIQ_NO_REBUILD_PASSTHROUGH=1` patches `rebuild_is_a_noop` to always return False, so any test that only passed because `rebuild_design` returned its input names itself. **Five of the seven legitimately have the pass-through as their SUBJECT** — `test_probe6_an_unedited_design_passes_through_untouched`, `test_the_passthrough_is_real_and_force_defeats_it`, `test_unedited_rebuild_is_byte_identical`, `test_recolour_and_rename_still_pass_through`, `test_fingerprint_survives_a_json_round_trip` — and carry `@pytest.mark.needs_rebuild_passthrough`, which skips them in that lane. **Two were hiding real properties and are rewritten to ask them where they can fail:** **(1) `test_repeated_rebuilds_never_accumulate_damage`** ran five UN-forced rebuilds, every one of which short-circuited, so it compared `d` to `d` five times and proved nothing about C6. Asked properly, the answer is **good and worth having**: digitize 8,903 → first regeneration 9,664 → then **bit-identical for every pass after**. Rebuild reaches a fixed point in ONE step, because the raster is driven by stored contours that rebuild never rewrites. That is the actual C6 guarantee and it is now provable. **(2) `test_fidelity_criterion_holds_verbatim`** asserted `worst == 0.0` — true only because nothing was regenerated. The docstring now says so plainly: the review's “no coordinate moves by >0.1mm” **holds for the shipped path and only that path**; regeneration moves the stitch count 8,903 → 9,664, where the criterion is not even well-defined. That gap is banded as P6 rather than hidden behind the assertion. **The escape hatch is watched.** `test_the_passthrough_marker_is_not_a_dumping_ground` walks the AST of every test file and fails if a marker appears that is not on an explicit allow-list — silencing a future failure requires editing that set, which is visible in review. Two more guards pin that CI actually runs the lane and that the switch actually switches (including that the DEFAULT lane is not accidentally patched). **Both lanes green: 1,125 passed / 2 skipped (default) · 1,119 passed / 8 skipped (no-pass-through).** CI backend time roughly doubles to ~45 min, which is the price of the class of defect this closes. |
| 133 | 2026-08-09 | Claude | 🧪 Test | **STEP 1 — the parity signal restored. P6 was comparing a design to ITSELF and could not fail; with the pass-through disabled the regenerated stream differs from digitize's by up to 8% overall and 36% on a single object, none of which was visible.** The §8 probes only ever exercised `digitize_image`, and B1.5's provenance short-circuit (`if rebuild_is_a_noop(...): return design`) turned that gap into a blind spot: an unedited design rebuilds to *the very same object*, so P6 — “does rebuilding an unedited design reproduce its stream?” — asserted stitch counts within 1%, coordinates within 0.1mm and matching commands **against `d` and `d`**. Six probes were hollowed out the same way (CTO verdict V2, “probe-gaming”). **`rebuild_design(design, *, force=False)`** now exists so the suite can measure the generators instead of the short-circuit; production never sets it, and a test pins BOTH halves — the un-forced call must still pass through (re-stitching an untouched object is the defect B1.5 fixed) and `force=True` must genuinely regenerate. **New `tests/test_probes_three_paths.py` runs every probe on all three paths** — digitize · rebuild-unedited (forced) · rebuild-after-a-real-1%-edit. P1 ring crossings now banded per path (digitize 0, rebuild ≤2; the edited path read **98** before STEP 0 while the probe reported 0). P2 lock coverage on all three. P4 gains the counterpart it never had: an UNEDITED satin must keep its direction through a forced rebuild — the risk the v131 provenance short-circuit introduced. P5 on both rebuild paths, and **digitize has no appliqué path by design**, pinned by a test rather than asserted in a comment. **P6 restored with real bands** (measured, digitize vs forced regeneration): 01 8,374→9,084 (1.08, worst object −4.9%) · 04 1,560→1,470 (0.94, −10.1%) · 05 1,345→1,343 (1.00, −6.8%) · 06 1,284→1,256 (0.98, −18.9%) · **07 10,759→10,322 (0.96, −36.2%)** · **02 7,296→6,821 (0.93, −23.1%)**. The badge and fine-text figures are the remaining real gap and are recorded as outstanding, not certified. Two meta-tests keep the bands honest: they must reject a half/double-size design, and the STEP −1 regression (1.95× on fixture 01) must fall outside them. **Process note:** writing P2's lock criterion out by hand here immediately produced a STRICTER rule than P2's own — it failed all three paths including digitize, which P2 passes. It is now IMPORTED. Same lesson as STEP 0: a copied constant drifts. Suite **1,122 passed, 2 skipped, 2 deselected, 3 xfailed**; no re-pin; ruff clean on touched files (one pre-existing RUF007). |
| 132 | 2026-08-09 | Claude | 🐛 Fix | **Four more arguments digitize passes to a shared generator that rebuild did not — plus the silent object delete now RAISES. Trims on an edited rebuild: 02_logo_fine_text 12 → 5, 07_circular_badge 63 → 55 (−17.5s and −20s of machine time).** Found by an independent four-lens audit of the two per-object emission loops, then verified at the line; the audit's satin claims were correctly REFUTED because v131 had already fixed them. All four are the `route_pad` defect shape — same generator, different arguments — and none could be caught by a stitch hash, because the hash moves with the regression. **(G1) the wide-remainder tatami took the SATIN pitch.** When a satin stroke has a patch too wide for a column both paths tatami it; digitize paces those rows at the fabric's fill pitch `prof["row_mm"]`, rebuild used `spacing_px`, the object's satin column pitch. Separate profile keys that coincide only on cotton (0.40/0.40 — knit 0.50/0.45, jersey 0.55/0.50) and are unrelated after any density edit, which is exactly why the all-cotton corpus hid it. **(G2) …and filled it at 0°.** digitize passes `angle_deg=_fill_angle(wide_mask)`; rebuild omitted the keyword and silently took the 0.0 default, so every wide patch came back with horizontal rows crossing the satin columns it sits between. **(G3) the PARALLEL underlay crossed the wrong angle.** `FILL_UNDERLAY_ANGLE_OFFSET_DEG` exists to lay the crossing layer 90° off the top rows, and digitize keeps that invariant by construction. Rebuild passed the raw stored `stitch_angle` — a different number for contour/spiral/radial fills (rebuild's own code says the stored angle “does not describe this object”) and for anything with a Stitch Flow line, so a user who drew a flow line 90° off got an underlay running PARALLEL to the rows it was meant to brace, at 3× their pitch. Now each branch records the angle it actually filled at. **(G4) every object transition trimmed.** digitize gates on `TRIM_MIN_GAP_MM` (v2 Part 48) because a trim costs ~2.5s; rebuild trimmed unconditionally. **Also: the fabric profile.** `Design.fabric_type` existed all along and rebuild used cotton constants regardless — edit one object on a fleece design and it was re-stitched with cotton underlay spacing (2.0 vs 1.5mm) and inset (0.6 vs 0.8mm). `_fabric_profile` was already a shared helper; rebuild just never called it. **And the silent delete is gone:** `if len(pts) < 2: continue` dropped an object with no error and no log — measured live on two fixtures in v131 — and now raises naming the object and its type. **Measured** (digitize vs rebuild after a real 1% edit, before → after): 04 5→4 trims · 05 ratio 1.02→1.00 · 06 6→5 trims · 07 63→55 · 02 12→5; no object drops anywhere, no raises on the corpus. Honest note: I expected G4 to be a large machine-time win and it is not — on 01 rebuild already trimmed FEWER times than digitize (58 vs 77), so the gate's value is correctness and the badge/fine-text savings, not a headline number. Suite **1,101 passed, 2 skipped, 2 deselected, 3 xfailed**; no re-pin (digitize untouched); ruff 11 pre-existing. |
| 131 | 2026-08-09 | Claude | 🐛 Fix | **The three defects `250e850` left behind — and two of them were silently DELETING AN OBJECT. 06_wordmark_script edited-rebuild 733 → 1,263 stitches against digitize's 1,284; worst single-object loss −85.0% → −18.9%; 6/7 → 7/7 objects survive.** `250e850` moved rebuild's satin TOP LAYER onto the medial axis and left three things behind, all found by measuring: **(R1) the underlay stayed on the bounding rect.** `_rebuild_underlay` called `_center_walk(mask, rect, …)` — a straight line through the centroid at the stored angle — while the column above it followed the spine. On a ring or an arc that line leaves the column entirely and the underlay is sewn on bare fabric. digitize never had this: `_satin_width_underlay` takes `axis_pts` precisely so the two layers agree. Rebuild now threads the spine through and falls back to the rect midline only when the column really came from the rect sweep. **(R2) pull compensation was applied twice.** Rebuild measured the already-`_dilate_pull`-ed mask AND passed `pull/2` again as the extra column half-width — the exact failure `pipeline.py` warns about at its own call site (“measuring the pre-dilated mask would fold pull comp into the width test and push a 3.66mm stem over the cap”). Both the DT pre-measure and the skeleton call now take the undilated `mask`. **(R3) the auto-angle gate was tighter than its own noise floor.** The 0.15° tolerance compared a stored angle (rounded to 1dp, measured by digitize at ~18px/mm) against a `minAreaRect` recomputed on the same contour requantized to rebuild's 10px/mm grid with integer truncation. Measured on a fresh digitize with **no edits at all**, so every delta is pure requantization noise: 04 → 6/6 satins took the spine, 05 → 4/6 (max delta 3.700°), **06 → 2/7 (max delta 0.637°)**. Five of seven untouched script columns were being sent to the very path B1.5 moved them off. The numeric test is now the FALLBACK; the primary test is provenance — `is_unedited` means the angle IS digitize's own by definition, whatever the raster says — and the fallback band is `SATIN_ANGLE_AUTO_TOL_DEG = 1.0°`, above measured noise and below a UI drag, with 180° wrap. **Measured before → after** (digitize vs rebuild after a real 1% density edit): 04_thin_line_outline 1,465 → 1,477 st · worst −11.4% → −10.1% · 6/6 survive · 05_wordmark_caps 1,058 → 1,368 st · worst −86.6% → −6.8% · **5/6 → 6/6 survive** · 06_wordmark_script 733 → 1,263 st · worst −85.0% → −18.9% · **6/7 → 7/7 survive**. The survival columns are the headline: a rect sweep across a curved script stroke yields under two points and `rebuild.py`'s `if len(pts) < 2: continue` dropped the object without a word. That silent-delete is still on the ALSO FIX list and lands next, now that it is provably not firing on the corpus. P4 (an edited angle must change the stream) re-checked and green — the provenance short-circuit only fires on objects the user did not touch. Suite **1,086 passed, 2 skipped, 2 deselected, 3 xfailed**; no lock or baseline re-pin needed (digitize output untouched); ruff 11 pre-existing, one FEWER than HEAD because the unused `axis_pts` was itself the R1 smoking gun. |
| 130 | 2026-08-09 | Claude | 🐛 Fix | **CTO verdict STEP 0 — digitize and rebuild finish a segment with the SAME parameters, via shared FUNCTIONS rather than copied constants. Editing the ring no longer reopens its counter: 98 needle paths across the open hole → 1.** Both paths call the same finishing generators (`_coalesce_short`, then `_route_travel`) and each computed the arguments inline, so they had drifted: `route_pad` was `(FILL_BORDER_MM/2 + pull)/mm_per_px` = **8px** on digitize and `pull/mm_per_px` = **2px** on rebuild, and the coalesce call clamped a FILL's minimum to its own row pitch (`FILL_ROW_CONNECT_KEEP`) and passed the penetration floor for satin **only on digitize**. Border satin and pull comp land stitches outside the region, so at 2px their jump endpoints read “outside” and were never routed — they shipped as trimmed jumps straight across the counter. C2 was CLOSED on digitize and OPEN on every edit, and the §8 probes could not see it because they only exercise digitize (that blindness is STEP 1). Now `routing.travel_route_pad_px` and `routing.coalesce_params`, called from both. **A third copy fell out of the structural guard:** the border sweep width `max(2.0, FILL_BORDER_MM/mm_per_px)` was derived inline in `pipeline.py` and again in `rebuild_fill_border`. Not drifted yet — which is exactly how `route_pad` started — so it is now `satin.fill_border_width_px`. **Measured.** Ring counter crossings after a real 1% density edit **98 → 1** (digitize 0, unchanged). Fixture 01 digitize output **bit-identical** at 8,749 st / 3.8% sub-0.5mm / 13.73 machine-min — zero blast radius, no re-pin of locks or baselines. Fixture 01 edited-rebuild total 6,262 → 9,084 st. **AND THIS CORRECTS MY OWN STEP −1 WORK.** On the sub-0.5mm number alone STEP 0 reads as a 24× regression (0.4% → 9.6%) and I nearly rejected a correct change. The entire difference sits between **0.45 and 0.50mm** — the tatami row-to-row connection, one row pitch long BY CONSTRUCTION and industry-standard at 0.4–0.45mm, which `FILL_ROW_CONNECT_KEEP` exists to preserve. At `MIN_PENETRATION_MM` (0.3mm), where the needle actually re-enters its own hole, the two paths are at parity: **1.0% rebuild vs 0.8% digitize**. The old 0.4% was not a better result — it was the over-aggressive 0.5mm coalesce deleting the first point of every row and receding the fill edge, the defect v2 Part 15 fixed on the digitize side and never on this one. The harness now records `sub_penetration_share` and the bands assert BOTH lines. This also revises v129's claim about the badge: its 14.9% “worst in the corpus” is **1.2%** at the damage line. **Guarded structurally, not just numerically.** `test_step0_shared_finishing.py` walks the package AST and fails if any module re-derives a quantity another one owns — a test pinning only today's values would pass again the moment someone copied the constant back. Plus a negative control that forces the old 2px pad and asserts the counter reopens, so a probe that stops measuring cannot read as a pass. Suite **1,074 passed, 2 skipped, 2 deselected, 3 xfailed**, vitest 184, tsc clean, ruff unchanged from HEAD (12 pre-existing, none on touched lines). |
| 129 | 2026-08-09 | Claude | 🐛 Fix | **CTO verdict STEP −1 — the travel resampler's floor was 2 PIXELS, not 2 millimetres. Fixing the reference: 01_flat_2color_logo 17,078 → 8,749 stitches (−48.8%), sub-0.5mm share 44.4% → 3.8%, 21.4 → 13.7 machine-minutes.** The verdict (`docs/CTO-VERDICT-2026-08-09.md`, committed as `757ad0e`) **inverts the B1.5 ordering**: rebuild was being chased toward parity with a digitize output that is itself the defect. Digitize put **44.4% of consecutive stitches under `MIN_STITCH_MM`** and 36.8% under `MIN_PENETRATION_MM` at 21.3 machine-minutes; rebuild's “lossy” output was 2.1% / 0.4% at 8.4 minutes. Parity executed perfectly would have taken rebuild from 0.4% to 40.0% floor violations and doubled machine time. Fix the reference first. **Root cause, measured not reasoned:** `resample_inside` in `_route_travel` halved its travel pitch until chords cleared the boundary, bottoming out at `while pitch >= 2.0` — **2.0 pixels**, which at the working resolution is about a tenth of a millimetre. So on every concave boundary it re-injected exactly the sub-floor penetrations `_coalesce_short` had removed one line earlier. **70.8% of every shipped stitch on fixture 01 was manufactured here.** The floor is now `MIN_STITCH_MM / mm_per_px`, threaded from both call sites and derived from the caller's own travel pitch when absent, so no direct caller can silently reinstate a pixel floor. **Second cause:** a travel run could cost arbitrarily more than the jump it replaced — walking most of the way around a region to dodge one short move is sewn thread, machine time and needle wear standing in for a single 2.5s trim. New `DETOUR_COST_MAX = 2.0`, chosen by sweep on machine time (3.0/2.5 → 14.8 min; 2.0 → 13.7; 1.5 → 12.9 but outside the verdict's 2–3× band, so reported and not taken). **Measured before → after** (and my own first measurement of this was WRONG — I patched `routing._route_travel` while `pipeline.py` binds the name at import, so the “before” column was really “new floor, no cost cap”; re-measured through `pipeline._route_travel`): **01_flat_2color_logo** 17,078 → 8,749 st · 44.4% → 3.8% · 21.4 → 13.7 min · **07_circular_badge** 45,793 → 34,665 st (−24.3%) · 29.9% → 14.9% · 58.0 → 46.5 min · **05_wordmark_caps** 2,518 → 1,864 st (−26.0%) · 15.0% → 1.0% · 3.4 → 2.7 min. **The bench could not see any of this**, which is the deeper finding: it measured stitch counts for a whole phase and never once asserted a fixture was SEWABLE, and exact-hash gating cannot catch a regression the hash simply moves with. Added `sub_floor_share` and `machine_minutes` (est. time **including** 2.5s per trim, so a routing change cannot shift cost sideways from stitches to trims and look like a win) to the harness, plus `SEWABILITY_BANDS` assertions on three fixtures — **bands, not hashes**, wide enough not to trip on a legitimate generator change and tight enough to fail if the pixel floor returns. The badge keeps a deliberately wider 20% band: at 14.9% it is still the worst fixture in the corpus and is called out as outstanding work rather than papered over. Re-pinned 4 stream locks and 10 visual baselines (legitimate output change) and eyeballed 01 and 07 renders — full coverage, no travel over bare fabric. Suite **1,067 passed, 2 skipped, 2 deselected, 3 xfailed** (1009s), ruff clean on touched lines. |
| 128 | 2026-08-08 | Claude | ⚡ Perf | **CTO B1.5 (3/3) — rebuild sews satin off the MEDIAL AXIS, like digitize and like the competition. Editing a curved satin no longer wrecks it.** Per the CTO's direction ("do it now in B1.5") and the competitor model that settled the other half: in Wilcom/Hatch/Ink-Stitch the OBJECT is the source of truth and stitches are derived, so the same generator must serve both paths — rebuild using the bounding-rect zigzag while digitize used the skeleton meant editing one parameter of a ring, arc or bend replaced a spine-followed column with a rect sweep that \"only ever worked for a straight bar\" (Part 4). Rebuild now calls `_skeleton_satin_hires` with digitize's own arguments, including the wide-remainder tatami fallback. Measured on the all-satin fixture, a 1% density nudge: **−35%…−58% on nine of ten objects → −6%…−10% on eight**, two outliers left (−37%, −42%). **Two gates, both found by tests rather than reasoning.** (1) A spine has NO single angle, so running it unconditionally would have silently undone A5 and re-broken probe P4. The stored angle is compared against the contour's own normalized axis: equal means auto (the user never touched it) → spine; different means an explicit override → rect sweep. That is also how the commercial tools behave. (2) The first width gate used the distance-transform median and **it under-reads a rectangle badly — 3.6mm for the 8mm probe bar** — so an over-cap bar sailed through and came back as horizontal tatami rows, failing the angle probe. The gate now uses the skeleton's OWN `median_w` plus `SATIN_MAX_UNCOVERED`, exactly digitize's test; over-cap shapes fall back to the rect sweep, because digitize would reclassify them as tatami but the user has explicitly asked for SATIN here — which is precisely why Part 4 retained that path. Fills unchanged this commit (Fill 1 still −68%, the v127 diagnosis stands). Suite **1,063 passed, 2 skipped, 2 deselected, 3 xfailed** (1223.88s), vitest 184, tsc clean, ruff clean on touched files. |
| 127 | 2026-08-08 | Claude | 🔬 Diagnosis | **B1.5 remainder diagnosed: the parity gap is NOT a missing generator — a `DesignObject` does not describe its own stitches.** Chasing the residual −68% on fixture 01's Fill 1, I ran **digitize's own generators** on **that object's own stored contour, holes, density and angle**, at **digitize's own working resolution** (18.6px/mm), and summed every stage: fill rows 2,401 + border satin 1,700 + edge walk 106 + parallel underlay 550 = **4,757 stitches. The stored object has 15,453.** Rebuild is not missing a stage — reproducing digitize's entire recipe from the stored parameters still yields under a third of the stitches. A step-length histogram of the stored object says why the arithmetic never closed: **70.2% of its stitches (10,846 of 15,461) sit at the 0.5mm coalesce floor**, a population none of those four generators emits (fill grid 11.3%, border width 9.7%, travel/underlay 4.5%). So the object's parameters are **lossy** with respect to what digitize actually did to the region — something between segmentation and emission shapes the stream in a way `contour + density + angle + underlay_type` does not capture. **This reframes the remaining work**: B1.5's second half is not "call the missing generator in rebuild", it is either (a) find and record the missing determinant on the object, or (b) accept that an edited object is regenerated rather than reproduced, and make the regeneration good on its own terms. That is an architecture decision, so it is handed back rather than guessed at. Also noted for whoever picks this up: a fill whose stitches are 70% minimum-length is worth a quality look in its own right — the A9 scorer's tiny-stitch gate is 0.3mm and does not catch a 0.5mm population. Diagnosis only, no code change; no suite run required. |
| 126 | 2026-08-08 | Claude | 🐛 Fix | **CTO B1.5 (2/2, PARTIAL) — fill-border satin restored on rebuild; the parity gap is measured, and it is far worse than the review said.** **Measurement first, and it reframes the item.** The review describes the gap as rebuild "omitting fill-border satin and full underlay recipes". Measured by nudging ONE object's density by **1%** — a change that should move its stitch count by about 1% — and comparing that object before and after rebuild: fills **−76%** and **−47%**, satin **−35% to −58%** on nine of ten objects (one **+31%**). Editing any parameter does not adjust an object, it **replaces it with a much sparser one**. That is the real severity of C6, and it is why the CTO put B1.5 ahead of transforms. **Fixed here:** rebuild now applies digitize's fill-border satin — same generator, same `FILL_BORDER_MIN_MM2` area gate, same 0.6mm half-width — to TATAMI / CONTOUR_FILL / SPIRAL_FILL / RADIAL_FILL. Fill 2 recovered **−47% → −12%**. **NOT fixed, stated plainly:** Fill 1 remains **−68%**, so **parity is NOT achieved and this row does not claim it**. The residue is structural — the digitized fill's median stitch step is 0.503mm against rebuild's ~4mm grid, so the two generators are not laying the same geometry — and diagnosing that is the remaining work. **Satin parity is deliberately NOT attempted here:** rebuild uses the bounding-rect `_satin_zigzag` while digitize uses the medial-axis skeleton, and porting the skeleton into rebuild now would be thrown away by **B3 (spine-based satin engine)**, which is exactly that subject. Recommend folding rebuild's satin parity into B3 rather than paying for it twice — **flagged for the CTO to confirm, not decided unilaterally.** **Structural fix instead of a fifth shave:** adding the border pushed `pipeline.py` over the 1,500-line layering gate for the FOURTH time this session. Rather than trim comments again to squeak under it, `rebuild_design` moved to its own module — measured first: it references **zero** names defined in pipeline.py, so the two entry points were sharing a file and nothing else. pipeline 1,503 → **1,224**, new `rebuild.py` 345, registered as the top layer. Suite **1,063 passed, 2 skipped, 2 deselected, 3 xfailed** (830.34s), vitest 184, tsc clean, ruff clean on touched files. |
| 125 | 2026-08-08 | Claude | ⚡ Perf | **CTO B1.5 (1/2) — rebuild stops damaging what you didn't edit; probe P6 flips to green, verbatim.** C6's real cost is cumulative: rebuild re-rasters everything, so every Apply and every Optimize roughened every edge and repeated edits compounded it. A8's 10px/mm raster shrank the damage but could not remove it — **regenerating from a contour approximates coordinates, it cannot reproduce them**, so the review's "no coordinate moves >0.1mm" criterion was unreachable by better rasterisation alone. Fix: notice there is nothing to do. Each object now carries `params_hash`, a fingerprint of everything that determines its stitches (geometry + generator parameters), stamped by digitize AND re-stamped by every rebuild so provenance survives an edit; a design whose objects all still match — in the order their stitches actually appear — is **returned untouched**. P6 now holds exactly: worst coordinate move **0.0000mm**, not merely ≤0.1mm, and five consecutive rebuilds move nothing. **Two design traps found and avoided, both by tests rather than by luck.** (1) The fingerprint deliberately IGNORES `sequence_order`, so 'every object unedited' is NOT sufficient — Optimize reorders objects and edits none, and short-circuiting there would make Optimize a silent no-op. Caught by the reorder regression, which failed on the first implementation; the check now verifies objects appear in the order their **entry points occur in the stream**. (2) The first design recorded a `stitch_start` index per object to splice unedited objects individually — **abandoned after measuring that it is unsound**: digitize rewrites the stream after emitting bodies (`_lock_stream` inserts ties inside every block), so a recorded index is stale on return — fixture 01's first object ended **2.88mm** from its own stored exit point. The field was REMOVED rather than shipped looking plausible; per-object splicing needs boundary tracking through those passes and is the **remaining half of B1.5** along with generator parity (fill-border satin + underlay recipes for EDITED objects). Legacy safety: no fingerprint (every pre-B1.5 save, every imported file) → regenerates exactly as before. Pipeline crossed the 1,500-line gate twice more; extracted `provenance.py` (new bottom layer) and moved `_uncovered_chunk_mm2` to geometry. 18 regressions covering both directions — a pass-through that fires wrongly is data loss, one that never fires is the original defect. **A third discovery, from verifying my own claim rather than asserting it.** I told the CTO the two paths' differing `stitch_count` convention made the quality walker mis-slice REBUILT designs; measuring it showed the mis-slicing hits **both** paths equally (1 of 2 objects on fixture 01, digitized and rebuilt alike) and the dominant cause is the same post-emission rewrite, not the convention. So `penetration_metrics` has been attributing per-object numbers to the WRONG object — measurement integrity, which this project's whole discipline rests on. `_object_slices` now anchors each slice on the object's recorded `entry_point` instead of counting: misattribution **1/2 → 0/2** in both paths, and an object whose anchor cannot be found yields an EMPTY slice rather than a wrong one (no measurement beats a misattributed one). **Timing tests made honest:** the two loop-lag guards pass standalone and failed inside the full suite — a loaded machine makes scheduling delay meaningless. Rather than loosen the ratio until they could not fail, they carry a `timing` marker, are deselected by default, and run via `pytest -m timing`; the always-on structural guard is what actually prevents regression. Suite **1,062 passed, 2 skipped, 2 deselected (timing), 3 xfailed** (839.81s; +18 parity regressions, P6 xfail→pass), vitest 184, tsc clean, ruff clean on touched files. |
| 124 | 2026-08-08 | Claude | 🚀 Deploy | **CTO B1 (2/3) — deployable: two images, compose, Sentry both apps, and a readiness probe that can actually fail.** **Images.** `apps/backend/Dockerfile` (python:3.11-slim — the same pin CI uses, because the stream locks and SSIM baselines are float-sensitive instruments captured on 3.11; changing that line changes stitch output), build deps purged in-layer, U2-Net weights baked in best-effort, non-root, **gunicorn + UvicornWorker**. Worker PROCESSES, stated plainly in the runbook: the measured 1,198ms stall is GIL contention, so threads cannot fix it and `WEB_CONCURRENCY` is the only lever until the queue lands. `apps/frontend/Dockerfile` builds the workspace from the repo root (installing from inside `apps/frontend` resolves an unlocked tree) and serves via nginx with an `/api` proxy — one origin, no CORS, `client_max_body_size 20m` matching A11's cap so an oversized upload gets the app's JSON 413 rather than nginx's HTML one, and `index.html` explicitly `no-store` (a cached copy pins users to deleted bundles). Plus `docker-compose.yml` (named volumes so a rebuild can't wipe saved designs), `.dockerignore` (excludes `.env`, tests, benchmarks), and [`docs/DEPLOY.md`](./docs/DEPLOY.md). **`/health` vs `/health/ready`.** `/health` stays pure liveness — no I/O, never raises — and that IS the X1 detector: a wedged loop stops answering. Dependency health is a different question, so `/health/ready` **writes and deletes a probe file in the designs dir**: the failure it exists to catch is a container whose volume didn't mount, which looks perfectly healthy to liveness and silently loses every save. Returns **503, not an exception**, so an orchestrator drains rather than restart-loops. `/health` also now reports environment, release and whether error reporting is live — the first two questions asked when a deployment misbehaves. **Sentry, both apps, off by default.** Never fatal (missing SDK or bad DSN → warning, boots anyway) and scrubbed: the backend drops request bodies, query strings and secret-shaped headers; the frontend strips fetch/XHR bodies from breadcrumbs — a stitch stream is the customer's IP and must not reach a third party. **Measured bundle cost: ZERO without a DSN** (Vite statically eliminates the dynamic import), a separate **163KB-gzip lazy chunk** with one. 14 backend regressions (+1 skipped) and 5 vitest: liveness identity, a structural guard that `/health` stays I/O-free, readiness green/503 both ways — with the read-only-volume case **skipped under root, which bypasses permission checks and would have passed for the wrong reason** (a root-proof file-collision case always runs), SDK-missing and bad-DSN survival, and header/body scrubbing. **Verification honesty: the images are AUTHORED BUT UNBUILT — this container has no Docker daemon, so `docker compose up` has not been executed and must not be reported as working.** What was verified: the compose file parses, every build context, Dockerfile and `COPY` source resolves to a real path (14/14), the frontend build itself runs clean, and both Sentry paths were exercised for real. What is unverified until someone runs it: image layers actually building (apt/pip resolution, the pycairo compile, the U2-Net warm-up), and the nginx↔backend service wiring. Suite **1,045 passed, 1 skipped, 4 xfailed** (831.87s; +13 health/observability, 1 root-skip), vitest **184** (+5), tsc clean, frontend build clean, ruff clean on touched files. |
| 123 | 2026-08-08 | Claude | ⚡ Perf | **CTO B1 (1/3) — event-loop unblocking, and X1's stated fix turns out to be wrong.** **Measured first.** X1 claims the CPU endpoints "run synchronously inside `async def` handlers" and that the first fix is a one-word `async def` → `def`. Against this tree that premise is **stale**: digitize, lettering, convert, export, package, worksheet, optimize and the stitch/image editors are ALL already `def`, so FastAPI already threadpools them. **The freeze is real anyway, and the mechanism is the GIL** — a worker thread building tens of thousands of pydantic `Stitch` models holds the interpreter lock and the loop thread cannot run. Measured: **one digitize = 5,747ms request with a 1,198ms worst loop stall, already in the threadpool.** So the queue with separate worker PROCESSES (B1 2/3) is the actual fix, not a later nicety — recorded here because the backlog's one-word remedy would have been reported as done while the app still froze. **What WAS on the loop and is now off it:** every `local_store` call in the designs router (synchronous file read + full-design JSON parse — a plain GET stalled the loop **50ms**) and `render_preview` (**174ms**), the endpoint B8's library grid fires once per tile. All eight now go through `run_in_threadpool`; preview stall **174 → 39ms**, GET **50 → 19ms** (residual is GIL contention, which only processes remove). **Instrument note:** per-request latency CANNOT see a blocked loop — the loop simply never schedules the poller, so the stall falls between samples. The probe measures scheduling delay (tick every 5ms, record how late it fires), and the tests assert a RELATIVE ratio (stall ÷ request duration) because absolute ms is a property of the machine. 4 regressions: two timing guards (CI-skipped — shared runners add their own jitter), one **structural** guard that fails if any CPU-heavy handler becomes `async def` without `run_in_threadpool`, and a **negative control** proving the instrument can still detect a real 300ms block — a green timing test whose instrument is broken proves nothing. Suite **1,032 passed, 4 xfailed** (858.21s; +4 loop tests, +4 migration-sync tests from v121), vitest 179, tsc clean, ruff clean on touched files. |
| 122 | 2026-08-08 | Claude | 🔀 Merge | **Ops 2 + 3 — `main` exists and is the reconciled line; the backlog of record is stamped.** **Ops 2:** `main` did not exist and the GitHub default was the stale `feat/studio-dashboard`. Checked before creating anything: `feat/studio-dashboard` is a **strict ancestor** of this branch (0 commits would be lost), so `main` was pushed at the reconciled head — no merge commit needed, the trees are identical. CI covers `main` already (`branches: [main, 'claude/**', 'feat/**']`). **The default-branch switch could NOT be made from this session: the agent proxy refuses repository-settings writes** (`PATCH /repos/… → 403 "Repository settings writes are not permitted through this proxy"`) — a policy block, not a missing tool. One manual step remains: **GitHub → Settings → General → Default branch → `main`** (or `gh repo edit KevinD003/EmDesign_Automater --default-branch main`). Everything else about `main` is done. **Ops 3:** [`docs/CTO-REVIEW-2026-08-07.md`](./docs/CTO-REVIEW-2026-08-07.md) §8 now opens with a COMPLETE-AND-VERIFIED banner: one row per item A1–A15 with its outcome, commit and STATUS row; the probe table with before→after numbers; and an explicit carried-forward list (X1→B1, P6→B1.5, P3→B4, live RLS→v121) so nothing reads as silently absorbed. Docs/branch-only: no suite run required. |
| 121 | 2026-08-08 | Claude | 🔒 Security | **Ops 1 — identity RLS packaged for the LIVE project, with an anon-key probe. NOT YET APPLIED: blocked on credentials.** A13 closed S3 in `db/schema.sql`, but schema.sql is a create-from-scratch script — a live database is not updated by running it. New [`db/migrations/001_identity_rls.sql`](./db/migrations/001_identity_rls.sql): the RLS delta alone, transactional, re-runnable (`create policy` has no `IF NOT EXISTS` in Postgres, so every policy is preceded by a `drop policy if exists` — without that a re-run fails halfway and leaves RLS half-applied), safe to paste into Supabase Studio. New [`apps/backend/scripts/verify_rls.py`](./apps/backend/scripts/verify_rls.py): the verification the CTO asked for — a PUBLIC-ANON-KEY probe over raw PostgREST (the same surface an attacker uses) that reads and writes all three tables and exits non-zero if any is exposed. Its read/write criteria differ on purpose: Supabase grants `anon` table-level SELECT, so an RLS-protected table answers **200 with an empty array**, not an error — empty is the pass, any row is the fail; a write must be refused outright, and a write that SUCCEEDS is reported loudly with the row to delete. **Status: authored and unit-tested, NOT run against production — this container has no Supabase credentials (`.env` is gitignored and absent; all three settings empty), and a production verification will not be fabricated.** To close: `psql "$SUPABASE_DB_URL" -f db/migrations/001_identity_rls.sql` then `SUPABASE_URL=… SUPABASE_ANON_KEY=… python apps/backend/scripts/verify_rls.py` — exit 0 is the evidence, and this row gets amended with it. 4 regressions keep the migration and schema.sql from drifting: same three tables, identical policy names, every create preceded by a drop, and the `team_members`-must-not-reference-`teams` recursion guard restated. |
| 120 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A15 — Appendix A N1–N6, all closed (N3 was already shipped).** **N1** (reorder corrupts color bindings): `reorderColorStop` swapped and renumbered stops but never remapped `objects[*].colorStop`, so after ▲/▼ every object pointed at the OTHER color's stop — looked fine until the next rebuild sewed each region in the other region's thread, and Save persisted it. Objects now remap across the swap; regression pins the swap AND the round trip. **N2** (transparent PNGs lose black artwork): `IMREAD_COLOR` strips alpha → transparent decodes as black → the corner heuristic deletes the artwork's own black linework. Now `IMREAD_UNCHANGED` + alpha composited over white; the review's exact reproduction (black ring + red square on transparency) digitizes both. **N3** (small-image upscale): shipped in Part 17 (`_MIN_WORK_PX=1200`) — disposition, not code. **N4** (non-Latin names 500 every download): worksheet-pdf put the RAW name in Content-Disposition (Starlette is Latin-1 → UnicodeEncodeError), and export's `safe_stem` uses UNICODE `\\w` so ロゴ passed through untouched — the 500 was live on all three endpoints. New RFC 5987 `content_disposition()` helper: forced-ASCII fallback + `filename*=UTF-8''…`; 13 regressions sweep ロゴ/emoji/umlauts across all three endpoints. **N5** (tokens never refreshed, 401s unhandled): new backend `POST /api/auth/refresh` (GoTrue refresh grant), client retries a 401 exactly once through an injectable handler, authStore refreshes-or-clears (local-profile sessions clear immediately — no grant exists); 3 backend + 3 frontend regressions. **N6** (response races): one global `busyOp` in the design store replaces three private flags (Apply/Optimize/Open/Load/stitch-edit can no longer interleave) + an `epoch` stale-response guard — a rebuild landing after the user opened another design is dropped, not applied; store regressions pin claim/release and bump semantics. **Gate discoveries, both fixed:** the export-header sweep caught `content_disposition` re-splitting at dots INSIDE the stem via `safe_stem`'s own rsplit (a name carrying `..` lost everything after it — 'bad name!/..\"x' → 'bad_name'); the stem is now sanitized directly. And N2's alpha block pushed pipeline.py over the 1,500-line gate a fourth time — extracted to `geometry._decode_image_bgr`. Suite **1,024 passed, 4 xfailed** (1220.26s; +17 A15 regressions), vitest 179 (+6), tsc clean, ruff clean on touched files. |
| 119 | 2026-08-08 | Claude | 📝 Docs | **CTO Phase A14 — doc-sync pass: the claims match the machine again.** The review's §2 drift table, closed item by item: README claimed Python 3.14 (twice) — corrected to **3.11**, the version CI pins and every stream lock / visual baseline was captured on, with the drift hazard stated; README's test counts (83 pytest / 57 vitest) → current reality (1,000+ / 170+, pointing at this changelog for the live number); README called CI \"written but unverified — no remote yet\" → **green on GitHub** with what it runs; STATUS header carried a stale Mac path (`/Users/INDIA/...`), a `(part10)` latest-commit ref and a 105-file count → GitHub repo/branch, changelog-is-the-ledger, ~1,300 tracked files (the header sync itself rode in A13's commit — noted there and here). \"Commit this review\" was done at reconciliation (376f1a4, v106). Docs-only: no suite run required, per this ledger's own precedent. |
| 118 | 2026-08-08 | Claude | 🔒 Security | **CTO Phase A13 — RLS closes the identity tables; the PUT half was already shipped.** Disposition first: `PUT /designs/{id}` with version snapshots (S4) EXISTS — `update_design` bumps `version` and writes full-design `design_versions` snapshots — shipped after the review's 99-commits-stale audit base, so only S3 remained open. **S3:** `users`, `teams`, `team_members` had NO row level security — readable AND writable via the public anon key (subscription tier, usage counters, team rosters exposed). Policies added recursion-safe: `team_members` policies never reference `teams` (a `teams` policy may subquery `team_members`; if `team_members` subqueried back, Postgres RLS evaluation recurses infinitely — the classic Supabase membership pitfall), membership WRITES are service-key-only by granting clients no insert/update policy at all. Users: own-profile only; teams: owner manages, members see. 3 regressions as a schema LINT: every `create table` in db/schema.sql must carry an RLS enable (a new table can't ship open), the four S3 policies must exist by name, and `team_members` policies must structurally never reference `teams`. Suite **1,007 passed, 4 xfailed** (1217.39s; +3 schema-lint tests), vitest untouched by this item, ruff clean. |
| 117 | 2026-08-08 | Claude | ✨ Feature | **CTO Phase A12 — four OFL font families ship in the repo; lettering no longer depends on the server's OS.** C10: unsupported glyphs digitized as tofu boxes on Linux, output depended on server-installed fonts, and the hardcoded Arial/Helvetica paths could not legally be bundled. `apps/backend/fonts/` now carries **Open Sans** (clean sans), **Montserrat SemiBold** (small-text sans — the google/fonts variable file's DEFAULT instance is Thin, undigitizable, so a static wght=600 was instanced with fonttools `updateFontNames=True`; Montserrat's OFL declares no Reserved Font Name, so the modified static is compliant), **Playfair Display** (serif) and **Dancing Script** (script) — the faces an embroidery shop actually uses — each with its OFL license text alongside (~1.3MB total). `_font_files()` already preferred a repo `fonts/` dir that never existed; it now resolves the same faces FIRST on every deployment. 9 regressions: ≥3 OFL families present, license-alongside enforced for every family (keeps future additions honest), repo fonts outrank system fonts, `list_fonts` reports all four, every face loads and draws non-degenerate glyphs, and lettering renders end-to-end with the bundled SemiBold. Suite **1,004 passed, 4 xfailed** (1166.34s; +9 A12 regressions — the suite crossed 1,000), vitest 173, tsc clean, ruff clean on touched files. |
| 116 | 2026-08-08 | Claude | 🔒 Security | **CTO Phase A11 — every compute endpoint authenticated; body cap to the backlog's range.** S2: `/digitize`, `/lettering`, `/convert`, `/export`, `/optimize/*` and the rest of the compute surface had no `current_user` dependency — free CPU for anyone and a DoS surface. All nine compute routers (digitize, lettering, convert, export, optimize, stitch_edit, image_edit, worksheet, files) now carry `dependencies=[Depends(current_user)]` at router level; designs/threads already had auth, admin has its own. The keyless fresh-install fallback (no profiles → LOCAL_USER) is untouched, so the offline single-operator mode and the test suite still work; once ANY profile exists, an unauthenticated compute call is 401 BEFORE body parsing. Rate limiting and body caps already existed (Part 19 middleware); the global cap moves 25MB → **20MB** (backlog verbatim \"10–20MB\"). **Frontend hole found and closed in the same change:** the three blob-returning calls (`exportDesign`, `exportPackage`, `worksheetPdf`) bypassed the `request()` helper and sent NO auth header — logged-in users would have started getting 401 on export; they now attach `authHeaders()`. One Part 35 assertion updated with cause: an anonymous caller is now refused at the auth gate (401), not billed as free tier (402) — anonymous-as-free WAS the S2 hole. **Second discovery from the first suite attempt:** `/api/formats` — the static capability listing the frontend queries BEFORE login to build its format dropdowns — was swept into the authenticated export router and broke collection (the dev checkout has profiles, so the module-level fetch got a 401). It now lives on a deliberately PUBLIC router (like `/health`): static metadata, no compute, no user data. **Third discovery:** the corpus-digitize module's MODULE-scoped fixture runs BEFORE the function-scoped autouse store isolation (higher scope wins), so it authenticated against the operator's REAL `data/local_users.json` — invisible pre-A11, an 8×401 wall after; the fixture now isolates the store itself per conftest's own escape-hatch contract (only such fixture in the suite — swept). 18 new regressions: per-endpoint 401-once-profiles-exist and authenticated-caller-admitted for all nine routers. Suite **995 passed, 4 xfailed** (1177.75s; +18 A11 regressions), vitest 173, tsc clean, ruff clean on touched files (one pre-existing B008 FastAPI idiom). |
| 115 | 2026-08-08 | Claude | 🔒 Security | **CTO Phase A10 — `APP_ENV=production` fail-fast: a crashed deploy beats a fail-open one.** S1 (deploy blocker): with any `SUPABASE_*` var missing or typo'd, auth silently fell back to the `local-dev` sentinel and per-process in-memory dicts — production would run fully unauthenticated, mix every user into one account, and lose all designs on restart, with no startup validation. New `app_env` setting + `_validate_production_config()` at module import (uvicorn refuses to boot): production requires all three Supabase keys non-blank (whitespace-only counts as missing — a quoted-empty `.env` value can't sneak past) and rejects `STITCHIQ_OPEN_ACCESS=1` (dev-only switch). Development semantics unchanged — the keyless test suite and offline single-operator mode still work. 7 regressions drive the validator directly with patched settings: dev boots keyless, production boots fully configured, each missing key refuses by name, open-access refuses, whitespace refuses. Suite **977 passed, 4 xfailed** (1128.36s; +7 A10 regressions), vitest 173, tsc clean, ruff clean on touched files. |
| 114 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A9 — quality scorer rewritten around real rejection criteria; the letter grade is now earned.** C11: the scorer checked only long/tiny stitches, jump count and color count, and certified broken output — the review's 8mm lettering with jump-crossed counters and no ties scored **98/A**. Five new score-affecting detectors, each the criterion a commercial digitizer is actually rejected on: **unlocked thread ends** (every cut end in a ≥12-stitch block must carry a tie — error, up to −35), **thread-attached travel over open fabric** (STITCH or untrimmed-JUMP segments sampled at 1mm against the object contours+holes with a 1.5mm margin — the whisker/counter-crossing defect; a TRIMMED jump across the void is detached thread and correctly NOT flagged — error, up to −35), **satin crossings wider than any real column** (>7mm zigzag throws — error, up to −25), **uniform fill angles** (≥3 tatami fills sharing one angle — warn, −5), **trims per 1,000 > 50** (warn, −10). Every detector self-gates on assessability: toy streams have no room for locks, imported machine files carry no contours — skip, never guess. Measured: 8mm 'Peak' lettering now scores **80/B** (155 tiny-stitch findings; its locks and routing genuinely pass since A2/A3 — the review's 98/A case was partly FIXED upstream, and the score now reflects what remains). All 11 pre-existing scorer tests pass UNCHANGED (the gates did their job); +6 regressions pin each detector on constructed bad input AND its clean twin. Suite **970 passed, 4 xfailed** (1081.85s; +6 A9 regressions), vitest 173, tsc clean, ruff clean on touched files. |
| 113 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A8 — rebuild validation: orphan `color_stop` raises loudly; raster lifted to DST resolution.** C9: an object referencing a nonexistent color stop was silently filtered out of the rebuild emission loop — the object simply VANISHED from the output. Rebuild now pre-validates and raises `ValueError` naming every orphan and the known stops, so the caller fixes the data instead of losing stitches. C6: rebuild re-rastered at ≤4px/mm (0.25mm grid, ≤800px canvas), roughening every edge on each edit with damage accumulating across repeated edits; now **10px/mm (0.1mm grid — DST's own coordinate resolution)** with a 2000px canvas cap (full resolution to 200mm, beyond every supported hoop). Regressions: orphan raise with the object named; rebuilt coordinates must land off the old 0.25mm grid. **Probe P6 stays strict-xfail with fresh measurements — honestly, not silently:** at the new raster the gap is digitize 17,078 vs rebuild 4,989 (70.8%) — STRUCTURAL, not resolution: rebuild's generator family omits digitize's fill border satin and full underlay recipes, and the probe's verbatim criterion (\"no coordinate moves >0.1mm\") additionally requires pass-through of unedited objects. Both flagged as discovered work for the user to sequence, per the do-not-decide-alone rule. **Layering kept honest (again):** the growing rebuild pushed pipeline past the 1,500-line gate a third time; two cohesive blocks moved down-stack — the appliqué phase recipe to `underlay.py` as `_applique_phases` (underlay may import satin per the pinned layer order; satin may not import underlay, which is why it lives there) and the rebuild finishing chain to `routing.py` as `_finish_rebuild_segment` (pipeline ~1,496). Rebuild-only change: no lock/baseline re-pin. Suite **964 passed, 4 xfailed** (1074.23s; +2 A8 regressions), vitest 173, tsc clean, ruff baseline unchanged. |
| 112 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A7 — run pitch 2.5mm default, density-aware; the manual-run 6mm bug is closed.** C7 verbatim: rebuild's RUNNING/MANUAL branch passed `max_step_px` — the 6mm MACHINE CAP — as the resample PITCH, so every manual or running object sewed 6mm floats (snag-prone, visibly coarse), and `density` on run objects was ignored entirely. New `RUN_PITCH_MM = 2.5` (pro default); pitch = `1/density` clamped to [MIN_STITCH_MM, MAX_STITCH_MM] when density is meaningful, default when density=0 (which is what the manual tool stores — checked, not assumed; dark-linework runs already sew at 1/density=1.4mm by construction and are untouched). Rebuild-only change: stream locks and visual baselines are digitize-derived and needed no re-pin. Two regressions: default pitch ≈2.5mm on a density-0 run; density 1.0 vs 0.25 give ~1mm vs ~4mm pitch (the ignored-field bug can't return silently). Suite **962 passed, 4 xfailed** (1134.03s; +2 A7 regressions), vitest 173, tsc clean, ruff baseline unchanged. |
| 111 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A6 — fill quality verbatim: 0.4mm rows, 4mm fill-stitch cap with ceil() repair, alternated isotropic angles.** **(1) Spacing:** `ROW_SPACING_MM`/`FABRIC_DEFAULT`/cotton `row_mm` 0.45 → **0.40** (backlog verbatim; industry 40wt default ~0.35–0.42mm; 0.45 was the measured-good pre-A6 value — overridden per the reconciliation decision, other fabrics keep their tuned pitches). **(2) Fill stitch cap:** fills previously subdivided on the general `MAX_STITCH_MM` grid and the stagger end-guard left end segments unbounded by it — measured **15.9mm worst case** at a 12.7mm step, over the machine limit itself (the review's C8 measured 7.9mm on the old tree). New `MAX_FILL_STITCH_MM = 4.0` grid for tatami rows (digitize both call sites + rebuild scanlines; contour fill already capped at 3mm; underlay mesh stays at 6mm deliberately — scaffolding, not visible fill), and `_scanline_fill` now ceil()-subdivides any over-step gap in `[end, grid…, end]` into equal parts — interior points keep the stagger diagonal, only end repairs are unstaggered. Regression pins max within-row segment ≤ step at 0°/30°/77°. **(3) Alternation:** per-region long-axis angles already existed (Part 24); now successive ISOTROPIC fills rotate their fallback angle 90° so push/pull spreads instead of accumulating — free by proof: the edge-avoiding penalty |cos(2(θ−e))| has period 90°, so the rotated angle dodges the same edges equally well (`_fill_angle_provenance` exposes axis-vs-fallback; regression: two discs digitize ⊥ to each other). Stagger (25% phase, `FILL_STAGGER_ROWS=4`) already satisfied the offset-pattern item. **Layering kept honest:** pipeline hit the 1,500-line gate again; the digitize satin width-band underlay recipe moved to `underlay.py` as `_satin_width_underlay` (pipeline ~1,492). **First-suite discovery — a real defect, not a test artifact:** the pitch change made a contour-fill row in fixture 07 trace a thin tongue out-and-back, and the needle-into-needle turnaround (0.146mm same-side pair) could NOT be repaired: `_drop_floor_reversals` merges two row steps into one stitch, and at `CONTOUR_ROW_MAX_STEP_MM = 3.0` that merge is 6.0+mm — just over `MAX_STITCH_MM`, so the repair declined by policy. New invariant: contour row step STRICTLY under `MAX_STITCH_MM/2` (3.0 → **2.9**) so a retrace tip is always repairable; fixture 07 heals, all 33 quality-metric tests green. Also: `test_more_pull_widens_coverage_on_rebuild` gained a 1% count tolerance — pull comp guarantees coverage, not a monotone stitch count, and the pitch re-quantization shed 3 stitches (2455 vs 2458) while the extent still widened as required. Locks (4) + baselines (10) re-pinned twice (after the pitch change, again after the contour step change); renders eyeballed (01/07: full coverage at the denser pitch, no travel over bare fabric). Suite **960 passed, 4 xfailed** (1181.28s; +3 A6 regressions), vitest 173, tsc clean, ruff baseline unchanged. |
| 110 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A5 — satin `stitch_angle` honored on rebuild; the underlay menu stops lying.** Probe P4 baseline: rebuild recomputed `minAreaRect` and produced byte-identical streams at 0° vs 77° (C4). Root cause was a data-semantics bug, not a missing branch: digitize stored RAW `rect[2]`, which is off by 90° whenever the generators' long-axis flip fired — so the stored angle could never be trusted and rebuild ignored it. Now digitize records the NORMALIZED walk axis (`_satin_axis_deg`: rect angle + flip, mod 180) and rebuild builds the satin frame from the stored angle verbatim (both rect consumers use only center + the flip test, so pre-normalized extents make the angle authoritative). Unedited rebuilds reproduce the old frame to 0.1°. Semantics pinned by regression: axis 0° → ~90° rungs, axis 90° → ~0° rungs, and digitized satin objects' stored angle must equal their contour's normalized rect axis. **C5 (fake underlay menu):** rebuild used to collapse ANY non-NONE choice to center-walk (satin) / edge-walk (fills); now the SELECTED type dispatches to its real generator — satin: CENTER_WALK / DOUBLE_ZIGZAG (zigzag lattice along the column midline, digitize's own recipe) / EDGE_WALK; fills: EDGE_WALK / PARALLEL (edge run + crossing tatami layer, previously unreachable from an edit) — with legacy out-of-family saves keeping the old collapse instead of erroring. Three duplicated per-branch underlay blocks consolidated into one dispatch. Frontend: satin Angle field re-enabled (was disabled with \"columns follow the shape axis\"), underlay dropdown offers per-family REAL options only via new pure helper `lib/underlay.ts` (runs/manual/appliqué: control disabled — no underlay exists for them; CONTOUR never offered — no generator), legacy stored values appended so the select can't render blank. **Migration shim:** pre-A5 saves recorded RAW `rect[2]` — NEGATIVE in this cv2 — so a stored negative angle that exactly equals the contour's raw rect angle is provenance, not an edit, and rebuilds in the normalized frame it always meant (regression: legacy save rebuilds byte-identical to the normalized twin; a deliberate negative edit that doesn't coincide with raw folds via mod 180). **First-suite discoveries, all fixed:** the facade layering test caught `_satin_axis_deg` unexported; the 1,500-line monolith gate caught pipeline.py at 1,516 — the underlay dispatch moved to `underlay.py` as `_rebuild_underlay(...)` (pipeline back to ~1,480); and the Part-64 benchmark's vertical satin bar carried `stitch_angle=0.0` — meaningless under no-op semantics, wrong under honored semantics (0° walks ACROSS the bar → 25mm rungs → classifies as fill) — fixture corrected to its true axis 90°, which is exactly the hazard class old saves face and the shim exists for. Probe P4 flipped from strict-xfail to green; +4 backend regressions, +8 vitest. Suite **957 passed, 4 xfailed** (1158.23s; was 952/5 — P4 xfail→pass, +4 regressions; stream locks and visual baselines untouched, the digitize change is metadata-only), vitest 173, tsc clean, ruff baseline unchanged. |
| 109 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A4 — appliqué STOPs: the machine now pauses after placement AND after tackdown.** Baseline (probe P5): rebuild sewed the appliqué sequence — placement run, tackdown run, satin cover — as one uninterrupted stream, so there was never a moment to lay or trim the appliqué fabric; the feature was decorative, not manufacturable. Rebuild now keeps the three phases separate through the finishing transforms (coalesce → travel-route → floor-repair each phase independently) and emits a `STOP` at the previous needle position between consecutive surviving phases. Scope note: `digitize_image` cannot produce APPLIQUE objects (auto-digitizing classifies regions as fill/satin/run only), so the rebuild path IS the complete fix — the review's \"digitize_image + rebuild_design\" wording assumed a path that does not exist. `STOP` was verified through the persistence layer: `_STR_TO_CMD` maps it for pyembroidery and a PES round-trip regression test asserts the count survives export/import; a second regression asserts the STOPs land BETWEEN sewn phases at the held needle position (a STOP at the stream edge or mid-jump would pause the machine somewhere useless). Probe P5 flipped from strict-xfail to green. Non-appliqué rebuild is byte-identical (same transforms, same order). Also this session: CI run 4 died in setup — `opencv-python-headless==5.0.0` is not a real pip version (the distribution is 4-part: `5.0.0.93`, though `cv2.__version__` reports `5.0.0`); pin corrected. Suite **952 passed, 5 xfailed** (1143.82s; was 949/6 — probe P5 xfail→pass, +2 regressions), vitest 165, tsc clean, ruff clean on touched lines. **CI milestone this session: run 5 was the first GREEN pytest suite on GitHub** (946 passed, 3 skipped, 6 xfailed — the 3 skips are the documented dev-hardware timing/preflight guards); its one failing step was `verify_lint_claim.py` choking on parts 12–17 audits whose LINT-VERIFY markers reference the pre-consolidation `app/services/digitizer.py` path — stale point-in-time claims now skip loudly instead of failing forever, per the checker's own "history is not retroactively gated" principle. |
| 108 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A3 — the ring probe reads ZERO: no stitch or jump crosses open fabric.** Baseline: 92 trimmed jumps straight across the letter-'o' counter (already thread-cut — C2's whisker claim was fixed by Part 25's lock pass — but a 95-trim explosion on a plain ring). Two routing defects found by instrumenting every rejected jump: **(1)** the boundary-detour's resampled travel chords cut across CONCAVE (hole) boundaries and the all-inside re-check rejected the whole detour — fixed by halving the travel pitch on failing chords (`resample_inside`); **(2)** 77 of 109 connections had endpoints the router considered "outside" — border satin overhangs the region by half its width and pull comp dilates the top, so border-phase connections were never routable at all — fixed with an endpoint pad (`pad_px`). **The first pad version dilated the whole test mask and travel promptly bridged the open gaps between letters (stitched runs across bare fabric on the badge's HARBOR CLUB — caught by eye on the render, not by the probe); shipped version pads ENDPOINTS only, path interiors must stay inside the tight region, and entry/exit hops are length-capped.** Ring: 92 crossings/95 trims → **0 crossings/1 trim** (travel routed inside the body). Probe P1 flipped from xfail to green; the lock probe moved to a same-colour-separation fixture (the ring now sews trimless, which broke the probe's own precondition). All 10 visual baselines re-pinned after eyeballing the failing fixtures (badge arc text and wordmarks render cleaner than before); 4 stream locks re-pinned. Suite **949 passed, 6 xfailed** (1084.16s), vitest 165, tsc clean, ruff 12. |
| 107 | 2026-08-08 | Claude | 🐛 Fix | **CTO Phase A2 — lock stitches complete: the stream's first object start now gets a tie-in.** Disposition first: the §8 probes were measured against the current tree and committed as `tests/test_cto_probes.py` (1 pass + 5 strict xfails carrying the measured baselines: ring 92 crossings→A3, satin angle no-op→A5, appliqué no STOP→A4, lettering 102 st/letter satin→B4, rebuild fidelity 4,820 vs 2,298→A8). C1's substance was already shipped in Part 25 (`_lock_stream`: tie-off before every TRIM/COLOR_CHANGE/END, tie-in after every cut — measured 95/95 trims locked on the ring probe), but `pending_tie_in` initialized False, leaving the FIRST thread end of every design unsecured: 95/96 starts tied. One-line fix; **lock probe now 96/96 on the DST round trip**. The 4 stream-lock hashes re-pinned per their documented intended-change procedure (every design gains one 3-penetration tie at the start). Suite **948 passed, 7 xfailed** (1104.81s), vitest 165, tsc clean, ruff 12, visual baselines 10/10 (SSIM gate absorbed the tie). |
| 106 | 2026-08-08 | Claude | 🔀 Merge | **CTO competitive review merged in as backlog of record: [`docs/CTO-REVIEW-2026-08-07.md`](./docs/CTO-REVIEW-2026-08-07.md).** The review branch (`claude/code-review-competitive-o2qw0o`) was audited against the v32-era tree — 99 commits behind this branch — so its STATUS ledger (v33) is superseded by this one; the review document and the `requirements-dev.txt` pyembroidery fix (fresh-clone pytest/CI were broken without it) are taken whole. Per the user's reconciliation decisions: execution happens ON THIS BRANCH, each Phase-A item's acceptance probe is run against the current tree, and **where the backlog names a spec that conflicts with a measured constant here, the backlog wins verbatim** — with stream locks and visual baselines re-pinned and the overridden measurement recorded in the item's row. A per-item disposition (already-shipped / still-open / spec-conflict) precedes execution. |
| 105 | 2026-08-07 | Claude | 🗑️ Cleanup | **Documentation pruned: 17 stale MD files deleted (~148KB), reference-checked first.** All `docs/SESSION-REVIEW-*.md` (dated 2026-07-28/29) removed: point-in-time review notes from the parts 1–12 era, fully superseded by this changelog's rows and the per-part audits; the only remaining reference anywhere was a single historical citation in the part 2.5 audit (left as-is — git history retains the files). **Everything else was checked and kept for cause**: the `v2-part*-audit.md` ledger is linked from every row of this table and is the standing per-part deliverable; `COMPETITOR-COMPARISON` / `COMPETITIVE-BLOCKERS` / `COMPETITIVE-GAP-ANALYSIS` / `LAUNCH-READINESS-GAPS` are cited by application code and tests as the rationale for engineering decisions (`constants.py`, `optimizer.py`, `analytics.ts`, two test files); the two `PROMPT-*` commissioning briefs are referenced by the R-list and audits for the still-open graph-engine/direction work; `LAUNCH.md`, `FABRIC_TEST_PROTOCOL`, `ENGINEERING_STANDARDS`, `FAQ`, `USER-GUIDE`, `ARCHITECTURE`, `DATA-MODEL`, `TRAINING-DATA-SPEC`, `REFERENCE-CAPTURE-PROTOCOL`, both `EVALUATION-*` docs and `STATE-SNAPSHOT` are live process/product docs. Docs-only change: no code touched, no suite run required. |
| 104 | 2026-08-07 | Claude | ♻️ Refactor | **v2 Part 66 — consolidation: one copy of everything, nothing weakened: [`docs/benchmarks/v2-part66-audit.md`](./docs/benchmarks/v2-part66-audit.md).** Production-readiness dedupe, measured before editing: **8 copies** of the enum `.value if hasattr` dance → one `enum_str` in `app/models/design.py` (the fact that made them dead code — `use_enum_values=True` means enum fields are ALWAYS plain strings — was verified against construction and JSON round-trip, not assumed); **20 test-file sys.path shims** → owned once by `tests/conftest.py`; **5 stream comparators** → `tests/helpers.py::stream_of`; the **doubled-angle row instrument's 4 copies** → 2 deliberate homes (tests/helpers for tests, bench_competitor for scripts — tests do not import the code they measure); **3 label-bar + 2 each of largest-tatami/crop/overlay** → `scripts/_viz.py`; and 12 files' repeated seeded digitizes → one lru-cached `digitized_fixture` returning deep copies, cutting the four newest test files from **125 s to 66 s**. Net **97 insertions / 262 deletions**. **Proof nothing moved**: 184 targeted lock/baseline/IO tests green first; regenerated evidence pixel-compared — renders identical, differences confined to caption bars (one label style now) and a ≤470px overlay strip where the OLD code drew the control line unclamped on edge-cropped panels (the shared helper is the correct one). **Deliberately not merged, with reasons recorded**: per-part test files (the executable spec — merging loses documentation, parallelism and bisection), the 25 measurement scripts (documented reproduce paths), bench analysis code into `app/` (no consumer), `split_digitizer.py` (Part 42 provenance decision). Suite **947 passed, 2 xfailed** (1175.02s, wall-clock inflated by a mid-part container restart), ruff 12, locks 4/4, baselines 10/10, frontend untouched. |
| 103 | 2026-08-07 | Claude | 🐛 Fix | **v2 Part 65 — photographic rescue: the fish test, fixed in the engine: [`docs/benchmarks/v2-part65-audit.md`](./docs/benchmarks/v2-part65-audit.md).** Part 64's angelfish verdict acted on: self-evaluated, diagnosed from the pipeline's own logs, fixed in `pipeline.py`. **Diagnosis chain**: 37/67 planned regions skipped as `sub_thread_feature` (yellow body webbed to ~0.16mm slivers by photographed thread rows) → Part 27's mean-shift never fired because the texture metric's edge-avoiding sample collapses on dense thread texture (fish 1.86 vs gate 6.0, and the gate can't be lowered — flat art reaches 4.10) → two obvious rescue gates measured and rejected (element-level lost_share reads the failed fish at **0.9%**, one webbed element "covered" by any fragment; owned-pixel share reads 15.9% because halo suppression leaves the dead pixels unowned) → the honest base is **segmentation foreground minus substrate**: fish 22.8% unsewn, worst fixture 14.8%, others ≤4.4%. **The fix, triple-locked**: when the plain path leaves ≥0.19 of that base unsewn AND the largest connected unsewn piece is ≥50mm², re-digitize once with the smoothing forced and keep it only on a ≥0.10 recovery (fish 0.228→0.102); flat art cannot pass the improvement gate even with the trigger gates forced open (pinned). **The chunk gate exists because the suite caught my first version breaking two contracts**: a correctly-empty design (uncovered 1.0) was "rescued" into 3,680 stitches of sewn noise (Part 47's empty→422 contract, 3 tests red) and a speck-noise fuzz image doubled digitize cost while the recursion clobbered the caller's drop log (Part 49's test red) — both shapes lack a 50mm² connected piece by construction, and a rejected retry now restores the logs it overwrote. Kept results carry a user warning recommending flat artwork over photos. **Result**: full striped body + fins + face + the competitor's decorative swirls recovered as 55 running paths (R008 partially closed as a side effect); before/after committed. Locked fixtures never reach the retry by measurement; 32 lock/baseline tests green pre-suite; recursion-path determinism pinned. **Cleanup half**: orphan scans ran clean (frontend imports, backend scripts); only deadwood found was two untracked local corpus JSONs (deleted); `split_digitizer.py` kept per Part 42's recorded provenance decision; script analysis code stays canonical in `bench_competitor.py`, deliberately not promoted into `app/` (no consumer). Benchmark + training rows regenerated (85→155 rows). 4 new tests; suite **943 → 947 passed, 2 xfailed** (811.05s), ruff 12, locks 4/4, baselines 10/10, frontend untouched. |
| 102 | 2026-08-07 | Claude | 🧪 Test | **v2 Part 64 — competitor benchmark and training-data pipeline: [`docs/benchmarks/v2-part64-audit.md`](./docs/benchmarks/v2-part64-audit.md).** Measurement/dataset infrastructure, no engine change. (The brief was titled "Part 63" but divided flow had already taken that number.) **Benchmark harness** (`scripts/bench_competitor.py`): 8 cases — 5 fixtures (full object+stream metrics), 2 foreign machine files (stream exact, types inferred from segment statistics, both trivial 26-stitch test patterns), and **1 matched-artwork competitor case**: the royal-present angelfish render digitized by STITCHIQ on the same image. The generated report refuses a scalar score: with no Wilcom/Hatch native files and no competitor machine files for matched artwork, **every numeric cross-comparison is marked "not measurable from the files available"** — the framing section is generated into the report itself. Block-type inference is **calibrated where truth is known** (STITCHIQ generators round-tripped through DST come back as run/satin/fill, pinned): satin ≈ every-stitch reversal; run ≈ thread length 1–3× bbox diagonal vs fill ~50× — the coverage ratio, after a first per-area-density version misclassified thin straight runs and a test caught it. **Angelfish findings (human-judged, labelled as such)**: body-region recovery lost on the photographic source (k=6/8 makes it *worse*: 5,957 → 2,993 stitches, still 27–30/30 SATIN), decorative run-work swirls dropped entirely (R008 territory on a real competitor piece), watermark → blobs; all-satin typing itself is "different, not clearly worse" (the competitor's body is satin too — the gap is 30-way fragmentation). Badge fixture corroborates: "HARBOR CLUB" digitizes illegible. **Top-3 gaps with levers**: photographic region recovery (algorithm), small-text fidelity (algorithm + product default: route glyphs to the existing lettering engine), object formation/fragmentation (model training eventually — exactly what the schema targets — editor merge/split as bridge). **Training data** (`docs/TRAINING-DATA-SPEC.md` + `scripts/extract_training_rows.py`): JSONL schema with four provenance levels (native_competitor_object / machine_file_inference / human_labeled / stitchiq_generated), provenance REQUIRED and stray labels rejected by the validator; **85 rows extracted** (72 level-4 with contours+crops, 13 level-2 with nulls-not-guesses). **Question-3 answer recorded: current data cannot support supervised training toward competitor-grade decisions** — level-4 teaches imitation, level-2 loses the properties worth learning; needs native competitor files or a human labelling pass. Tiny baseline deliberately skipped (circular: it would be scored by the system that generated its labels, which the fish shows is systematically wrong). 10 new tests; suite **933 → 943 passed, 2 xfailed** (777.87s), ruff app 12, frontend untouched. |
| 101 | 2026-08-07 | Claude | ✨ Feature | **v2 Part 63 — divided Stitch Flow: two regions, two directions, one divide line: [`docs/benchmarks/v2-part63-audit.md`](./docs/benchmarks/v2-part63-audit.md).** The brief's preferred minimal implementation, shipped whole: a `TATAMI` object can carry an optional **divide line** (`flowDivide`) splitting it into two half-plane flow regions plus a second direction line (`flowLineB`); at rebuild **each side sews at the angle of the line whose midpoint lies on that side** (first claim wins, on-divide midpoints claim nothing, unclaimed sides stay automatic). Half-planes by cross-product sign, and the two masks **partition** the region — no gap, no overlap, asserted — with per-side scanlines concatenated safely (each opens with a jump) and `_route_travel` handling the crossing. **Legacy safety byte-compared four ways**: no metadata, `flowLineB` without a divide (ignored), set-divide-then-remove, and a Part-62-shaped dump — all identical, on synthetic and real digitized designs. **Coverage preserved at 1.000** on the crescent demo both plain and divided (0.19 mm tolerance); its stitch count moving 1,901 → 1,078 is chord geometry (rows along the body vs across the band), not lost territory. **Visual gate on three curved shapes** (`part63-divided-{crescent,bent-leaf,bowl}.png`): one line fixes one half and streaks the other, the divide fixes both — and the bowl's divide is *horizontal* (arms 0°, base 90°), showing the divide is a real degree of freedom. **Frontend**: two-click `divide` tool, orange divide + teal lines all with draggable endpoints, per-side line routing (`flowLineSlot`), panel reads `divided: 41° / 139°` with Draw side line / Redraw divide / Remove lines / Remove divide; removing the divide clears the side line with it and falls back to Part 62's single-line model. Frontend helpers mirror the backend on the same fixtures so the sides can't drift. Live Playwright proof: divide crescent → line per side → Apply 200 → each side's rows follow its line. Honest limit stated: two regions cannot serve a full ring — that is multi-segment future work. 14 new backend + 10 new frontend tests; suite **919 → 933 passed, 2 xfailed** (819.97s), **frontend 155 → 165**, `tsc` clean, ruff 12, locks 4/4, baselines 10/10. |
| 100 | 2026-08-07 | Claude | ✨ Feature | **v2 Part 62 — Stitch Flow: a persisted direction line that fills actually obey: [`docs/benchmarks/v2-part62-audit.md`](./docs/benchmarks/v2-part62-audit.md).** First user-facing *editing* feature since Part 60, and the first new design-model field since the contour: a `TATAMI` object can carry a two-point **`flow_line`** (JSON `flowLine`, design mm). At rebuild the line's angle replaces the automatic one **for that object only**; the override **never overwrites `stitch_angle`**, which is what makes set+remove+rebuild **byte-identical** to never having set it, and every pre-Part-62 design (no field → `None`) rebuild **byte-identical** by construction — both pinned by tests, along with rows-follow-the-line (length-weighted doubled-angle mean of the object's row segments within 10° of the drawn line), neighbours-untouched, and the camel-name JSON round trip. **Frontend is a real editor, not a checkbox**: a `flow` tool captures exactly two clicks on the canvas (a direction needs two points — no Finish step), the committed line renders as a dashed teal overlay with **draggable endpoints**, and the Properties panel shows *automatic (45°)* / *line at 90°* with Draw/Redraw/Remove — every edit one undoable history step; the Angle input disables while a line is set. Angle semantics defined once and mirrored (`_flow_line_angle` backend, `flowAngleDeg` frontend: fold to [0,180), head/tail symmetric, degenerate → automatic), unit-tested to the same contract on both sides. **Live end-to-end proof** (`docs/benchmarks/v2-part62-ui/`, real backend + real app via Playwright): draw a vertical line on fixture 01's 45° disc → Apply returns 200 → **the rows turn vertical** — the before/after pair is rebuild-vs-rebuild because measurement showed the first Apply changes stitch counts *with or without a line* (4,820 → 2,298 lineless; pre-existing digitize-vs-rebuild gap, out of scope, worth its own brief). Rendered proof on two fixtures in `part62-flow-01/07.png` (45°→90°→135°; 82°→127°→172°). Scope held: tatami-only consumption, control only offered where rebuild consumes it — no dead UI on satin/curved fills; no R004, trim, or satin work. 7 new backend + 12 new frontend tests; suite **912 → 919 passed, 2 xfailed** (749.85s), **frontend 143 → 155**, `tsc --noEmit` clean (one environment fix: `@types/node` was only an optional peer of vite, so a fresh install failed typecheck at HEAD — now an explicit devDependency), ruff 12, locks 4/4, baselines 10/10. |
| 99 | 2026-08-07 | Claude | 🧪 Test | **v2 Part 61 — the aggressive trim profile on imported streams: safe, and twice vindicated: [`docs/benchmarks/v2-part61-audit.md`](./docs/benchmarks/v2-part61-audit.md).** Measurement/validation closing Part 59's stated residual; no default change, no UI change. **Population**: the repo's two true foreign files (`sample.dst`, `sample.pes`) plus two tier-A designs round-tripped through **all five machine formats** (dst/pes/jef/exp/vp3) — the honest way to get machine-encoded stream shapes at scale, since DST really splits long moves into jump chains at write time. **Trim-only diff asserted (not sampled) on all 12 streams; conservative asserted to be the identity on all 12.** Every hazard the brief named appeared in real data: jump chains up to 19 long behind 637/663 of A03.dst's trims; **1,435 consecutive double-trims** in one PES round-trip; trailing trims with no following stitch (every VP3 file); a jump-free encoding (VP3: 17 explicit jumps for the whole panel). **Two findings go beyond "no harm": (1) the carried-path rule is load-bearing on machine formats** — a naive first-hop rule would decide up to **39% of trims differently** (258/663 on A03.dst), so Part 59's design choice is now measured as necessary, not defensive; **(2) the profile actively repairs over-trimmed imports** — the PES encoding inflates the panel's 663 trims to 2,435, and aggressive takes it to **583**, within 4% of the native 559, dropping only cuts the encoding invented on moves the design originally carried. VP3's higher removal (186 vs DST's 102) is the encoding's own truth: without explicit travel, the carry is the machine's direct move. **Decision: safe for imported streams — advertise generally**, with one sentence of caveat for user copy: *on formats that do not encode travel explicitly (e.g. VP3), "under 10 mm" is measured over the machine's direct move*. 3 new invariant tests (trailing trim kept, double-trims decide together, DST round-trip trim-only + rule-exact); suite **909 → 912 passed, 2 xfailed** (816.92s), ruff 12, locks 4/4 and baselines 10/10 untouched. **This closes the last stated uncertainty on the trim-profile feature.** |
| 98 | 2026-08-07 | Claude | ✨ Feature | **v2 Part 60 — the Part 59 decisions, visible in the UI: [`docs/benchmarks/v2-part60-audit.md`](./docs/benchmarks/v2-part60-audit.md).** Frontend only; **no backend file changed**, no default moved. **Trim control**: this app has no export dialog — the toolbar's format select *is* the export UI — so the control is a sibling select in user language: *"Trims: safe (any machine)"* (default) / *"Trims: fewer (auto-trim machines)"*, tradeoff as tooltip helper text, one catalogue (`lib/exportOptions.ts`) feeding the control, the API client and the tests so labels and wire values cannot drift (a test asserts the wire values never appear in what the user reads). **Gate 1 pinned at the request level**: the default profile is *omitted from the URL*, so an untouched control sends a request **byte-identical** to before the control existed — captured from the live app by clicking the real Package button: `?format=dst&trim_profile=aggressive` when opted in, bare `?format=dst` at default. **Warning display**: `App.tsx` has rendered every `Design.warnings` entry verbatim since Part 25; that block is extracted unchanged into `DesignWarnings.tsx` so the present/absent contract is testable in the repo's node test environment via `react-dom/server` — no new dependencies, no second warning mechanism. Screenshots of the real app (design carrying the Part 59 colour-limit warning, loaded through the app's own Saved mechanism) in `docs/benchmarks/v2-part60-ui/`. **Honest note on gate 3**: the digitize dialog already clamps at `max={8}`, so a UI user cannot request the inert range — Part 58's decision expressed as UX; the warning reaches the screen on designs arriving via the API, saved masters or cloud, and the dialog was *not* widened to 12 just to make the warning reachable (that would add a do-nothing knob, the exact anti-pattern Parts 57–58 closed). 12 new frontend tests; **frontend 131 → 143 passed**, `tsc` at its pre-existing baseline with 0 new errors; backend locks 4/4 and visual baselines 10/10 untouched. Residual: tooltips are invisible on touch — if the export flow grows a real dialog, the helper text should become visible copy. |
| 97 | 2026-08-06 | Claude | ✨ Feature | **v2 Part 59 — two measured facts become product behaviour: [`docs/benchmarks/v2-part59-audit.md`](./docs/benchmarks/v2-part59-audit.md).** First engine change since Part 52, and it is one appended warning string. **(1) The colour cap now speaks.** `PLAN_MAX_COLORS = 8` replaces the bare literal at `pipeline.py:257` (facade-exported, Part 57's measurement in its comment), and a request above it appends via the *existing* `user_warnings` machinery: *"Colour limit: 12 colours were requested, but colour planning uses at most 8 — this design was planned with 8. Values above 8 do not change the result."* The message reports `k_plan`, not the bare cap, because the outline-check retry can legitimately plan more. Stitch output unchanged — **pinned by a test that asserts the k=12 and k=8 streams are identical**, turning Part 57's byte-identity finding into a standing regression test. **(2) Machine-aware trim profiles at export time**: `?trim_profile=` on `/api/export` and `/api/export/package`. `conservative` (default, unknown machine) returns **the same object** — the no-op is structural; `aggressive` (opt-in, auto-trim machines) drops every TRIM whose **carried thread** is under Part 48's measured 10 mm, named profiles not a numeric knob, unknown names 422. `TRIM_MIN_GAP_MM` itself untouched. Export-time rather than a generation parameter because the trim decision never influences geometry, so the threshold is a fact about the target machine, not the design. **Measured effect** (script *asserts* the stream differs only in TRIMs): A01 364→**302** (−17.0%), A02 796→**710** (−10.8%), A03 panel 663→**559** (−15.7%); jumps, stitches and travel byte-for-byte identical; ~**10.5 minutes of machine time** across the three, no quality claim made. **And the number that had to be explained before shipping: 559, not Part 48's 485.** Both correct, 74 trims apart, reconciled exactly: the engine's gate tests the **entry gap** (178 panel trims in [6,10) → 663−178=485, Part 48 reproduced), while the profile walks the **needle path** to the next real penetration — and 74 of those 178 carry ≥10 mm of actual thread through the travel jumps the next object opens with. The path is never shorter than the gap, so the filter only ever *keeps* trims the entry-gap rule would drop — conservative relative to the engine's own rule, and correct for imported DST jump chains. **My first docstring claimed the two rules equivalent; that was wrong** and both docstring and test now state the measured truth, with the direction pinned (`carried ≥ entry gap` on every trim of a real design). 9 new tests; suite **900 → 909 passed, 2 xfailed** (678.55s), ruff 12, 4 stream locks and 10 visual baselines untouched. Residuals stated: no UI control yet (API-level only), and no imported-file corpus was measured — the mechanism is tested, not a foreign-file population. |
| 96 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 58 — cross-colour ordering is worth 0.9%; decline it: [`docs/benchmarks/v2-part58-audit.md`](./docs/benchmarks/v2-part58-audit.md).** Measurement only; no file under `app/` touched. Reordering colour stops can only move the jumps *between* stops, so the ceiling is inter-stop travel — measured at **2.2–7.4%** of all jump travel. Solved **exactly** (brute force over permutations ≤8 stops, best-of-all-starts greedy above, so the figure is a floor on the optimum): A01 peacock 15 stops, 12.23 m travel, 0.90 m inter, best 0.61 → **saves 0.29 m**; A02 neckline black 18 stops, 20.50 m, 0.46 m inter → **0.04 m**; A03 panel 18 stops, 18.26 m, 0.71 m inter → **0.14 m**. **Total 0.47 m of 50.99 m = 0.9%**, and **trims do not change at all** — every object needs one transition whatever order the stops run in, which is Part 48's finding holding. **A03 reproduces Part 48 exactly (663 trims, 18.26 m) from a different code path**, which is what makes the table worth reading. **The comparison that settles it**: those three designs spend ~15.2 / 33.2 / 27.6 minutes on trims, against a whole reachable travel saving of well under a second of machine time — four orders of magnitude apart, which no policy or threshold changes. **Interleaving objects across colours fails on arithmetic before safety**: each switch back costs a colour change (~20 s re-thread) against a trim's ~2.5 s, so adding even one to save the entire 0.47 m is a net loss of ~19 s. **Risk taxonomy recorded anyway** (clearly unsafe: any reorder crossing a layering dependence — the engine's darkest-first order, Part 14 hole absorption and Part 16 detail deferral all key on `stitch_rank`; plausibly safe: spatially disjoint stops with no deferral or absorption between them; ambiguous: interleaved-but-not-overlapping, the common photographic case). **Recommendation: decline — not "too risky yet" but too small at any risk level, since the measurement is a ceiling.** Stated instead: Part 48 already took this win (36.20 → 18.26 m) and the remainder is 96–98% intra-stop and already optimised; **if machine cost is the priority the honest lever is the trim gate** — Part 48 measured `TRIM_MIN_GAP_MM` at 10 mm giving 485 trims against 663, a **27% cut**, and refused it on the stated principle that this writes files for arbitrary machines. That refusal stands and is not overturned here, but it is a product question with a price tag: if the target machine is known at export time, 27% is available. **Separate decision, `max_colors` cap (Part 57's finding): clamp explicitly and tell the user; do not raise the cap.** No measurement has ever shown a quality gain from a larger palette, and Part 57 showed more colours do not reduce fragmentation — but the current behaviour silently discards a parameter the user set. `user_warnings` already carries the opposite case (plan *raised* to N colours) and `Design.warnings`' own docstring names "a colour count that could not be honoured" as an example it does not currently cover. Narrowest fix is ~4 lines using existing machinery; **not implemented here** — it should be its own small part so the warning text gets reviewed rather than slipped in beside a routing analysis. Ships `scripts/measure_cross_colour.py`. Stream locks 4/4, visual baselines 10/10, ruff 12. |
| 95 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 57 — R005 is not a colour-count decision; close it: [`docs/benchmarks/v2-part57-audit.md`](./docs/benchmarks/v2-part57-audit.md).** Controlled hypothesis test, no engine change, no file under `app/` touched. **Neither outcome the brief anticipated: halving the colour count does not reduce the object count at all.** Sweeping `max_colors` 12/8/6/4 on the three tier-A photographs — A01 peacock 336 / 336 / 342 / **347** (**+3.3%**), A02 neckline black 834 / 834 / **929** / 840 (**+0.7%**), A03 panel 771 / 771 / 725 / **724** (**−6.1%**) — against a 30% gate, with **coverage holding within ±0.8 and improving on two designs** (A01 95.80→95.00, A02 95.90→**96.10**, A03 97.00→**97.10**). So it is not "objects fall while coverage holds" and not "both fall together"; the lever simply has no purchase. **Why: object count is driven by how spatially scattered each cluster's territory is, not by how many clusters there are.** Merging two colour clusters does not merge their regions — it produces one cluster whose pixels are interleaved across the image, which is *more* connected components. **A02 at k=6 is the clean demonstration: 929 objects from 6 colours against 834 from 8**, costing 2,814 extra stitches and 118 extra trims. That also explains Part 55's zero merges at a 0 mm gap: the tiny objects were never fragments quantisation split, they are separate pieces of the photograph, and re-quantising re-cuts them without joining them. **Separate finding worth its own decision: `max_colors` above 8 is inert.** k=12 and k=8 are **byte-identical** on all three photographs — same objects, stitches, trims and coverage — because `pipeline.py:257` caps the planner at `min(max_colors, 8)`. The product accepts 12 and silently plans 8; anyone reading a `max_colors=12` result until now was reading an 8-colour plan. Not fixed here (this part changes no code) but flagged, because it silently limits a user-facing parameter. **Decision: close R005 as working-as-intended on quality grounds.** Three independent levers are now measured and all refuted — post-contour merging (Part 55, knee-less, 2–4%), label-map smoothing (Part 56, gentlest possible filter fails gates 1–3), colour count (this part, no effect). "Median 19 stitches per object" is real and still true; it is not a defect removable without removing content. **The residual value is a cost problem, not a quality one** — even Part 56's failed filter cut trims on all three designs and this sweep cut A03's 663→571 — so the win is in *how* the objects are sewn, which is Part 48's territory extended to the cross-colour ordering it declined. A "simplify photo" mode is rejected on this evidence: a mode is worth building only if its knob does something, and if wanted for aesthetic reasons it should be scoped as a feature, not justified by fragmentation numbers it does not move. Ships `--colors` on `scripts/measure_r005_gates.py` and three visual strips in `docs/benchmarks/v2-part57-colors/` (the k=12 and k=8 panels are pixel-identical, which is the cap made visible). Stream locks 4/4 and visual baselines 10/10 pass; ruff 12. |
| 94 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 56 — R005 at the label map: a clean negative, and the stop condition fires: [`docs/benchmarks/v2-part56-audit.md`](./docs/benchmarks/v2-part56-audit.md).** The change was built, measured against all four digitize gates, and **reverted**; `app/` is byte-identical to Part 55. Built `_smooth_labels` in `planning.py` — a majority vote over the label map for photographic input only, gated on `is_textured` so flat art never reached it (gate 5 held by construction). The radius was **derived, not chosen**: `MIN_FEATURE_W_MM / 2`, on the argument that label structure finer than the narrowest sewable feature can never become a stitched object and so costs no content. **That argument is refuted.** Gate run, same seed and hoops: A01 peacock 336→**330** (−1.8%), interior 95.80→95.60, stitches 38,409→39,237; A02 neckline black 834→**859** (**+3.0%, worse**), interior 95.90→95.90, stitches 58,800→**63,032** (+7.2%); A03 panel 771→**342** (−55.6%) but interior **97.00→96.10 (−0.90)** and stitches 56,505→41,749 (**−26%**). **Gate 1 fails** (−30% needed on all three), **gate 2 fails** (coverage fell on two), **gate 3 fails** (stitches rose on two); only gate 4 passed (trims fell on all three: 364→344, 796→741, 663→339). Gates 5–7 were not reached — a change failing 1–3 does not get defended on flat-art baselines. **The sharpest fact is that the radius was already at its floor**: `round(0.125 mm ÷ 0.26 mm/px)` is 0, so the implementation clamps to 1 px and every number above is the *gentlest possible* version of the idea. So this is not a tuning failure and there is no smaller setting to retreat to. Three further readings: the response is **wildly heterogeneous** (−55.6% / −1.8% / +3.0% from one filter at one radius on three real photographs), majority voting can **split** a region by eroding a narrow neck below connectivity — so it is not even monotone in object count, ruling out "same idea, larger radius" — and A03 shows exactly the shape Part 55 forbade, shedding 56% of objects while deleting content. **Stop condition fires**, and the claim it licenses, stated so it can be attacked: **much of what Part 55 counted as fragmentation is content.** The tiny objects are isolated (nothing merges at 0 mm) because they are isolated *things* — flecks of colour genuinely in the photograph. That relocates R005 from "recover the shape that was cut apart" (no shape was) to a product question about how much of a photograph's colour detail is worth sewing. **Ships: `scripts/measure_r005_gates.py`** so the four digitize gates run the same way next time and this negative reproduces; no test is added for reverted behaviour. **Recommended next, before any more engine code**: re-run the three photographs at `max_colors` 6 and 8 against the same gates — if objects fall steeply while coverage holds, the fragmentation was colour count all along and the fix is a better photographic default; if coverage falls in step, close R005 as working-as-intended and take the win in *how* the objects are sewn (trims fell even here), which is Part 48's territory extended to the cross-colour ordering it declined. Suite **900 passed, 2 xfailed** (unchanged), ruff 12, 4 stream locks and 10 visual baselines untouched. |
| 93 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 55 — next target is R005, but not the R005 anyone proposed: [`docs/benchmarks/v2-part55-audit.md`](./docs/benchmarks/v2-part55-audit.md).** Measurement/scoping only; no engine change, no file under `app/` touched, no direction score anywhere in the part. **R008 declined on evidence that already exists**: Part 49's gate fired negative (coverage rises smoothly 3.5%→67%, no knee, longest run 10 beads), so restarting it means building a motif-along-a-path detector whose success cannot be measured. **R005 chosen** because it is real, general, and judged entirely by instruments that do not touch the disqualified direction reference. **Fragmentation is not one photograph's quirk**: A01 peacock 336 objects / median 30 / 46% ≤25 stitches, A02 neckline black 834 / 20 / 57%, A03 panel 771 / 18 / 64% — and the panel's median 18 confirms Part 46's "median 19" from a different code path. **The bench cannot judge this and that is the trap**: the ten fixtures hold **93 objects between them**, ~9 each, so flat artwork is not fragmented and a large visual-baseline change would be evidence the fix is hitting the wrong population, not evidence it works. **Both obvious levers measured and set aside.** Merging neighbouring same-colour regions is **knee-less** — 0 / 7 / 16 / 56 / 143 objects merge on the panel at 0.0 / 0.4 / 0.8 / 1.5 / 3.0 mm (2–4% at any gap a digitizer would defend) — the same shape that stopped R008; the tiny objects are **isolated colour islands**, not fragments of a cut-apart shape, which is why nothing merges at 0.0 mm. And speck absorption **already exists and is wired** (`_absorb_specks`, `planning.py:493`) — the sixth proposed fix found already shipped. Raising its threshold is explicitly **not** the route: Part 49 showed the dropped set contains real ornament and Part 47 showed the same instinct would have meant sewing sub-thread features. **So the lever is upstream — colour quantisation of photographic texture — and the next part gets seven numbered gates** measured on tier-A photos, not the bench: objects −30% on all three, **interior coverage must not fall (the anti-cheat, since deleting content moves both together)**, stitches fall or hold, trims fall, flat-art baselines near-untouched, no new zero-stitch designs or errors, and no runtime regression on 1500 px noise — plus a stop condition to report "fragmentation is content, not noise" rather than tune `SPECK_ABSORB_MAX_MM2` to hit a number. **A defect in my own scoping script, caught by its own test**: the neighbour search first called `np.where(labels == oid)` per object — a full-frame scan multiplied by the object count, the exact Parts 48/49 shape, in the script written to study fragmentation (23× for a 25× frame). Bounding boxes now come from one pass; a second bug rode in with that fix (the new loop shadowed the dilation kernel with its index) and the tests caught it too. Two of my test expectations were also wrong and were corrected rather than the code. 6 new tests; suite **894 → 900 passed, 2 xfailed** (765.54s), ruff 12, 4 stream locks and 10 visual baselines untouched. |
| 92 | 2026-08-04 | Claude (Opus 5) | 📋 Docs | **v2 Part 54 — the reference protocol: a capture spec that was measured, and a fallback: [`docs/benchmarks/v2-part54-audit.md`](./docs/benchmarks/v2-part54-audit.md) + [`docs/REFERENCE-CAPTURE-PROTOCOL.md`](./docs/REFERENCE-CAPTURE-PROTOCOL.md).** No engine change, no file under `app/` touched. **Explicit gate statement: the reference is NOT good enough to resume consumer work, and no better photograph exists here** — the other two uploads are 474×695 and 380×578 against the panel's 736×1689. So this delivers Priority A's tooling, validated end to end, and Priority B's fallback. **The headline number is corrected: Part 53's "roughly 0.05 mm/px" was arithmetic; measured, the requirement is 0.074 mm/px** (2.5× the current linear resolution), and more usefully **0.120 mm/px — 1.5×, easily within an ordinary camera on a close crop — already takes satin from 23% usable to 84%**. Derived by downsampling the real panel and re-measuring where the reading flips from edges to thread: crossover 2.0 mm at 0.186 mm/px rising to 3.0 mm at 0.531, and **the full-resolution row reproduces Part 53's 23% exactly from a different code path**, which is what makes the rest of the table trustworthy. In pixels the crossover runs 10.8 → 5.6, drifting because two limits alternate (the analysis window is fixed in px, thread pitch is fixed in mm), so extrapolation deliberately uses the **finest** point rather than a mean. **Crop registration** (ORB + partial affine) lets a macro close-up be placed in the design's frame — self-tested against transforms applied on purpose: origin within **0.4–0.5 px** and scale within **0.2%** at 2×, 3× and 4× upscale, with a test requiring random noise to FAIL rather than register confidently. **Two mistakes of mine, both caught and both recorded.** (1) The obvious synthetic-sew-out simulation was built first and **thrown away**: a periodic thread pattern near the sampling limit aliases to **zero 3-tap gradient response** (Sobel over `[a,b,a]` is 0), so it reported "edges" at every width and every blur including zero, and calibrating it back needed **two free parameters fitted to one observation** — which would fit anything. The downsample law needs none. (2) The first `crossover_width` scanned upward for the first bucket reading thread, which **collapses on one noisy narrow bucket** — it reported "0.4 mm crossover, 100% usable" at the very scale Part 53 had measured as unusable. Now checked top-down (lowest width from which *every* populated bucket above also reads thread); both bug and fix pinned by tests. **Priority B stated plainly.** Still answerable with today's panel: which of two contour-parallel field variants is better seeded (Part 52's ranking is unaffected — one registration, one territory, all boundary-seeded variants), direction on structures ≥2 mm (23% of satin, 32% of tatami), and everything that does not depend on orientation at all — coverage, penetration, trims, fragmentation, runtime. **No longer answerable: whether consuming the field helps satin (77% below the crossover) or tatami (68% below), and any absolute direction figure on thin artwork.** If no better photograph arrives, the alternative axis is **paired visual comparison** using Part 44's deterministic renderer and Part 50's quiver panels, with the call recorded — weaker than a number, and honest about being weaker, but the only axis that does not reward agreeing with outlines. **Recommendation: stop R004 numeric optimisation until a better reference exists; spend effort on R005 or R008, both measurable with instruments already trusted.** 6 new tests; suite **888 → 894 passed, 2 xfailed** (797.15s), ruff 12, 4 stream locks and 10 visual baselines untouched. |
| 91 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 53 — neither: the sew-out reference cannot judge this question yet: [`docs/benchmarks/v2-part53-audit.md`](./docs/benchmarks/v2-part53-audit.md).** Measurement only, no engine change, no file under `app/` touched. The brief asked whether satin's 0.15° aggregate was hiding a local win. **It was hiding something worse: the 0.15° was never a measurement of satin.** Every field table since Part 50 scores "today" as `per_object_field(o.stitch_angle)`, one angle per region — correct for a tatami fill, meaningless for satin, whose `stitch_angle` is `cv2.minAreaRect(contour)[2]`, a bounding-box property, while the column is actually swept perpendicular to the medial axis and turns along its length. New `scripts/measure_field_headroom.py` compares, **at the same points**, what we actually sewed, what the field would say there, and the sew-out — registration by true uniform scale, checked against the pipeline's own seed (IoU 0.872), and validated against Part 46: **48.41° over 42,600 sewn segments vs Part 46's independent 49.9°**. **It then produced a +16.98° satin "win" — the largest result in this line of work — and the win is an instrument artifact.** Two things in the same table said so: a **constant 90° beats our actual sewn output** (33.91 satin / 33.71 tatami vs 48.26 / 53.68), and the width buckets **invert** — thin columns +23.09, and the widest **−2.20**. Cause: a structure tensor reports the dominant orientation *in its window*, so on a column narrower than the window the two column EDGES dominate and it returns the **column's axis**, perpendicular to the thread there. A correctly sewn column is then scored ~90° wrong and a contour-parallel field — which is what `direction_field.solve` produces by construction — is scored right. Verified by asking which the reference agrees with: at 0–1 mm and 1–2 mm it agrees with the AXIS (42.64, 39.82) over SEWN (47.36, 50.18); at 2–3 mm and 3–4 mm it **flips** to agreeing with SEWN (42.40, 41.76) over AXIS (47.60, 48.24). **Not the window's fault** — the verdict holds at windows 5, 9, 15 and 21. Forced by scale: at **0.186 mm/px** a 1.5 mm column is **8 px** and satin's ~0.4 mm thread pitch is **2.1 px**, at the sampling limit. **77% of all satin segments sit below that threshold**; on the 23% the reference can resolve, the field is **2.20° worse than what we already sew**. **Q2 answered too: the tatami ceiling is not resolution-limited** — 384→1152 px moves committed share *down* (0.663 → 0.625), so finer solving does not make ring and border interiors directional, and the field at 384 px (26.27) is **already past the one-angle-per-region oracle (26.54)**, an upper bound chosen against the reference itself — so no smarter angle chooser has anything left to win; only a generator that varies direction *within* a region, which Part 51 showed a scanline fill cannot be. **Cost gate: raising resolution does reintroduce growth** — 1500 px noise 0.486 s → **6.519 s** at 1152/72, 13×, so the 384 px bound is earning its place. **Decision: neither consumer is justified — not "not yet worth it", but not measurable with this reference.** The unblocking step is not code: a macro sew-out photograph at roughly **0.05 mm/px** would put ~8 px on every thread and make the satin question answerable for the first time. Also stated plainly: **the 49.9° headline that has driven R004 since Part 38 is partly this artifact** — on resolvable columns our sewn error is 42.3°, not 50°. Parts 50–52 stand: the field, the instrument and the two-pass architecture are unaffected, since Part 52's seed ranking used one registration throughout. 5 new tests pinning the artifact on synthetic artwork with known truth (a band of horizontal threads reads *vertical* once the window outgrows the band, *horizontal* when it does not) plus the oracle bound; suite **883 → 888 passed, 2 xfailed** (791.34s), ruff 12, **4 stream locks and 10 visual baselines untouched**. |
| 90 | 2026-08-04 | Claude (Opus 5) | ♻️ Refactor | **v2 Part 52 — the two-pass split: the validated seed now exists before any generator runs: [`docs/benchmarks/v2-part52-audit.md`](./docs/benchmarks/v2-part52-audit.md).** `digitize_image` was one loop — find a cluster's regions, sew them immediately — so the union of the design's object contours did not exist until every object had already been sewn, which is why Part 51's D2 could only reach the worst of the three seeds. It is now **pass A** (cluster masks, substrate rule, contours, speck filter, smoothing, seed union — no stitch, no colour stop, no log write), **one field solve**, then **pass B** (sew exactly what pass A decided). **Stitch output is byte-identical**: 4 stream locks, 10 visual baselines, panel 56,505 stitches and 771 objects before and after, and both diagnostic logs unchanged because the drop log and the classification log are still written in pass B at their original points with their original `seq`. Proximity ordering also stayed in pass B — it needs where the last stitch landed, which is generation state. New module `staging.py` (layered between `planning` and `pipeline`, enforced) holds `RegionPlan`/`ClusterPlan`/`DigitizePlan` plus a light `FieldArtifact` snapshot, so a measurement can ask from outside WHICH seed the field came from rather than trusting the code that was just written. **Correctness gate, on a real run: the union seed wins on every territory** — whole 37.97 / tatami 36.49 / satin 37.75, against per-cluster 40.30/37.67/40.23 and foreground silhouette 38.31/38.84/38.17. **And it produced a correction to Part 51 that is mine to own.** The gate first read 36.49 where Part 51 validated 32.34, and rather than report the gap I chased it: the seed accumulator is **bit-identical** to a naive full-frame draw (IoU 1.000000 over 771 regions), holes are not the cause (punching *more* helps; dropping the small ones costs 2.7°), and the working frame is not the cause (Part 51's own union mask downsampled into it still scores 32.64). The cause is that **Part 51 compared seeds across two coordinate frames** — its `rasterise` stretches the design's mm extents to fill the source frame while the silhouette came from the pipeline's working frame, and since the design's bbox fills only 98.9%×99.2%, the two differ by ~1% scale and 6px, moving **~17% of all boundary pixels** (IoU 0.704). The silhouette figure, which came from the pipeline, is **38.84 in both**; the union and per-cluster figures were each flattered by 2.4–4.2°. **So the seed-class spread is 2.35°, not the 6.50° Part 51 claimed.** The ranking survives and Part 51's revert of D2 stands — its constant-90°, random and region-angle controls all shared one frame — but the magnitude was overstated. **Performance measured HEAD vs split on both a real panel and pathological noise**: 47.58→44.98 s (panel), 83.24→85.96, 153.50→151.13, 181.52→177.21 s (600/900/1500 px noise) — within run-to-run noise in both directions, which is the Parts 48/49 trap avoided by solving **once per design** at fixed resolution and accumulating the seed **bounding-box local** (a region costs its own area, not the frame's). 8 new tests including sequencing asserted on the real call order of `solve` against `_fill_by_component`; suite **874 → 883 passed, 2 xfailed** (748.42s), ruff 12. **Nothing consumes the field yet, deliberately.** Corrected headroom under one registration is 40.09→36.49 on tatami and 37.90→**37.75** on satin — the satin gain is now **0.15°**, not the 4.4° the uncorrected table implied, so **neither consumer looks compelling on this evidence** and D3 should measure before building. |
| 89 | 2026-08-04 | Claude (Opus 5) | 🧪 Test | **v2 Part 51 — R004-impl D2: tatami cannot consume this field, and the reason is structural: [`docs/benchmarks/v2-part51-audit.md`](./docs/benchmarks/v2-part51-audit.md).** D2 was **implemented, measured, and REVERTED** — no engine change ships. It cleared the brief's bar (38.27 → **38.10** on the panel) and should not have been merged on that. **The bench told me first: byte-identical output on all ten fixtures.** Instrumenting every call site explains it — **0 of 26 tatami regions took the field**, coherence median **0.0002**, max 0.0138. Alignment was verified before concluding (same shape, 100% overlap), so this is genuine washout, not a framing bug: 24 diffusion iterations reach about a quarter into a 1200px mask, so the middle of a 311,000px region is **exactly zero**. Part 50 measured committed share at 7–14% over the whole mask; at *region* granularity on large fills it is nil. **The control killed my first result: a CONSTANT beat my direction field.** On tatami territory — constant 90° **37.05**, D2 as wired **38.32**, current principal axis **40.09**, random angle **40.38**. The current principal axis performs no better than random on this panel, and the threshold sweep had **no knee** with its best point at **0.00** (ignore the confidence gate entirely) — so the confidence fallback, the specific mechanism the brief asked for, was not earning its place. **Why, and it is my defect, not the field's: I wired in a different field from the one Part 50 validated.** The pre-wiring sweep predicted 35.59 and the wired engine gave 38.32; **I first wrote that off as an over-optimistic offline model and that was wrong** — the model was right and the implementation was feeding the generator a worse field. Part 50 solved on the union of object contours; my wiring solved on `fg_mask`, the segmentation foreground, whose boundary is the subject's *silhouette* rather than the colour regions'. Per pixel on tatami territory: union of contours **32.34**, per-cluster **35.31**, fg_mask **38.84** — **6.5° of field quality lost to a seed mask I chose without measuring**. With the right field the collapse does beat the constant (35.83 vs 37.05), so the mechanism was sound and the input was not. **The ceiling is the real finding: a straight row cannot use a field.** On tatami the field is worth **7.75°** per pixel (40.09 → 32.34); one angle per region captures **4.26°**, or **55%**. The other 45% is unreachable while fill rows are straight — a ring is the clean case, where the field runs all the way round and the doubled-angle mean cancels to zero, pinned by a test. **And the bar could not have resolved this anyway: tatami is 9.3% of panel area**, so field, constant and random all land within **0.7°** of each other on the whole-panel number. **No drop-in correction exists**: the winning seed is the union of every object contour, which does not exist at any point where a fill is generated, and the seed that *is* available before fills (per colour cluster) measures **38.94** collapsed — worse than the fg_mask version it would replace. Consuming the validated field needs `digitize_image` restructured into two passes (build all region masks → solve → generate), which is larger than this brief scoped. **Ships: `scripts/measure_field_consumption.py`** (ranks seed masks, measures the collapse, runs the constant/random controls) and **`tests/fixtures/reference_sewout.jpg` — the sew-out photograph, now in the repo**; Parts 46, 50 and 51 all headline against it and it existed only in an ephemeral uploads directory, so every direction number in this project was irreproducible from a fresh clone until now. 6 new tests; suite **868 → 874 passed, 2 xfailed** (718.73s), ruff 12, **4 stream locks and 10 visual baselines untouched**. **D3 is satin**, for a stronger reason than area: it is **93.7%** of the panel against tatami's 9.3%, carries the same headroom (37.90 → **33.49** per pixel), and a satin column already varies its angle along its length — so it can consume a field **without the collapse that costs tatami 45% of the gain**. Curved tatami rows are the other half and need their own scoping. Both need the two-pass restructure first. |
| 88 | 2026-08-04 | Claude (Opus 5) | ✨ Feature | **v2 Part 50 — R004-impl D0/D1: the instrument, and a field that nothing consumes: [`docs/benchmarks/v2-part50-audit.md`](./docs/benchmarks/v2-part50-audit.md).** **D1 is worth taking to D2.** On the photographed sew-out — the one non-circular reference, 83% qualified — the solved field scores **33.91°** against per-object **38.27°** and constant-45 **45.02°**, and **within-15° goes 20.8% → 34.3%**. Solve is **0.06–0.09 s** on the 771-region panel. **No stitch output change: 4 stream locks and 10 visual baselines untouched**, because nothing reads it. **D0**: `measure_direction_field.py` scores a FIELD rather than sewn stitches, so candidates rank before anything consumes them; self-test extended past stripes to two synthetic varying fields (radial **1.88°**, concentric rings **1.71°**); reports median, within-15/30, a 3×3 spatial breakdown, Part 46's `qualified_share`, and a new `committed_share`. **I found and fixed a flaw in my own instrument mid-part**: it weighted each pixel by the candidate's own confidence, which is 1.0 everywhere for per-object and lower for a diffused field — so the diffused field was scored mostly where it was already sure. Worth **2.2°** (31.73 → 33.91). Weights are now uniform; a test pins it. **D1**: doubled-angle representation (mean(179°,1°) must be 0°, not 90°), boundary TANGENT seeds — from the contour polyline, not an image gradient, which at a mask edge is the normal — diffused inward. Gives contour-parallel flow. The badge picture is the argument: its rings currently get one vertical angle across the whole annulus; the field runs concentrically. **Stated against my own result**: on flat artwork the reference IS the artwork's edges and the field is seeded from those edges, so bench agreement is partly circular — hence the panel is the headline. **Limitation that shapes D2**: the field washes out in large interiors — committed share 81–94% on thin strokes but **7–14% on large flat areas** — so D2 must decide what happens where the field is undecided. **Cost bounded before anything depends on it** (the Parts 48/49 lesson): solved at 384px, so 24,737 → 154,449 contours costs 0.374 → **0.405 s**, flat not linear. 14 new tests; suite **854 → 868 passed, 2 xfailed**, ruff 12. |
| 87 | 2026-08-03 | Claude (Opus 5) | 🧪 Test | **v2 Part 49 — R008: the dropped specks do not separate into ornament and noise: [`docs/benchmarks/v2-part49-audit.md`](./docs/benchmarks/v2-part49-audit.md).** The brief gated on step 1 — *"if they do not separate cleanly, stop and report that"* — **and they do not, so this stops there.** No detector, no generator, no threshold change. Part 48's figures checked out (768 drops, median 0.896 mm², bead diameter 1.07 mm) and the ornament IS in the drop set — `v2-part49-dropped-specks.png` shows marker strings following the neckline arc and the trellis bars. **Three tests, all negative.** (1) Interior-of-a-chain: 15.8% qualify, 77 runs, **longest 10 beads** where a neckline border is dozens. (2) Gap-tolerant growing swept over permissiveness: coverage rises **smoothly 3.5% → 67%** with **no knee** — a distinct chain population would jump then plateau, so any threshold here picks how much noise to accept, not where the ornament is. (3) Combining drops with small kept objects: **15.0% vs 15.8%** — refuted. **Why it fails is the useful part: 529 of 771 objects are already ≤8 mm²**, so "small round region" describes flower centres, petal tips and leaf dots equally; and dots under ~4 px never reach the drop log at all (removed before contouring). A chain is split across three populations — kept, dropped, and vanished — and none holds the whole thing. Recovery needs repeated-motif-along-a-path detection at the **mask stage**, which re-scopes R008 to something comparable in size to the direction field. **What ships: `_DROP_LOG` now records each dropped region's centroid**, without which the log can say how much and how big but never whether it was structured. That had to be instrumentation, not a script: the morphological open inside the loop removes single-pixel noise, so my first measurement re-derived the drops outside the pipeline and counted **25,822 against the real 768** — wrong by 34×. **A performance regression shipped and the fuzz suite caught it — again.** The first centroid took `cv2.moments` over the FULL-SIZE filled image once per dropped region: invisible on a real design, catastrophic on noise where one colour holds tens of thousands, taking the fuzz suite's 1500×1500 post from **~16 s to over ten minutes**. It failed 28 minutes into a full `-x` run, with the targeted tests, locks, visual baselines and ruff all green beforehand — exactly the Part 48 shape. Fixed by taking moments over the **contour** (O(outline points)) with a mean-of-points fallback; fuzz suite back to 7 passed in 4m39s, and a cost test now lives in the fast file. **This is the second perf regression in this same loop in two parts** — the contour loop runs once per region BEFORE filtering, so anything added there is multiplied by the noise count, not the design count. Also worth recording: an earlier draft of the audit quoted "853 passed" from arithmetic before the run finished; it was stripped before committing and would have been wrong (the run returned **1 failed, 769 passed**). **Suite 849 → 854 passed, 2 xfailed** (+5: four for the log, one pinning the cost); ruff 12, locks and visual baselines unchanged (no behaviour change). Recommended next: **R004-impl D0/D1** — it has a written plan, an abandon condition, and its first two stages change no stitch output. |
| 86 | 2026-08-03 | Claude (Opus 5) | ⚡ Perf | **v2 Part 48 — R006: the gaps were long because the order was wrong: [`docs/benchmarks/v2-part48-audit.md`](./docs/benchmarks/v2-part48-audit.md).** **844 → 663 trims (−21.4%)** and **36.20 m → 18.26 m of jump travel (−49.6%)** on the reference panel, coverage identical. Measured first: the median trim spanned **27.05 mm** because `findContours` returns regions in raster order, so a colour's regions were sewn top-to-bottom regardless of where the last one ended. **The brief's third lever was dropped on measurement** — only **2.6%** of trims spanned under 3 mm, so converting short jumps to running stitches would have touched 22 of 844. **The attribution is the interesting part, and the obvious reading is wrong**: reordering cannot remove a single trim (still one transition per object — 844 either way), it halves the *distance*; the 6 mm gap gate is what removes trims and alone is worth only 8.2%; together they give 21.4%, because proximity order is what makes short gaps common (28.6% of gaps under 6 mm after, 9.2% before). *I nearly reported this backwards: the first sensitivity sweep showed the gate having no effect at any threshold, which was the TEST being wrong — `TRIM_MIN_GAP_MM` is imported by value, so patching `constants` never reached the call site, the same live-binding trap Part 42 documented for `_PENETRATION_FLOOR_MM`.* **The threshold is NOT tuned to the target**: 10 mm gives 485 trims and would have beaten the brief's "<586", and is refused — this writes files for arbitrary machines, and assuming an aggressive auto-trimmer leaves 1 cm of loose thread on every machine without one. **Scope: interior coverage identical on all 10 fixtures**, object counts unchanged, all 10 visual baselines above the SSIM gate (lowest 0.998768); 3 of 4 stream locks re-pinned deliberately — this is a behaviour change, not a refactor. Bench fixtures gain only 2.0% (539 → 528) and that is expected: they are small designs with few regions per colour, and the pre-work simulation predicted it (badge 0% headroom, mascot 42%). Corrected the cited economics too: 181 fewer trims is ~7.5 min of machine time, not the 35 min the brief attributed to all 844. **Not done, deliberately**: cross-colour reordering (changes the operator's thread-change order and can put light under dark — own change, own measurement) and exact TSP (greedy already halves travel). 11 new tests. Also adds [`docs/PROMPT-direction-field.md`](./docs/PROMPT-direction-field.md) — the R004-impl scoping brief the reviewer asked for: five stages (instrument → field visualised but consumed by nothing → fills → satin → the user control), each with its own gate, plus an explicit list of what would make the whole thing not worth doing. **Follow-up in the same part: the ordering first shipped unbounded and HUNG the fuzz suite** — nearest-neighbour is O(n²) over contours *before* the speck filter, and a 900×900 noise image puts **70,516** in one colour against the panel's **251**. That is a DoS shape reachable from the public API, not a slow test. Bounded at `NN_MAX_REGIONS = 2000` (8× above real, 35× below noise) with a raster-order fallback, inner loop vectorised (2000 points in 0.09 s), 4 tests pinning it. Every targeted gate — locks, visual, corpus, frontend — had passed before the full suite caught it. |
| 85 | 2026-08-03 | Claude (Opus 5) | 🐛 Fix | **v2 Part 47 — R007: the zero-stitch designs were not the bug: [`docs/benchmarks/v2-part47-audit.md`](./docs/benchmarks/v2-part47-audit.md).** Re-ran the full corpus: **8 zero-stitch, 0 errors** — the "9" dated from Part 36 and had drifted (C02 and C15 started working via the Part 36 palette recovery; C04 regressed in) because it was being quoted rather than re-measured. **One of them is a blank image**: `C01_hairline_linework` has ZERO pixels differing from its background — `hairline_linework` draws its only ink in `pal[3]` and `PALETTES[1][3]` is (250,250,250) on a (255,255,255) canvas. Zero stitches is correct there, and the fixture has been counted as a pipeline failure in every audit that quoted the number. **The other seven are the engine being right**: each logs `sub_thread_feature` at a median region width of **0.23mm** against a 0.4mm thread, and its warning ("a larger hoop keeps more detail") is TRUE — same artwork at 200x300 instead of 130x180: C14 0 → **36,133**, C28 0 → **18,250**, C30 0 → **4,618**, C43 14 → **4,621**. So "fix the zero-stitch designs" would have meant weakening the sub-thread guard — making the engine sew features narrower than its own thread. Fourth consecutive brief refuted by measuring first. **What WAS broken: a correct refusal arrived as a success** — `POST /api/digitize` returned **200 with an empty Design**, so the caller got a valid-looking design and an export that sews nothing. Now **422** naming the hoop and carrying the engine's own explanation. The guard sits at the **API boundary, not in the engine**, because the corpus runner and bench need an empty result as data, not an exception — pinned by a test. Corpus generator fixed too: palettes ordered by contrast against the canvas, and `_write` refuses any fixture under 0.2% ink. 6 new tests; suite **828 → 834 passed**, ruff 12, locks and visual baselines unchanged (no pipeline change). Remaining 7 are tier-C parametric synthetics unstitchable at their assigned hoops — raise the hoops or leave them as proof the guard fires; neither is engine work. |
| 84 | 2026-08-03 | Claude (Opus 5) | 🧪 Test | **v2 Part 46 — R004: five hypotheses for the 49.9°, four of them mine, all refuted: [`docs/benchmarks/v2-part46-audit.md`](./docs/benchmarks/v2-part46-audit.md).** No pipeline change — this part establishes what the number is and is not. **Two of the brief's three premises are false.** *"No shape analysis at all"*: `_fill_angle` has computed each region's principal axis from central image moments since Part 24 (moments over `minAreaRect` deliberately — a plus, a ring and a star share a square hull), satin runs perpendicular to the medial axis by construction, contour fill follows the outline. *"Uniform 45° fills"*: measured **41 distinct angles** across the bench, per-fixture sd up to **67.5°**. Third consecutive part where the proposed fix was already shipped. **The 49.9° itself survived four attacks.** (a) Convention bug — global offset sweep is a shallow bowl, 49.48 at 0° bottoming at 40.04 near 100°; a flipped perpendicular would give a deep minimum. (b) Fragmentation — error is FLAT across object size (54.3 / 49.9 / 48.6 / 46.0 / 47.0 / **51.25** for 250+, which carries 55% of segments), so **R005 will not fix it**. (c) Satin-specific — no: SATIN 49.7 (n=30,216) vs TATAMI 50.0 (n=3,621). (d) Misregistration, the one I most expected — the instrument's own check shows mean per-stop colour distance **35.9** and its largest stop (38% of all segments) at **100.2**, yet gating per segment to the best-matched 13.7% moves the number only 49.5 → **45.8**. Ruled out. **Two real instrument defects found by auditing it:** the per-type breakdown read `stitch_type.value` but `use_enum_values=True` makes it a plain string, so **every object fell to the `ALL` default and the breakdown has reported one bucket since it was written** (this also means my earlier "30,332 vs 3,794" split did not come from the code path I thought; corrected figures are 30,216 vs 3,621); and a bad score had no way to defend itself, so the well-registered subset is now always printed beside the headline. **Conclusion: the remaining work is not PCA** — that is built and at its ceiling. Error uniform across size, type and registration quality means the approach is wrong, not the implementation: per-object independent axes cannot express a flow shared by neighbouring regions, which is the case `PROMPT-graph-stitch-engine.md` already makes for a direction FIELD. The brief's >0.7 correlation is not reachable by the route it proposes. 4 new tests; suite **824 → 828 passed**, ruff 12, **4 stream locks and 10 visual baselines unchanged** (no pipeline change). Cheaper items genuinely available next: R007 (9 zero-stitch designs — content loss), R006 (837 trims), R008. |
| 83 | 2026-08-03 | Claude (Opus 5) | 🐛 Fix | **v2 Part 45 — R011: knocked-out artwork is not the page; the Part 41 content-loss regression is fixed: [`docs/benchmarks/v2-part45-audit.md`](./docs/benchmarks/v2-part45-audit.md).** **Both discriminators I expected failed.** Enclosure: fixture 02 is 99.9% enclosed, the neckline panel 92.8% — no separation. Thickness: 85.9% vs 88.5% of enclosed area above a 0.30mm half-width, and the panel's gaps are *wider* (1.51mm vs 0.84mm). Measuring them needed the real palette stage, which only became spy-able because **Part 42 turned `_plan` into module-level `_plan_palette`**. **What does separate them is the input class**: flat artwork scores 0.00–4.10 on `_interior_texture`, a photograph of cloth 8.41–10.90, against an existing 6.0 threshold. So textured input keeps Part 41 **exactly** (whole substrate cluster unstitched — the "never thread the background" instruction is untouched), while on flat art a substrate-coloured component is page only if it REACHES the page or is too big to be knocked-out detail. **The size half exists because the Part 44 harness caught my first fix filling fixture 04's ring with 176 white stitches on white fabric** — the interior of an outline ring is enclosed and is still page. Two gates, both restored in spirit from the rule Part 41 deleted: `SUBSTRATE_ENCLOSED_MAX_SHARE = 0.05` (15× above the wordmark's 0.33%, 6.5× below the ring's 32.5%) and `SUBSTRATE_ENCLOSED_MAX_MM2 = 40.0` (2.3× above the glyphs' 17.6mm², 2.4× below the hub's 97.6mm²). The old 8.0mm² cap is deliberately NOT reused — calibrated for catchlights, it would have kept only the smaller half of the letters. **Fixture 02: 3 → 4 stops, 6,185 → 6,895 stitches; the wordmark is not merely restored but sews properly for the first time** (pre-Part-41 it had just 31 white stitches). **Scope measured, not asserted: all 4 stream locks byte-identical, 9 of 10 visual baselines at SSIM 1.000000** (only the broken fixture changed), corpus tier A 13 ok / 0 errors / 0 empty, both panels' closest stop to cloth 13.9 and 110.9 against a 12.0 delta. Six new both-sides tests; the fixture-02 xfail is now a passing assertion. Suite **817 → 824 passed, 3 → 2 xfailed**, frontend 131, ruff 12. **Residual stated honestly**: `_interior_texture` measures texture, not provenance, so a rotated/rescaled flat logo still reads as textured and keeps the blanket rule — unchanged from today, not a new regression. |
| 82 | 2026-08-03 | Claude (Opus 5) | 🧪 Test | **v2 Part 44 — R003: a committed visual-regression harness, and the two defects it found within the hour: [`docs/benchmarks/v2-part44-audit.md`](./docs/benchmarks/v2-part44-audit.md).** New `services/stitch_render.py` draws the stitch STREAM (not contours) as a pure function — same design in, byte-identical PNG out — plus `scripts/visual_regression.py` (SSIM + diff strip + `--update` + `--contact-sheet`), 10 committed baselines, 14 tests in the default suite, and the browser half in `tools/visual/app-screenshots.mjs` (7 pages × light/dark, fails on uncaught JS). SSIM is implemented on OpenCV/NumPy rather than adding scikit-image for one function. **The plan's 0.85 threshold was measured and rejected**: an unchanged pipeline scores exactly **1.000000**, and disabling the penetration floor — a real change — scores **0.909–0.993**, so 0.85 would have passed all of it silently. Gate is **0.995**, with the argument pinned in a test. **Defect 1 (fixed): the auth pages ignored light mode** — `AuthPages` rendered `.dz-root` with no `data-theme`, so sign-in/sign-up/forgot were byte-identical dark in both themes (mean 15.8 vs the dashboard's 247.5/21.0); a light-mode user went from a white dashboard to a black sign-in page. Extracted `lib/theme.ts`, used in both, verified in-browser at 247.0/15.8, guarded by a source scan proven to flag the pre-fix code. **Defect 2 (recorded, NOT fixed): fixture 02 lost its wordmark in Part 41.** Bisected to `c62bf33`: before, 4 stops including `#fafafa` with 31 stitches, 7 objects, 6,221 stitches; now 3 stops, 6 objects, 6,185 — the white type on the green card reads as substrate because the page behind is also white. **My Part 41 audit said 'the flat-art corpus is bit-for-bit identical' on the strength of 4 locked fixtures; 02 is not one of them, and four fixtures are not the corpus.** No metric could catch it — coverage is scored against the objects that WERE emitted. Left unfixed on purpose: the enclosure-vs-contiguous discriminator must also hold on the black neckline panel, and I could not reproduce the clustering outside the full pipeline, so only one side is measurable today. Recorded as a strict xfail so the baseline PNG is not mistaken for approval. **Tracked as R011, now above R004.** Suite **803 → 817 passed, 3 xfailed**; frontend **127 → 131**; tsc clean; ruff 12; locks unchanged. |
| 81 | 2026-08-03 | Claude (Opus 5) | 🐛 Fix | **v2 Part 43 — R002: the enum stops advertising stitches the engine cannot sew: [`docs/benchmarks/v2-part43-audit.md`](./docs/benchmarks/v2-part43-audit.md)**, plus a verified triage of the uploaded industry-comparison deck: [`docs/EVALUATION-industry-comparison-verified.md`](./docs/EVALUATION-industry-comparison-verified.md). **Measured rather than read**: rebuilding one design under every enum member and hashing the stream showed **23 names → 9 distinct outputs**. Eleven (CROSS_STITCH, CHENILLE, PHOTO_STITCH, GRADIENT_BLEND, MOTIF_FILL/RUN, STEMSTITCH, ZIGZAG, E_STITCH, ACCORDION_FILL, LAYDOWN) returned a **byte-identical tatami fill** through `rebuild_design`'s catch-all `else`; **BACKSTITCH was byte-identical to RUNNING_DOUBLE** and **REDWORK to RUNNING_SINGLE**. *Correcting my own earlier figure of "16 phantoms, 4 real": it is **13 misleading names, 9 real behaviours**.* **Deleted 13 members**; kept 10 (the 9 distinct plus `MANUAL`, which is provenance not a generator and is declared as an alias in the test). **The actual fix is removing the catch-all** — rebuild now raises and names the object instead of silently sewing tatami, so the next added member cannot repeat this. Legacy designs still load: `LEGACY_STITCH_TYPES` + a `field_validator` map every removed name to the stream it really produced, so old files open and rebuild identically. Frontend enum mirrored. **Suite 788 → 803 passed** (+15 property tests: no two types may share a stream), frontend 127 passed, `tsc` clean, ruff 12, **4 stream locks unchanged**. **On the deck itself: two of its five "critical gaps" are false.** *"Zero pull compensation"* — measured 0.15–0.50mm/side across 16 fabrics, finished width 55.20 → 55.80mm on fleece, 8 tests. *"Zero fabric awareness"* — `FABRIC_PROFILES` is 16 fabrics × 5 parameters; same bar, same hoop: denim 1,462 vs fleece 886 stitches, a 65% swing. *"0/5 path optimization"* is overstated (`_route_travel` removed 63 hole-crossing trims in Part 25) though the 837-trim number is real. The other three gaps (direction 49.9°, fragmentation, phantom types) were already ours. |
| 80 | 2026-08-03 | Claude (Opus 5) | ♻️ Refactor | **v2 Part 42 — R001: `digitizer.py` split into a layered package: [`docs/benchmarks/v2-part42-audit.md`](./docs/benchmarks/v2-part42-audit.md).** Rank 1 of the reviewer's revised plan. One **4,731-line** module became **ten modules behind an unchanged import path** — `constants` (760) · `geometry` (561) · `skeleton` (439) · `columns` (467) · `fills` (443) · `satin` (298) · `underlay` (262) · `routing` (331) · `planning` (513) · `pipeline` (1,131), plus a facade re-exporting all **189** public names, so **no caller changed**. Layering is strict bottom-up and **tool-enforced**: `scripts/split_digitizer.py` refuses to write if any import points upward — it found **nine back-edges** on the first run from names that lie about where they belong (`_run_along` reads like geometry and is underlay; `_enforce_floor` reads like satin and operates on column pairs), each now an explicit `OVERRIDE` entry. **Three tool defects were caught before it wrote anything:** an off-by-one in comment capture; `AnnAssign` ignored, which would have silently dropped `FABRIC_PROFILES` and the three runtime logs (a `NameError` at first call, not at import); and trailing comment blocks — three measured design rationales — attached to nothing. The tool now asserts **every one of 4,487 non-blank lines lands in a module**. **The one thing that could not move verbatim:** `_PENETRATION_FLOOR_MM` is the only *rebound* global, so a plain re-export would snapshot it and `set_penetration_floor()` would appear to work while silently doing nothing at its 24 read sites; those reads go through the module object, and a test pins it. Also extracted `_plan`, a **224-line closure** over ten read-only locals, to `planning._plan_palette` — `digitize_image` **1,032 → 822** lines. **Proof it is inert: all 4 sha256 stitch-stream locks unchanged** (a single coordinate moving 0.0001mm changes the digest); suite **772 → 788 passed** (+16 new layering tests), 2 xfailed; `ruff check app` **12 before, 12 after**. Two tests updated for real reasons: a facade **cannot** forward monkeypatching (`test_part33` now patches `pipeline._verify_sketch`), and `test_verify_lint_claim` named the deleted file. **Honest miss:** `<400` lines is met by 7 of 10; `pipeline.py` is 1,131 because `digitize_image` is still one 822-line function whose 412-line cluster loop carries **18 mutable locals in and 7 out** — extracting it needs a state carrier, a design change rather than a move, so it is deliberately a separate task. *(Correcting the Part 41 row: the stream lock covers **4** fixtures, not 10.)* |
| 79 | 2026-08-03 | Claude (Opus 5) | 🐛 Fix | **v2 Part 41 — the garment is not a thread; background stitching removed: [`docs/benchmarks/v2-part41-audit.md`](./docs/benchmarks/v2-part41-audit.md).** User: *"We never do the thread for background. Its a black cloth. Do only for the design."* Correct, and it was the top defect in the Part 40 review — **2,928 stitches (5.1% of all sewing) were laid over bare fabric** on the black panel, from **two** independent paths. **(A)** A colour cluster the same colour as the cloth: the stops sat 10.5 from the substrate, inside the 12.0 `SUBSTRATE_DELTA`, so the rule saw them — but it only dropped LARGE garment-coloured regions and kept small ones as "knocked-out detail", and on this design the small ones are the **gaps between petals**. Garment-coloured clusters are now never stitched on raster input (SVG unchanged: a white shape on a white page is declared artwork, and Part 25 measured this rule deleting exactly that). **(B) The dark-linework pass, the bigger half** — Part 30's black-hat stitches outlines darker than their surroundings, but on dark cloth **nothing can be darker than the cloth**, so it traced the gaps: 2,644 stitches in 230 running objects. **Worth recording: dropping the black colour stop alone removed only 284 stitches because the linework simply RECOLOURED to the next-darkest thread — which is worse, being visible on bare cloth** — so the pass is skipped outright below `DARK_CLOTH_LUM = 60`. **Result: 57,027 -> 54,099 stitches (−2,928), 1,014 -> 768 objects, 1,080 -> 837 trims, 22 -> 19 stops, 230 -> 0 running objects — with interior 97.10 and edge band 94.20 IDENTICAL, floor/density 0/0.** Less thread, less machine time, 243 fewer trims, nothing of the design lost. **All 10 stream locks byte-identical** (no corpus fixture is on dark cloth); tier A corpus re-run 13 designs, 0 errors, 0 empty, interior median 99.05 unchanged. Dead code removed: `_drop_large_substrate_regions` (37 lines) and `SUBSTRATE_ENCLOSED_MAX_AREA` — there is no longer a which-garment-regions-to-keep decision. pytest **772** + 2 xf (4 new, incl. light cloth KEEPS its linework); ruff 19. |
| 78 | 2026-08-03 | Claude (Opus 5) | 📋 Review | **v2 Part 40 — side-by-side review of the shipped output against the original panel, rendered as real thread bands: [`docs/benchmarks/v2-part40-review.md`](./docs/benchmarks/v2-part40-review.md).** **Right:** at full-panel scale composition, proportion, placement and palette all match — the neckline, trellis, both flower garlands, the bottom motif and terminal bud land where the original has them, and it reads as the same artwork. **Wrong, ranked by visibility: (1) we stitch bare fabric** — 2,925 stitches, **5.1% of all sewing**, in near-black stops plus **230 RUNNING_SINGLE objects averaging 5 stitches**, laid over the black garment showing BETWEEN elements, which the Part 30 dark-linework pass mistakes for drawn outlines (qualified honestly: black-on-black is near-invisible on THIS garment, so it costs thread, time, trims and object count more than looks — on any other fabric it would be glaring); **(2) the bead-chain border is missing entirely** — the signature dot-chain running along the neckline and trellis is absent, a content loss in the same family as the thin-stroke zero-stitch failures; **(3) trellis bars read as chains** rather than smooth satin, the fragmentation already measured at median 16 stitches per object; **(4) small five-petal flowers fuse into masses** (the one-angle-per-region defect at element scale); **(5) curling tendrils and leaf spines lost**; **(6) rose shading flatter** than the original's gradient. **Consequence for the plan: the top two defects are NOT direction and are cheaper than it** — the graph-stitch prompt now says to do them first. No engine change; review only. |
| 77 | 2026-08-03 | Claude (Opus 5) | 🔬 Investigation | **v2 Part 39 — ran the graph-stitch prompt as a work order; baseline confirmed real, and a NEW engine defect found that invalidates a whole class of fix: [`docs/benchmarks/v2-part39-audit.md`](./docs/benchmarks/v2-part39-audit.md). No engine change shipped.** **(1) The ~50 deg direction error is NOT registration noise** — splitting every compared segment by registration quality gives **50.3 deg for well-registered** stitches vs 47.8 for mis-registered ones, so the target is legitimate (this closes the caveat Part 38 had to leave open). **(2) By SEGMENT count the error lives in satin: 30,332 vs 3,794 tatami (~8:1)**, not the 560/224 object split — which is exactly why Part 38's fill-only change landed inside the noise. **(3) The experiment:** where the source shows a coherent thread direction, refuse satin (whose fragment axes are arbitrary stubs, median 26 stitches) and fill at the measured angle. **Every gate improved — direction 49.9 -> 42.5 mean, within-15 15.8% -> 20.5%, interior 97.30, edge band 94.20 -> 96.20, spill 16.50 -> 16.00, floor/density 0/0.** On the numbers it ships. **(4) Looking at it says otherwise: a refused satin FLOODS its enclosed gaps** — the lattice diamonds fill solid with thread ([`v2-part39-lattice-flood.png`](./docs/benchmarks/v2-part39-lattice-flood.png)) because a satin path follows the axis so gaps never matter, while a fill floods any hole not captured in `hole_contours`. **The metrics actively REWARDED the flooding** (filling gaps raises coverage; the extra thread stays inside the union outline so spill fell) — the same class of failure as scoring a change against a reference the change itself altered, in a new disguise, and only a rendered picture caught it. Salvage attempt (keep satin where axis/width >= 3) preserved the lattice but gave back the gain: 48.6 deg and spill rose to 17.20, failing its gate. Both reverted. **Prompt updated** with the new dead end and the two carried-forward facts, plus the two prerequisites the next attempt needs: fix enclosed-gap capture before any satin->fill routing, and add a bare-fabric gate so the metrics stop rewarding thread laid where the source shows substrate. Locks 10/10 byte-identical; ruff 19. |
| 76 | 2026-08-02 | Claude (Opus 5) | 🔬 Investigation | **v2 Part 38 — ran the Part 33 sketch-verify architecture over the STITCH stage as asked; both verifications reported the stage is already fine, so the only thing shipped is the instrument: [`docs/benchmarks/v2-part37-audit.md`](./docs/benchmarks/v2-part37-audit.md) §5.** **(a) Verify the fill covers its region** — `_verified_fill` stitched, measured swept-thread coverage and re-aimed the needle, adopting a retry only when it measured better: **224 fills, ZERO below the 93% gate, worst first attempt 98.5%.** The check never fires; region coverage is not the defect; removed rather than left as dead weight. **(b) Verify the needle angle against the source** — a photographed sew-out SHOWS the real thread direction, so it can be read instead of guessed: built a structure-tensor orientation field, **validated it on synthetic stripes (worst error 1.4 deg)**, drove the fill angle from it with a coherence fallback. Moved angle agreement **49.6 -> 49.1 deg mean** and within-15-deg 16.7% -> 17.6% with interior/edge/spill/floor/density **identical** — inside the noise, and it reaches only the 224 tatami fills while 560 satin objects take their angle from the medial axis. Not shipped: a stream change that perturbs locks needs a gain visible above noise. **Shipped: `scripts/measure_stitch_direction.py`** — scores stitch directions against a real sew-out photo, with a self-test on known angles and a per-colour-stop registration check, because this session had the instrument wrong three times (bad dict keys, jump-travel counted as over-limit, and a rejection made against a reference the change itself had shrunk 34%). Baseline it reports on the neckline panel: **mean 49.9 deg, median 54.1, within-15 15.8%** — and crucially the error is **even across satin 49.3 / tatami 48.9 / running 52.6**, so this is not one bad stage and the fix must change how direction is decided everywhere (the guided fill), which now has a target to optimise instead of an opinion. Also found: part of that error is us **stitching thread into gaps the source leaves as bare fabric**. Locks 10/10 byte-identical; no engine change. |
| 75 | 2026-08-02 | Claude (Opus 5) | 🔬 Investigation | **v2 Part 37 — why the stitching still trails the source; diagnosed, two fixes tried and MEASURED WORSE, both reverted: [`docs/benchmarks/v2-part37-audit.md`](./docs/benchmarks/v2-part37-audit.md). No engine change shipped.** Root cause found and shown side by side: in embroidery **shape is carried by stitch DIRECTION**, and the source reads as five distinct petals because each petal is stitched along its own axis, while our quantizer (correctly) merges same-thread petals into one region and the fill then runs **one angle across the whole blob** — measured, an 18x18mm flower is ONE object at a single -42 degree angle. **The satin generator itself is sound**: same-side spacing holds 0.450-0.454 median with p90 0.464-0.544 on straight, curved and tight-curve synthetics; it degrades to median 0.547 / p90 0.742 / 28.9% over 0.6mm only on the ragged, fragmented photographic region map (one colour mask = 45,092px in 125 components; 79% of objects under 40 stitches). Long sewn stitches are NOT a defect (0.51% over 5mm). **Attempt A — lobe separation** (watershed cuts at concave necks so each petal gets its own axis) did what it promised (single-angle fill blobs became satin; objects >200 stitches 5 -> 39) but cost **1.1-1.4 points of edge-band coverage** for +0.3 interior, because the watershed ridge notches the outline where it meets it — outline crispness is what makes work look finished, so the trade was refused. **Attempt B — stronger contour smoothing** is worse on both metrics at every setting swept; the shipped 0.10mm/1-iteration values are already optimal. Widening the textured close does not consolidate fragments either (objects rise 1,014 -> 1,143). **The evidence points to a guided/curved fill** — a per-pixel angle field inside one object so rows follow the shape with none of the edge cost of cutting it — which is substantial engine work, honestly scoped as next rather than rushed. Locks 10/10 byte-identical; corpus untouched. |
| 74 | 2026-08-02 | Claude (Opus 5) | ✨ Feature | **v2 Part 36 — a 100-design corpus run that found three real engine defects, plus the image/thread/DST editors competitors ship: [`docs/benchmarks/v2-part36-audit.md`](./docs/benchmarks/v2-part36-audit.md).** Corpus provenance is stated honestly (no outbound image access): **13 real** (3 user photos + 10 fixtures), **40 real-derived** (crops/rotations/rescales of the real photos — real thread texture, synthetic framing), **47 parametric** across hard classes. **Result: 0 crashes; designs producing ZERO stitches 12 -> 9; interior median 97.00 -> 97.15; no real or real-derived design fails** (tier A median 99.05, tier B 95.90, both zero empties). **Three stacked defects behind every empty design, all thin-stroke:** (1) the palette is seeded from an ERODED foreground, which deletes thin strokes entirely — a lattice's ink never became a cluster, so any outline-only colour (linework, stems, text strokes) could vanish; fixed by recovering colour MODES the erosion missed, restricted to locally-uniform stroke cores; (2) k-means returns k centres even for a 2-colour image, so duplicate centres made every ink pixel read as an 'ambiguous blend' and get discarded — fixed by requiring the two nearest centres to differ and merging duplicates **greedily into the dominant centre, never transitively** (union-find chained white->halo->ink and collapsed the image into one cluster); (3) the speck filter used `cv2.contourArea`, which is **zero for a 1px stroke of any length** — 15,641 contours of 'area 0' were deleted; it now counts ink pixels. **Exactly one of ten byte-identity locks moved (fixture 07) and moved the right way** — 15->17 objects, +0.6% stitches, interior identical at 99.40, floor/density 0/0 — regenerated deliberately. **Both measurement instruments were caught being wrong** (bad dict keys, then counting jump/trim travel as over-limit: 285-865 phantom 'violations' on clean designs). A test was corrected rather than weakened: the fleece/cotton density test measured row pitch by y-gaps, valid only for horizontal rows, and **passed by coincidence** on an unstable fill angle; it now measures thread length per unit area. **New editors (real backends + tests):** image prep (crop/straighten/levels/posterize/denoise/background-removal reusing the digitizer's own segmentation, plus measured auto-prep advice), per-user thread chart + saved palettes, and a **stitch-level DST editor** that edits the raw stream (works on imported files with no objects) and restores the 12.7mm reach + single-END invariants after every edit, reporting each repair. pytest **768** + 2 xf (20 new); vitest 127; ruff 19. |
| 73 | 2026-08-02 | Claude (Opus 5) | ✨ Feature | **v2 Part 35 — accounts, admin, plans, email PIN-recovery + a rebuilt dashboard; production verdict stated honestly: [`docs/benchmarks/v2-part35-audit.md`](./docs/benchmarks/v2-part35-audit.md).** **Verdict: supervised LAN = ready (unchanged); public internet = not yet**, with the exact remaining list in the audit (real SMTP account, billing, HTTPS/deploy, multi-process scale). Shipped this part: `email`/`role`/`plan` on local profiles (backward-compatible `.get()` defaults, first profile = operator, last-admin demotion refused); **password reset by email** (`services/mailer.py` -> SMTP when `SMTP_HOST` is set, else a real `.eml` in the gitignored outbox) with single-use 30-min tokens, all sessions revoked on reset, and `POST /forgot-password` always answering 202 so the response never enumerates accounts; `routers/admin.py` (users, plan/role assignment, platform stats) behind a role check; and `services/plans.py` free/pro/studio gating on package ZIP, worksheet PDF, optimizer, converter and large hoops — **opt-in via `STITCHIQ_ENFORCE_PLANS=1`, inert by default so the whole existing corpus is unaffected**. **The old dashboard is deleted** (both components + their CSS layer); the new `components/dash/*` suite adds Overview / Analytics / Account & plan / Admin plus full-page Login / Signup / Forgot / Reset on **hash routing** (chosen because reset emails need URLs that survive a static host), with **real light AND dark themes** and charts built to the dataviz method (series `#2a78d6`/`#3987e5`, **validator run, all checks pass both modes**). SEO/GEO: full meta + OG/Twitter + generated `og.png` + **JSON-LD SoftwareApplication**, `robots.txt`, `sitemap.xml`, `manifest.webmanifest`, **`llms.txt`**, `<noscript>` fallback. **Self-review caught a real defect the tests would not have: enforcement on a Supabase deployment would have read every caller — including paying ones — as free and refused them; `enforcing()` now fails open there, pinned by test.** pytest **748** + 2 xf (9 new); vitest **127** (4 new); tsc + vite build clean; ruff 19. Verified by driving the live app with Playwright, including digitizing the neckline panel and reading its analytics. Honest note: the adversarial review workflow launched for this diff **died on usage credits and returned nothing** — the diff was reviewed by hand instead. |
| 72 | 2026-08-02 | Claude (Fable 5) | 🐛 Fix | **v2 Part 34 — user test (full-length floral neckline PANEL) digitized to a quarter of the design; root-caused to a PARTIAL matte and fixed comparatively: [`docs/benchmarks/v2-part34-audit.md`](./docs/benchmarks/v2-part34-audit.md).** U2-Net's matte covered ink at recall **0.203 — one point above Part 32's 0.2 floor** — keeping an arbitrary diagonal fragment (11.5% of frame vs 40.6% ink). No fixed floor can split 0.203 from fixture 09's *legitimate* 0.407, so the rule is now **comparative: a matte missing over half the ink is overruled only when the flood tier explains ≥95% of it** with a sane foreground fraction. Calibration over every input first: both failures (0.004, 0.203) have flood recall ≈1.0 and reroute; fixture 09 (0.407) keeps its matte because flood explains only 0.656 of its ink (margins ~0.29 on both sides); corpus recalls ≥0.709 never reach the comparison — **tier decisions unchanged, locks green with zero regeneration**. **Result: quarter-panel → full 135×312mm design, 12,900 → 54,519 stitches, 851 objects, 11 threads (+3 gradient shades), Part 33 outline check passes first attempt; DST/PES/EXP all round-trip.** Deliverables: machine files + stitch-out animation + renders. pytest **740** + 2 xf (2 new); ruff 19. |
| 71 | 2026-08-02 | Claude (Fable 5) | ✨ Feature | **v2 Part 33 — sketch → verify → fill, the artist's workflow the user asked for: [`docs/benchmarks/v2-part33-audit.md`](./docs/benchmarks/v2-part33-audit.md).** Before any stitch is planned, the colour plan is drawn as a **rough sketch** (`_sketch_from_labels`: every planned colour boundary + outer silhouette) and **verified against the original's own lines** (`_verify_sketch`: Canny edges within the artwork; coverage = "did the plan draw every line?", precision = "did it invent lines?"). Coverage gates at **0.55**; a failing plan is **re-drawn with two more colours** (up to 2 retries, keep-best), and the user is told via an "Outline check" warning. Quantization wrapped into a nested `_plan(k)` — **stream locks prove the refactor was pure code motion** (all byte-identical). **Calibration set the gate from measurement:** corpus 0.977–1.000 coverage / 0 retries; peacock 0.963; neckline 0.989 — no honest input pays a retry; a checkerboard forced to `max_colors=1` fails at **0.493 and is rescued to 1.000** with both reds recovered and the cap override explained in a warning. Precision logged, not gated (would punish textured inputs for correctly ignoring noise edges). pytest **738** + 2 xf (4 new); ruff 19; locks green; floor/over-limit/density 0/0/0. Visual: [`v2-part33-sketches.png`](./docs/benchmarks/v2-part33-sketches.png). |
| 70 | 2026-08-01 | Claude (Fable 5) | 🐛 Fix | **v2 Part 32 — new user test (dense floral neckline on BLACK) collapsed to 1 object; root-caused to an INVERTED matte and gated: [`docs/benchmarks/v2-part32-audit.md`](./docs/benchmarks/v2-part32-audit.md).** U2-Net decided the image's subject was the empty black neck OPENING (kept 15.6% of frame, ~0% ink) and discarded every flower; `_reclaim_ink` correctly refused the missed embroidery (one 35%-of-frame border-touching component — the exact signature both caps exist to refuse). **Fix: the matte is audited against ink evidence** — trusted only when covering >=20% of strongly-non-substrate pixels, else the classical flood-fill tier takes over. Calibrated on every fixture: corpus recalls 0.407-1.00, the failure 0.004; **the calibration table caught my own first draft (0.5), which would have kicked fixture 09 (legitimately 0.407 — its photographic backdrop counts as ink) off the rembg tier and caused the regression the gate guards against** — pinned in the test docstring. **Result: 1 object -> 330, 89 -> 19,775 stitches, 9 threads with Part 31's gradient split delivering the two-tone petals (magenta+lavender) and two greens; black flower centres survive as knockouts (correct: base fabric on a black garment); interior 97.8, floor 0, density flags 0 on a 330-object design.** pytest **734** + 2 xf; ruff 19; locks green. |
| 69 | 2026-08-01 | Claude (Fable 5) | ✨ Feature | **v2 Part 31 — gradient-band recovery; the peacock's teal comes back: [`docs/benchmarks/v2-part31-audit.md`](./docs/benchmarks/v2-part31-audit.md).** Key reframing: the source is EMBROIDERY, so its 'gradients' are discrete thread bands — recovering dropped colours as bands is faithful, not an approximation. `_split_bimodal_clusters` (textured only): k=2 probe per cluster; modes >=30 BGR apart each owning >=30mm2 are two threads the colour cap merged; top-3 by separation x size, medians re-centred. **Two diagnosed blockers first:** the ambiguous-blend cut (right for AA halos on flat art) was swallowing the whole teal band before the scan could see it — teal only appeared (inside the BROWN cluster, gap 49.8) with the cut off, so the cut is skipped for textured input; and mint|pale measured 32.8 vs a 36 threshold, lowered to 30 with pairs recorded. **Honestly unrecoverable: the deep-navy shadow (gap 35) is merged by mean-shift's own colour radius (52) before anything downstream sees it** — recovering it would re-shatter the fills that radius heals. **Result: 8 -> 10 distinct threads; teal `#4f7064` vs source `#4e7164` (one hex unit); two-tone eye irises; quills as clean separated bars; interior 97.8; floor/density 0/0; '+2 shades recovered' surfaced as a warning.** Corpus untouched, locks green. pytest **733** + 2 xf (2 new); ruff 19. |
| 68 | 2026-08-01 | Claude (Fable 5) | ✨ Feature | **v2 Part 30 — the outline pass, executing the detail comparison's 'buildable now' root cause: [`docs/benchmarks/v2-part30-audit.md`](./docs/benchmarks/v2-part30-audit.md).** The source patch edges nearly everything in dark stem stitch and none of it survived quantization (a 0.5mm line between two fields joins whichever is nearest). Recovered from the IMAGE: black-hat transform (thin-dark-on-lighter only) -> half-width cap (a dark REGION is not drawing; without it the tracer drew a spine down the navy tail — pinned by test) -> the satin medial-axis tracer orders the polylines -> sewn LAST as 1.4mm running stitch in the palette's darkest thread, before lock/merge so every line gets ties. Each line is a RUNNING_SINGLE object whose contour is the PATH (rebuild re-stitches along it). **Peacock: 61 line objects recovered — petal boundaries separate again, quill lines drawn, veins partial; spill 9.6 -> 9.1; floor 0, density 0.** RUNNING_SINGLE becomes the 7th generator-producible stitch type (was 3/21 at the blockers audit). Textured path only; corpus locks green. pytest **731** + 2 xf; ruff 19. |
| 67 | 2026-08-01 | Claude (Fable 5) | 🐛 Fix | **v2 Part 29 — peacock photo iterated to done on the user's 'still not perfect' verdict: [`docs/benchmarks/v2-part29-audit.md`](./docs/benchmarks/v2-part29-audit.md).** Three textured-path mechanisms, all gated on `is_textured` (corpus untouched, stream locks green): **(1) speck absorption** on the label map — 52 of 128 objects were sub-40-stitch colour flecks inside other fills, each a trim + lock + confetti; components <3mm² whose OWNED ring is >=70% one cluster join it, with three calibration lessons pinned as tests (boundary-straddlers keep their vote; the owned-ring rule exists because counting background as dissent left flecks at every leaf edge; the 0.6 -> 0.7 share sweep is recorded — 0.6 cleaned leaves but cost tail cohesion). **(2) Mask solidify** (close 0.4mm / open 0.3mm) healing photographic fringe. **(3) Seam fill** — on flat art, blend pixels are AA halos and deliberately unowned; on a photo they are region BOUNDARIES, and leaving them unowned drew white pinhole seams wherever navy met green; every unowned fg pixel now joins its nearest cluster (`distanceTransformWithLabels`, indexing verified on a toy case first). **Journey: spill 36.8 -> 9.6, composition from branch-missing to complete, floor and density flags 0 throughout.** Honest remainder: tail-edge whiskers are partly faithful feather barbs; the neck's green-to-blue gradient needs A3 photo-stitch; a photo stays a reconstruction — the vector path is still the clean one. pytest **729** + 2 xf (4 new); ruff 19. |
| 66 | 2026-08-01 | Claude (Fable 5) | ✨ Feature | **v2 Part 28 — learned from Ink/Stitch (concepts from their PUBLIC DOCS only; they are GPL, so no code was read or copied) and fixed a defect that survived 27 parts because no metric could see it: [`docs/benchmarks/v2-part28-audit.md`](./docs/benchmarks/v2-part28-audit.md).** Their fill docs name the 'valley effect': neighbouring rows' stitches falling in the same column. Measured on our output: **588 of 588 interior penetrations of a rectangle fill vertically aligned — 100%** — every fill we ever produced punched columns of holes, visible as ridges on the sew-out and a real part of 'the fill is not perfect'. Fixed with a 4-row staggered absolute grid (end-guarded; worst end gap 1.3 steps, SMALLER than the old rounding subdivision's 1.5): **100% -> 0% aligned**, applying to tatami, angled fills and the parallel underlay automatically. **The same defect turned out to be the satin cap's real blocker.** Part 24b abandoned raising SATIN_MAX_W_MM on 1,846 floor violations with mechanism unknown; locating the violating triples showed EVERY one at the column centre — crossings wider than the 6mm step subdivide evenly, so successive crossings' split points landed ~0.15mm apart down the centreline: a perforation line, the fill-valley disease in satin form. With staggered splits (their 'stagger split satin stitches' concept, our implementation): **straight 8mm bar 383 -> 0, ring 769 -> 0, arc 430 -> 0, corpus sweep at cap 6/8: 303/1,846 -> 0/0**. The cap stays 4.5 as a QUALITY preference (corpus coverage prefers current classification: 98.74 vs 98.28 interior at cap 8; satin has no border pass) but is **no longer a safety limit** — and user-forced wide satin via rebuild got the same staggered splits in `_satin_zigzag`, which previously perforated it. **Corpus: interior 98.73 -> 98.74, edge/spill unchanged, floor/over-limit/density 0/0/0, +1.3% stitches — coverage identical while the sewn texture changes fundamentally, which is exactly why the defect was invisible to the coverage metric for 27 parts.** Studied and deliberately not taken: meander/guided/ripple fills (later), short-stitch inset (the wide case that needed it no longer does; corpus shows no concave crowding at 4.5). pytest **725** + 2 xfailed (3 new); ruff 19; stream locks regenerated deliberately — every fill's interior penetrations moved, which is the feature. |
| 65 | 2026-08-01 | Claude (Fable 5) | 🐛 Fix | **v2 Part 27 — user-supplied PHOTO of a real stitched peacock patch exposed two pipeline defects; both fixed by measurement: [`docs/benchmarks/v2-part27-audit.md`](./docs/benchmarks/v2-part27-audit.md).** First run: **the entire left branch with flowers was MISSING** (86 objects) and the tail was navy shreds, spill 36.8%. **(1) The matte rejected a real element on two rules at once** — the branch was one 12,952px component, 5.90% of frame (over the 2% reclaim cap) AND attached to the bird that stands on it (attached = assumed matte fringe). Re-derived both rules from measurement: fixture 09's photographic backgrounds (6.94%/9.19%) **both touch the frame border** while dropped artwork floats free -> border-clear components now reclaim up to 8%; and a matte fringe is 1-2px thick while a real element has body -> attached components >=4px inscribed radius are reclaimable, preserving Part 22's fringe verdict. **Corpus IMPROVED, not just held:** the matte had been silently dropping thick attached elements there too — fixture 04 recovered 559px of real linework (spill 48.1 -> 46.0), fixture 03 edge +1.3, fixture 08 edge +0.6. **(2) Thread sheen shatters k-means into speckle** (341 fragments >=50px). Mean-shift posterization fixes it, gated on measured interior texture — **and the gate was wrong twice before it was right**: whole-foreground stddev ranked fixture 04's thin linework (99.2) as more textured than the actual photo of thread (34.7), because stddev spikes at edges; then measuring at work resolution let the 2x upscale smooth the photo from 7.43 to 5.86, under the 6.0 gate, so the fix silently never ran. Source-resolution interior-only measurement separates cleanly: corpus 0.00-4.10, peacock 7.43. Mean-shift sp14/sr52 chosen by fragment count (341 -> 159). **Peacock final: complete composition (branches, flowers, leaves, crest), cohesive tail with correct eye motifs, spill 36.8 -> 20.1, floor 0, density flags 0.** Honest remainder: photo edges stay fuzzier than flat art; the photo path is now USABLE, not equal to the vector path. pytest **722** + 2 xfailed (7 new); ruff 19; corpus bench edge **+0.19** / spill **-0.22** / floor 0; stream locks regenerated deliberately (fixture 04 changed = the recovered linework). |
| 64 | 2026-08-01 | Claude (Fable 5) | ✨ Feature | **v2 Part 26 — curved fills to competitor parity, and a dashboard that shows every measurement: [`docs/benchmarks/v2-part26-audit.md`](./docs/benchmarks/v2-part26-audit.md).** **(1) Spiral + radial fills** — the last two curved-fill effects the desktop suites market. Selectable per object via rebuild, so the ten-fixture corpus is untouched BY CONSTRUCTION (stream locks confirm). Disc probe: spiral covers 100% interior with **6 jumps vs the straight fill's 77 and 0.8% spill vs 2.1%** — one continuous path, zero interior row-ends. **The density metric caught a real defect before it shipped:** the first radial sent every 4th spoke to r=0, stacking **59 penetrations in one 0.5mm cell** (flag level 14); fixed with a hub keep-out + a short spiral cap (max 7/cell, test pins <=12). Fill family now interchangeable in the properties panel with plain-language labels. **(2) Dashboard analytics** (dataviz method: form -> colour by job -> validated palette -> mark specs -> hover -> render and look): six stat tiles (est. time charges trims at 2.5s and colour changes at 15s), stitch-length histogram from the real stream (series-1 token, no legend, peak-only label, per-bar tooltips), thread usage per stop (bar colour IS the data — the thread hex), Part 25 warnings surfaced, and an honest capabilities-vs-competitors table including our two 'no's (photo, stitch-level editing) and one 'partial' (lettering). Two defects found AT THE SCREENSHOT STAGE and fixed: a clipped peak label and a clipped table. **Scoreboard: A1 fully closed (angle+contour+spiral+radial), A2 closed, blockers 3/5 closed; open: lettering engine, photo digitizing, stitch-level editing.** Live UI exercised end-to-end (digitize fixture 07 through the running app -> dashboard). pytest **715** + 2 xfailed (5 new); vitest **123** (9 new); tsc clean; ruff 19 baseline. |
| 63 | 2026-08-01 | Claude (Fable 5) | ✨ Feature | **v2 Part 25 — executed items 1-5 and 7 of the blocker list, each researched and measured: [`docs/benchmarks/v2-part25-audit.md`](./docs/benchmarks/v2-part25-audit.md).** **(1) Event loop unblocked** — every CPU-bound handler was `async def` running synchronous work ON the loop; all now plain `def` (threadpool). Probing /health during a 12.6s digitize: **0 probes completed before, 37 after**, verified in shipped code. **(2) Silent failures now speak** — `Design.warnings` + frontend banner. Two drafts measured and rejected first (a region count cries wolf at ~1,650 AA specks; a contourArea share reads fixture 04's linework as '92% lost'); shipped element-level loss + a fine-detail scale heuristic (the dominant small-hoop loss channel is colour-merge, invisible to filter accounting) + the colour contract explained. Calibrated: **10/10 fixtures quiet at intended hoops, badge into 70x70/40x40 warns**. **(3) Locks, trims, travel** — no tie-off code existed and 638 jumps exceeded the machine limit untrimmed. Trim-everything was counted and rejected (~30min of machine time); shipped `_route_travel` (in-region jumps become hidden 2mm travel runs, with a **boundary-detour around holes**: a plain donut went **72 trims -> 1**) + `_lock_stream` (lock triangle at every cut; >10mm cross-fabric jumps get tie-off+TRIM), applied to rebuild too. **Three measured failures en route:** the fixed 0.7mm tie put 92 pairs under the floor (satin's 0.4mm pitch); adaptive length alone still failed because a satin end's back-direction points straight along the zig line (grew to 1.6mm, landed 0.05mm from an old hole — fixed by rotating the lock off the line); travel out-and-backs recreated Part 11's turnaround reversal (fixed by re-running `_drop_floor_reversals` post-routing). **Floor final: 0.** **(4+7) UI drift fixed** (underlay select showed BLANK for Part 24's types) **and adjacent same-hex stops merge** — colour changes **19 -> 17**; the 3 remaining avoidable ones are non-adjacent deferred-detail stops kept apart for layering, deliberately. **(5) SVG import** — `_decode_svg` renders the artwork twice (white and black background); identical pixels = opaque artwork, giving an EXACT foreground mask with no neural matte. Colour stops come back as the **exact source hexes**; **white artwork on a white page digitizes** (invisible to every raster heuristic — found the substrate rule deleting it and gated that rule off for vector input, where foreground is declared, not guessed). The Part-22 class of bug cannot occur on this path. svglib+rlPyCairo pinned; .svg accepted in the UI. **Corpus:** interior 98.62 -> **98.72**, edge 97.62 -> **97.91**, floor/over-limit/density **0/0/0**, jumps 1,216 -> **822**, untrimmed-long-jumps 638 -> **0**, unlocked cuts all -> **0**, colour changes 19 -> **17**. Costs stated: +16.4% stitches (travel and locks are real thread) and trims 54 -> 535 — each replacing a previously untrimmed thread trail; cutting them structurally is item 6's sequencing work. Density peak 10 -> 11 (a lock site, by design; flag 14, flagged cells 0). **NOT done: item 6 in full, item 8 lettering engine, item 9 photo digitizing; SVG <text> needs outline conversion. pytest 710 + 2 xfailed (was 698); vitest 114; ruff 19 — identical to baseline.** |
| 62 | 2026-08-01 | Claude (Opus 5) | 📘 Docs | **Measured audit of what actually blocks competitiveness, across input, parameters, performance, usage, features and the stitch stream: [`docs/COMPETITIVE-BLOCKERS.md`](./docs/COMPETITIVE-BLOCKERS.md).** No code changed. The three blockers are not the ones the output makes you look at first. **(1) We cannot read a vector file** — SVG, PDF and EPS all raise `ValueError: Could not decode image`. Every competitor digitizes from outlines; we digitize from a RECONSTRUCTION of outlines recovered by segmenting a bitmap, which is what Parts 4-24 have been improving. Detail saturates at ~640px (fixture 02 identical at 640/1200/2400px) and object recovery scales with input size (fixture 07: 10/15/16/21/23 objects at 200/260/320/640/900px). **(2) The stream has no travel and no locks** — `ConnectMethod` declares TRIM/TRAVEL_RUN/JUMP and only TRIM is ever produced; grep for `tie_off|lock_stitch` returns nothing. Measured: **1,216 jumps and 54 trims over 45,589 stitches, travel is 7.2-27.1% of needle movement, longest single jump 87mm**, and 6 of fixture 07's 20 thread cuts have no lock before them (the 14 that do are an artifact of dense satin ends, not deliberate). No coverage metric sees any of this. **(3) 3 of 21 stitch types are reachable** (SATIN, TATAMI, CONTOUR_FILL); UnderlayType 4 of 6; ConnectMethod 1 of 3. **The highest-value finding is a one-keyword bug:** `app/routers/digitize.py` declares `async def` and then calls the CPU-bound digitizer synchronously inside it, so FastAPI runs it ON the event loop. Measured by probing `/health` every 250ms during a 12.58s digitize: **0 probes completed as shipped, 37 with `async` removed** (diagnostic, reverted). With the runbook's `--workers 1`, one person digitizing freezes the server for every other user for 5-13 seconds — including the health check. My first test of this wrongly returned 'responsive' because the single probe landed during `await file.read()` before the blocking work began; the result stands on a slow fixture with repeated probes. **Three silent failures found:** a 40x40mm hoop takes fixture 07 from **21 objects to 4 with zero warnings raised** (81% of the logo gone — legitimate mechanism, silence is not); `max_colors` is a broken contract (ask 2 get 3, ask 10 or 16 get 4, and object count is non-monotonic — 21 objects at 4 colours vs 10 at 10 colours); and **5 avoidable colour changes** across the corpus where the same thread hex is mounted twice. **One drift I shipped and named:** the UI underlay dropdown still offers only None/Center walk/Edge walk, so an object can now carry the DOUBLE_ZIGZAG or PARALLEL underlay Part 24 added and the properties panel cannot display it. Recommended order: async fix (minutes) -> surface what was dropped (hours) -> tie-offs and long-jump trims (~1 day) -> UI drift (hours) -> **SVG import (~1 week, the root cause)** -> TRAVEL_RUN + branching -> same-colour merging -> lettering engine -> photo digitizing. Items 1-4 are ~2 days and remove three silent failure modes plus the scalability blocker. **Cold start +5.2s; warm digitize 1.6-12.6s; ~5-12 designs/min/worker, serialized.** |
| 61 | 2026-07-31 | Claude (Opus 5) | ✨ Feature | **v2 Part 24b — contour fill shipped; my own recommendation to raise the satin cap was measured and KILLED.** Audit: [`docs/benchmarks/v2-part24b-audit.md`](./docs/benchmarks/v2-part24b-audit.md). **(1) NOT SHIPPED — raising `SATIN_MAX_W_MM`.** Part 24's audit called our 4.5mm cap 'the real gap ... competitors satin to 8-12mm' and recommended raising it. The sweep says otherwise: at caps 4.5 / 6 / 8 / 10 / 12 the interior coverage goes **98.61 / 98.50 / 98.10 / 95.57 / 94.51** and floor violations go **0 / 303 / 1,846 / 5,137 / 6,743** — the 0.30mm penetration floor, at zero corpus-wide since Part 6, breaks immediately. On fixture 07 at cap 8.0 the ring alone puts **526 of 1,794 penetrations under the floor, min 0.15mm**, exactly half the floor. I first blamed curvature (the inner boundary of a curved column is shorter than the outer one that paces it, giving a clean bound W <= 2r(1-k)/(1+k)), then **measured a STRAIGHT 8mm bar and it violates too** — 383 under floor — so the derivation described a real effect that is not the operative one. Abandoned rather than shipped on a plausible mechanism. **The honest conclusion: 4.5mm is not a conservative preference, it is the width at which our medial-axis satin can still hold the floor**; going wider is a Part 7 pacing rework. **(2) SHIPPED — contour fill.** `StitchType.CONTOUR_FILL` had been a declared enum value with no generator since v1. Rows are iso-distance curves of the region's own distance transform, so neighbouring rows sit exactly one pitch apart even where the band bends, and a pinching region splits into components for free instead of needing self-intersection cleanup. Result: interior 98.61 -> **98.62**, floor/over-limit/density flags still **0/0/0**, +2.83% stitches — and the win the coverage metric cannot see, **jumps 144 -> 70 on fixture 03 and 630 -> 461 on fixture 07**, since every jump becomes a trim and a thread tail. **Three mistakes on the way, each caught by measuring not reading:** the first trigger caught SOLID shapes and fixture 07's star came back with **1,708 missed-interior components** (a solid's medial axis is a branching tree, so iso-distance rows crease along every branch); the second keyed on `reason == 'wider_than_satin_cap'`, which **never sees a wide ring** because a 10mm badge border trips the cheap `broad_fill_pregate` first — silently excluding the commonest contour-fill shape in real artwork; and the row pitch had no margin for the fact that contour rows come off a **pixel-quantised** distance field, so fixtures 03/07 scored 98.4/98.0 against the 99.4/98.9 of the straight fills they replaced. One pixel tighter fixes it, and multipliers of **0.9, 0.8 and 0.7 all gave byte-identical output** because they round to the same integer pixel — the tell that the defect was quantisation, not density. Trigger is now `_band_ratio = 2*peak_dist/sqrt(area)` <= **0.30**: the shape survey alone (narrow ring 0.154, letter-O bowl 0.269, square frame 0.303 | disc+pinhole 0.505, star 0.731, disc 1.111) would put the line at 0.45, but the corpus shows the regions at 0.334/0.430/0.439 all LOSE — 0.11 interior for +9.6% stitches. `rebuild_design` gained a CONTOUR_FILL branch so an edit cannot silently convert the rows back to straight. **Cumulative Part 23 -> 24b: fill angles 1 -> 15, underlay types 2 -> 3 of 6, fill behaviours 1 -> 3, interior 98.06 -> 98.62, edge band 97.32 -> 97.62, floor 0, over-limit 0, density flags 0, every fixture improved or held; +10.85% stitches. pytest 698 passed + 2 xfailed (was 678); ruff 19, unchanged from the pre-Part-24 tree.** |
| 60 | 2026-07-31 | Claude (Opus 5) | ✨ Feature | **v2 Part 24 — closed gaps A1 and A2 from the competitive analysis, and found the measuring instrument was wrong before either could be judged.** Full audit: [`docs/benchmarks/v2-part24-audit.md`](./docs/benchmarks/v2-part24-audit.md). **(A1) Per-object fill angle.** `stitch_angle = ... if is_satin else 0.0` sent every tatami fill to 0 degrees; `_scanline_angled` already existed but was reachable only through `rebuild_design`, i.e. only after a user hand-set an angle. Fills now run along the region's principal axis when it is elongated (axis ratio >= 1.15) and ~45 degrees off the dominant boundary direction when it is not — moments rather than `minAreaRect`, because a plus sign, a ring and a star all have a square hull and would get an arbitrary rect angle. **Distinct fill angles across the corpus: 1 -> 15.** **(A2) Underlay selected instead of fixed.** Satin under 2mm keeps its centre run, 2-4mm gets an edge run, 4mm+ a double zigzag whose half-width comes from the distance transform at each axis sample; fills over 100mm2 get the standard edge-run-plus-tatami recipe. **Underlay types generated: 2 of 6 -> 3 of 6.** **THE INSTRUMENT WAS WRONG FIRST, and taking it at face value would have killed the whole result:** A1 appeared to drop fixture 01 from 100.0 to 84.5. Two independent biases, both invisible while every fill was at 0 degrees — `cv2.line(..., thickness)` paints **5.00px perpendicular at 0/90 degrees but only 4.25px at 45** for the same nominal 4px, and a single measurement frame rewards rows that align with the pixel grid. Proof it was luck and not coverage, measured on the **UNCHANGED Part 23 designs** by rotating only the measurement frame: fixture 09 scored **100.0 / 99.4 / 97.6 / 99.9** at 0 / 22.5 / 45 / 11 degrees while the stitches never moved. Replaced with exact swept bands (rotated rect + round caps, sub-pixel via OpenCV `shift`) averaged over four frames; angle spread **7.0 -> 0.0 points**, frame spread **2.6 -> 0.2 points**. Under the sound instrument A1 alone is coverage-NEUTRAL (+0.06), not -1.04. **Result vs the corrected baseline: interior 98.06 -> 98.61, edge band 97.32 -> 97.62, spill +0.03, floor violations 0, over-limit 0, flagged density cells 0, no fixture worse on any measure; cost +7.80% stitches, all underlay.** **Two of my own bugs, both caught by measuring the emitted stream rather than reading the code:** the edge-avoidance was first a doubled-angle vector mean, which cannot represent a bimodal boundary — a diamond's +45/-45 edges cancel, so it returned exactly 45.0 for the one shape whose direction most needed avoiding; and `_zigzag_underlay` first derived its jump flag from point spacing like every other generator, but a zigzag's consecutive points are the two sides of the column, so **every throw came back a JUMP and the underlay stitched nothing**. **Reported not hidden:** DOUBLE_ZIGZAG never fires on the corpus — the widest satin column is 3.78mm against `SATIN_MAX_W_MM = 4.5`, leaving a 0.5mm band — which exposes the real gap that **we cap satin at 4.5mm where competitors satin to 8-12mm**; CONTOUR underlay and contour/radial fills are still absent, and the badge's outer ring proves they matter; the preview now shows the underlay lattice because the renderer draws every stitch equally. **pytest 692 passed + 2 xfailed (was 678), 14 new tests including a suite that pins the metric's angle-fairness and asserts the OLD rasteriser still fails; ruff 19 — identical to the pre-change tree, diffed finding by finding.** |
| 59 | 2026-07-31 | Claude (Opus 5) | 📘 Docs | **v2 Part 23 — competitive gap analysis, measured rather than assumed: [`docs/COMPETITIVE-GAP-ANALYSIS.md`](./docs/COMPETITIVE-GAP-ANALYSIS.md).** Answers *"why does our stitching still look different from Wilcom/Hatch/Embird output?"* — a different question from the feature matrix in COMPETITOR-COMPARISON.md, and the one the rendered output actually raises. Every claim about our own side is a grep, a measurement or a live run, not an impression. **Four gaps produce the visible difference. (A1) Every tatami fill is emitted at 0°** — `stitch_angle=round(rect[2],1) if is_satin else 0.0` (digitizer.py:883); measured on the new coffee emblem, all 5 fills at `0.0°` while satins vary correctly (`{-90:8, -45:1, -68:1}`). Competitors set angle per object from the shape's own geometry and additionally offer contour/radial/spiral/wave fills. One global angle means every shape reflects light identically — the flat, printed look. **(A2) Only 2 of 6 declared underlay types are ever produced**: `UnderlayType` declares NONE/CENTER_WALK/EDGE_WALK/DOUBLE_ZIGZAG/PARALLEL/CONTOUR but only `CENTER_WALK` (satin, digitizer.py:784) and `EDGE_WALK` (fills, :820) are assigned — measured `{CENTER_WALK: 10, EDGE_WALK: 5}`; the other three are enum values with no generator, so satin never sits up off the fabric. **(A3) No photographic/gradient digitizing** — grep for `photostitch|dither|halftone|stipple|floyd` across `app/services/` returns nothing, vs PhotoStitch (Wilcom) / PhotoFlash (Hatch) / Sfumato (Embird). **(A4) No digitized font library** — lettering rasterises a system TrueType through the image digitizer, discarding the outline the satin should follow, vs 62–250 hand-digitized fonts with stitch-aware kerning and 9 baselines. Also documents **seven areas where we are competitive or ahead** (machine-safety measurement — 0.30mm floor, 12.7mm limit, 0 violations corpus-wide, which no competitor publishes an equivalent of; satin quality; fill edge finish; 9 export formats; determinism), **seven second-order gaps** (corner rounding, sub-3mm blobs, per-object fabric values, same-colour stop duplication, branching/travel, appliqué–foam–trapunto, stitch-level editing), and **four suspicions the measurements disprove** so they stop being chased — coverage, safety, colour separation and the renderer are NOT the cause. **Recommended order by visible-improvement-per-unit-of-risk: A1 → A2 → C1+C2 → A4 → A3**, under the standing guardrail that the 10-fixture corpus stays byte-identical or better with floor violations at 0. No code changed in this part. |
| 58 | 2026-07-31 | Claude (Opus 5) | 🐛 Fix | **v2 Part 22 — tested with NEW artwork the pipeline had never seen, and it exposed a feature-losing bug that 21 parts of corpus tuning could not: U2-Net silently deleted the wordmark.** Two original logos were generated (a coffee emblem with overlapping shapes and a curved banner; a geometric mark with a wordmark beneath) and run through the LIVE stack exactly as a user would — signup, digitize, quality, export. **The geometric mark came back with its company name simply gone**: 2 objects instead of 8, and the design bbox proved it had been excluded before digitizing ever started. Root cause measured, not guessed: `segmentation.foreground_mask` returns U2-Net's matte verbatim, and U2-Net keeps the photographic SUBJECT while discarding what it reads as surrounding decoration — **0 foreground pixels in the wordmark band** vs 100% inside the mark. Icon-above-name is the most common logo layout there is, so this hit a large class of real customer artwork. **Fix — `_reclaim_ink`:** components the matte dropped are added back when they are unmistakably ink (BGR distance ≥60 from substrate) AND between 0.02% and 2% of frame AND **detached** from what the matte kept. Each bound is measured, and the first two attempts are recorded because both regressed the corpus: reclaiming with no area cap took fixture 09 from **1,747 to 9,319 stitches** (it swallowed the photographic background — candidates measured at 9.19%/6.94%/11.21% of frame vs the wordmark's 0.29%/0.14%/0.12%, a 20x separation); reclaiming ATTACHED components then cost fixture 05 **-0.5 interior / +2.2 spill** for no recovered content, because a touching component is the matte's own 1px-tight edge, not a missed element. With all three bounds the corpus is **byte-identical to v2-part17 on all ten fixtures, floor 0, over-limit 0** — the wordmark is recovered at zero cost to 21 parts of tuning. **End-to-end on the new art:** signup → digitize (6.5s / 2.4s) → quality **100/A, hoop fit true** → exports DST/PES/JEF/EXP/VP3 all 200 → package ZIP with all 7 artifacts including quality.json. **7 regression tests** pin reclaim, no-removal, background exclusion, attached-fringe exclusion and speckle rejection. **Known remaining, reported not hidden:** sub-3mm type ('SINCE 2019' at ~2.7mm) leaves blobs rather than dropping cleanly; small inner shapes lose sharp corners to contour smoothing; a deferred same-colour detail pass produces two stops of one hex (a real second pass, but it reads oddly on the thread card). **pytest 677 WITH rembg / 673 + 4 skipped WITHOUT; vitest 114; ruff 18.** |
| 57 | 2026-07-31 | Claude (Opus 5) | 📘 Docs | **v2 Part 21b — closed the two verification gaps my own fallback check-in named, and corrected the runbook my security fixes had invalidated.** **(1) The one gate not re-run after the Part 21 security work:** the full 10-fixture quality bench. Re-run as `v2-part21` and diffed against the `v2-part17` baseline — **byte-identical on all ten fixtures, floor violations 0, over-limit 0**, confirming the auth/middleware changes never touched stitch generation (the stream lock had only covered 4 locked fixtures). **(2) LAUNCH.md documented the vulnerability as current behaviour.** Its §4 'THE keyless caveat' told the operator that every user shares one library and 'nothing is private', and instructed them to ANNOUNCE that to users at launch — true when written, false after Part 21, and the single most misleading thing an operator could read on launch morning. Replaced with the verified post-fix behaviour table (Bob→`[]`, Bob-by-id→404, anonymous list/delete→401, Alice intact) plus the two real operational consequences: **users must create a profile before saving**, and **sessions expire after 12h**. Also corrected the WARN gloss ('shared design bucket' → per-user isolated), the worker-count note (rate-limit budget is now keyed per session token, not per IP — the old text still described the 12-req/s-for-everyone bug), and removed the header pointing at 'sections still marked as placeholders'. Every markdown link in README and LAUNCH re-verified to resolve; FAQ/USER-GUIDE/README carry no stale security claims (grepped). Security isolation + preflight suites green (17). |
| 56 | 2026-07-31 | Claude (Opus 5) | 🔒 Security | **v2 Part 21 — the review layer finally ran, returned NO_GO, and was right. Run as its own 6-agent workflow (5 adversarial critics + 1 reviewer) after three swarm attempts starved it of budget; every critic verified by EXECUTION against live servers, and the reviewer independently reproduced their two worst findings and corrected three severity ratings.** **BLOCKER 1 — authorization was decorative.** `deps.current_user()` returned the constant `LOCAL_USER='local-dev'` for every caller whenever Supabase was unconfigured, which IS the launch topology, so all ~100 users shared one namespace and `user_store.resolve_bearer` — the function whose own docstring called itself the HTTP auth hook — was dead code. Critics proved it end-to-end: Bob listed and read Alice's design with his own token; an **anonymous** caller listed it and **DELETE'd it (204)**; Alice's re-read 404'd. The frontend was already sending the token, so the UI presented a login the backend discarded. **Fixed:** offline mode now resolves the Bearer token to a real local user id and 401s without one, falling back to the sentinel only when NO profile exists yet (fresh install) or `STITCHIQ_OPEN_ACCESS=1`. Re-ran the exact attack: Bob gets `[]`, anonymous gets **401** on both list and delete, Alice's design survives, and designs are stored under her real user id. **BLOCKER 2 — the whole LAN throttled to 12 req/s.** The limiter keyed on the raw TCP peer, but the documented topology proxies through Vite, so all users arrived as 127.0.0.1 and shared ONE 120-req/10s bucket. **Fixed:** keys on the session token when present, else the leftmost X-Forwarded-For **only from a trusted loopback proxy** (a forwarded header from any other peer is ignored, or anyone could mint a fresh bucket per request). **MAJOR — live credentials in git.** `apps/backend/data/local_users.json` was tracked, containing a PBKDF2 hash and two session tokens; a token read from git HEAD still authenticated **22 hours later**. **Fixed:** untracked, `apps/backend/data/` gitignored, and a test asserts nothing under it is ever tracked again. **MAJOR — tokens never expired** (`issued_at` was written but only used as an LRU sort key). **Fixed:** `SESSION_TTL_HOURS = 12`, checked in `verify_token`, expired sessions dropped on read; an unparseable stamp counts as expired. **Root cause of why 662 green tests missed all of it: no test asserted cross-user isolation.** Added `tests/test_swarm_security_isolation.py` (8 tests): cross-user read, anonymous refusal, bogus token, revoked token on **design** routes (logout previously only revoked /auth), TTL expiry, per-user limiter keying, forwarded-header spoofing, and git-tracking of the credential store. Also added `tests/conftest.py` — an autouse fixture giving every test an empty user store, because the suite had been reading the operator's REAL credential DB (that pollution caused 25 spurious 401 failures the moment enforcement went on). **Deferred with reasons, per the reviewer's severity corrections:** PIN lockout and username enumeration are real but were downgraded — they grant nothing an anonymous caller did not already have until authorization exists, and now that it does they are the next queue; the CORS-ordering defect (429/413 carry no `access-control-*`) never fires under the verified same-origin proxy topology. **Gates: pytest 670 WITH rembg (was 662) and 666 + 4 skipped WITHOUT; vitest 114; typecheck + build clean; ruff 18; digitizer stream lock passes and output stays byte-identical to v2-part17.** |
| 55 | 2026-07-30 | Claude (Opus 5) | 🚀 Launch | **v2 Part 19 — swarm integration + launch verification. The 116-agent swarm reached 40/106 agents across two runs before the account session limit stopped it both times; the 5 critics and final reviewer never ran, so I performed the reviewer role myself.** **Landed and gate-verified:** request-logging middleware (X-Request-ID validate/echo, no body/query logging), global error handlers (clean 500 with requestId, 422 without payload echo), 25MB body-size limit (fast-path + streaming, verified 413), local user profiles (PBKDF2-SHA256, session tokens, `/api/auth/local/*` wired and **verified live** — create+login return real tokens), local design store (path-traversal-safe, per-user), toast store + Toasts UI, export round-trip tests over all 9 advertised formats + header/metadata tests, lettering font/min-size work, QA fuzz suites, preflight.py (10 checks, all PASS), USER-GUIDE.md. **REVERTED on evidence:** the perf team's digitizer optimization (bbox-cropped `_uncovered_mask` + hires crop). It claimed bit-identity; **its own stream-lock test — which that same team wrote — failed on fixtures 04 and 07**, and with no critic layer to adjudicate I reverted rather than ship an unverified change to the core after 17 fidelity parts. Post-revert the bench is **byte-identical to Part 17 on every fixture**; floor violations 0, over-limit 0, density flagged 0. The lock test is the swarm's most valuable single artifact: it caught its own team. **Measured launch capacity (real, not estimated):** 10 concurrent digitizes → all 200, serialized ~1.1s each, 11.1s for the 10th, server healthy; single digitize 3.1s (fixture 01) to ~13s (fixture 07). Adversarial: corrupt PNG → **415**, missing file → **422**, no 500s, no hangs. **docs/LAUNCH.md** written from these numbers (worker sizing = cores−1, build+preview config, data/backup layout, troubleshooting table, honest limits). **Gates: pytest 377 passed + 2 xfailed WITH rembg; 373 passed + 4 skipped + 2 xfailed WITHOUT; vitest 72; typecheck + build clean; ruff 31→18 (13 auto-fixed; remainder = 14 documented digitizer baseline + 4 FastAPI B008/style); secrets clean (.env.example DATABASE_URL is a template placeholder).** **NOT landed (session limit):** rate limiting, design-library UI wiring, arc-baseline lettering UI, FAQ.md, the entire critic + reviewer layer. Verdict: **GO for a supervised LAN launch**, NO-GO for public internet exposure (no rate limiting). |
| 54 | 2026-07-29 | Claude (Fable 5) | 🐝 Swarm | **v2 Part 18 — the 116-agent launch swarm, partial by session limit: 15 of 106 agents completed (all 10 team leads' plans + the first worker of five teams) before the account's session limit refused the remaining 91 (workers 2-9 of every team, all 5 critics, the final reviewer).** What landed, individually verified and gate-checked: **API team** — pure-ASGI request-logging middleware with X-Request-ID validation/echo (path-only logging, no query/body), wired after CORS in main.py; **auth team** — thread-safe JSON UserStore (atomic writes, PBKDF2-SHA256 PIN hashing 100k iterations, case-insensitive dup check) as a library, router NOT yet wired; **persist team** — local_store.py design store (per-user dirs, atomic writes, path-traversal-safe ids, stats matching supabase shape, 16 tests), router NOT yet wired; **UX team** — zustand toast store with dedupe/cap/TTL (7 tests), UI NOT yet wired; **export team** — live-parametrized round-trip tests over every advertised format (all 9 readable, zero skips); **perf team** — byte-identical stitch-stream lock (sha256 per fixture vs committed hashes) + v2-swarm baseline, scoped WITH-rembg (the two segmentation paths differ by design). **Gates after integration: pytest 230 WITH rembg / 226+4-skipped WITHOUT; vitest 71 (+7); typecheck/build clean; swarm backend files ruff-clean; secrets clean.** One integration fix by the orchestrator: the stream lock ran (and failed) on the no-rembg path whose baselines it never captured — now skipif-scoped with the reason documented. **Not landed (blocked by the limit): rate limiting, upload validation, auth/persistence ROUTES and UI wiring, lettering v1, QA fuzzing, LAUNCH.md/preflight, user docs, all criticism/review layers.** The workflow is resumable (cached 15 replay free). Also: a stale replay of the Part 10 brief arrived in-queue; verified already complete at c68497d (comment intact at test line 251) — no action. |
| 53 | 2026-07-29 | Claude (Fable 5) | ⚡ Perf+Feature | **v2 Part 17 — “still not granular”: correct, and fixed. Contours/borders/fills were traced on a 0.18mm/px staircase; global fine-resolution work was enabled after making it affordable.** **Profile first:** `_zhang_suen_thin` ran whole-canvas passes — 44 of 62s at 2x on the heaviest fixture. Cropping to the active bbox (pasted back) → **61.9s→18.4s**; windowing the deferral ring scan (4.7s of full-canvas dilates) → corpus **39.5s total, worst 13.4s**. Then `_MIN_WORK_PX=1200` (cubic, capped 2x, sources ≥400px). **What granularity exposed, fixed with measured values:** phantom 0.15mm blend-slivers (palette erosion now scales with upscale factor + `MIN_FEATURE_W_MM=0.25` sub-thread gate — **the first value, 0.35, silently deleted ALL of fixture 04**, whose real hairlines measure 0.30-0.33mm at fine resolution, not the coarse grid's 0.5; both populations recorded at the constant); a 160px lettering render at 7.5x produced nothing (cap 2x, floor 400px); resolution-dependent pins moved and were updated with provenance (04 zigzag objects 11→10, 08 max/cell 7→9-10). **The audit's own LINT-VERIFY caught a miscount during this part (claimed 14, actual 15 — one unused import) — the Part 12 check working as designed; fixed before commit.** **Result:** floor 0, over-limit 0, density 0, every 100/100 intact; **stitch counts DOWN 5-15%** (finer geometry overshoots less); band 03 +1.3 / 05 +1.1 / 08 +2.1; spill 04 −4.4 / 06 −1.1; **07's ring text partially legible, HARBOR CLUB crisper, rings tighter.** pytest **171 WITH and 171 WITHOUT rembg**; secrets clean; 2 strips committed. |
| 52 | 2026-07-29 | Claude (Fable 5) | ✨ Feature | **v2 Part 16 — fidelity loop round three: the user's named problems (text invisible, shapes imperfect, colours off, white gaps) each got a mechanism found and fixed. 07's HARBOR CLUB: invisible (P14) → outlined (P15) → fully legible navy satin (now); 08's face carries nose/smile/whiskers/dots/pupils on a solid head.** **Text:** a 640px source puts a 4mm letter at 2-4px strokes — un-columnable. A global work-res raise was tried and REJECTED on measurement (43.8s/fixture); shipped `_skeleton_satin_hires`: only regions with stroke < 8px are thinned/columned at up to 3x, points scaled back (corpus 47s, worst 13s). **The upscale exposed a latent Part-2-era bug: the vectorised Zhang-Suen thinner deletes BOTH sides of an even-width ridge simultaneously — a 2x-upscaled bar collapsed to ONE skeleton pixel (f=1→139px, f=2→1px, f=3→447px, all interpolations). Fixed with the checkerboard split**; skeletons change corpus-wide by design, two pinned tests updated with provenance. **White gaps:** 08's painted miss-map showed red wedges around every small dark detail — darkest-first stitches them FIRST, forcing the fill to keep uncrossable knockouts; Part 14's burial guard was right, the SEQUENCE was wrong. Components < 60mm² whose ring is ≥60% later-stitched are **deferred to a detail pass after the main clusters** — fill sews solid, detail lands on top. A first version opened a stop per component (19 stops on 08, caught on render) and was grouped: 08 ships 5 stops, 07 ships 4. Burial guard still covers non-deferrable large details; both directions pinned. **Colour:** cluster representatives are per-channel MEDIANS of members, not k-means centroids that average the anti-alias blend tail. **Held:** floor 0, over-limit 0, density 0, every Part 15 100/100 intact; 07 spill 6.2→5.4, 08 spill 4.0→3.2. **Reported give-backs:** 05 −2.0 interior/−1.7 band and 06 spill +4.1 (hires re-columned already-healthy strokes — next loop's first target); colour changes +1 on 02/07/10 (the detail pass); classification changed on 02/07 BY DESIGN. **pytest 171 WITH and 171 WITHOUT rembg; ruff 14 all pre-existing (LINT-VERIFY machine-checked); secrets clean; 3 strips committed.** |
| 51 | 2026-07-29 | Claude (Fable 5) | ✨ Feature | **v2 Part 15 — fidelity loop round two: edge truth and the pro finish. Interior AND edge band hit 100.0/100.0 on every flat-colour fixture (01/02/09/10) — the first 100s in project history — with floor violations 0 under a WIDENED metric and classification identical.** **Fix 1, the registration crescent:** adjacent fill rows connect with a stitch of exactly one row pitch (0.45mm cotton) — UNDER `MIN_STITCH_MM=0.5` — so `_coalesce_short` deleted every row's first point; straight edges hid it behind a hugging diagonal, fixture 02's sun arc exposed it as a crescent (found by painting emitted rows over the region). Fills now keep stitches down to 0.95× their own pitch (`FILL_ROW_CONNECT_KEEP`), the industry-standard connection every density guide prescribes. **Stated up front: `stitches_under_0_5mm` 9 → 1,201 — the legitimate pitch-length connections that were previously being DELETED, which was precisely the defect**; the quality report's tiny-stitch line moved 0.5→0.3mm to match (same grounding as the floor). **Fix 2, satin borders on fills** (`_fill_border`) — the finish separating rows-of-thread from embroidery, most of the perceived gap to the competitor tools. Three lessons applied: `_satin_border` carried the **Part 4 alternating-lead bug** — fixed to strict A0 B0 A1 B1; **the floor enforced at GENERATION** — the first version emitted **830 sub-floor pairs on fixture 07's rings alone** (0.057mm, staircase normal swing), a station now waits until BOTH sides advance a full floor (the Part 5 rule) because downstream repair could never fix 830 without shredding the border; and **the safety metric widened to ALL objects** (borders zigzag inside tatami objects), zigzag test still self-selecting so historical numbers stay comparable. Borders run outer contour AND kept holes (02's sun rim), centered, area-gated (30mm²), 1.2mm width; the coalesce backstop is now unconditional. **The trade, plainly:** stitches +30–125% on bordered fixtures (real thread; 13k for a 95mm badge is commercial range), spill +2–4pp BY DESIGN (half the border lies outside the traced contour, covering the artwork edge), jumps +3–12. **07: interior 97.8→99.1, band 95.3→98.1, HARBOR CLUB legible for the first time**; fixture 10 now unmistakably its input. Honest remainder: 07's small ring text and 08's fill texture — the small-lettering ceiling, same top gap as the competitor comparison. **Also:** the donut-hole test's stitch-count proxy died (a ring earns two borders); rewritten to assert the property itself (zero penetrations deep inside the hole, MIN-radius bound — the first rewrite used max-radius and failed against the border's own legal inner edge, recorded so nobody re-derives it). **pytest 170 WITH and 170 WITHOUT rembg; vitest 64; typecheck clean; ruff 14 all pre-existing (LINT-VERIFY machine-checked); secrets clean; 3 constants added, all grounded; 5 evidence strips committed.** |
| 50 | 2026-07-29 | Claude (Fable 5) | 🐛 Fix | **v2 Part 14 — the fidelity loop: why the stitch result doesn't look like the input, iterated one verified fix at a time.** **Iteration 0 cleared two suspects by measurement before touching code**: k-means centers correct, ambiguous pixels 0.3%, the green segmentation mask painted pixel-perfect — the defects were all downstream of everything Parts 0–13 hardened. **Fix 1, the big one: the renderer has been LYING SINCE v1.** The stitch stream around fixture 02's sun was proven complete (dump shows segments ending exactly at the circle edge), but `render_preview`'s `flush(); run=[]` on every JUMP discarded the jump's landing coordinate — the FIRST penetration of the next segment — so every post-jump segment lost up to 6.4mm and 2-point segments were never drawn. One line fixes every render since v1; **fixture 10 is transformed in the grid with a byte-identical stitch stream** — the machine file was always better than the preview claimed, and every render-based visual judgement in Parts 0–13 understated coverage around every hole (the committed metric rasterizes the stream itself and was never affected). **Fix 2: fills sewed straight across small knockout holes** — hops under CONNECT_MM (3mm) are plain stitches, so every letter collected one surface crossing per fill row (NORTHFIELD read as mush). Standard practice applied: holes under `HOLE_KNOCKOUT_MIN_MM2 = 50mm²` are absorbed and the detail stitches on top; feature-scale holes (02's 26mm sun) keep the knockout. **Fix 2b, the regression the loop CAUGHT: the unguarded version buried fixture 07's HARBOR CLUB** (navy letters stitch before the cream disc, darkest-first). `_hole_covered_later` absorbs only when the hole's owning cluster stitches LATER, or the hole is a ≥30%-ring fringe of a continuing earlier shape (edge overlap ≠ burial); verified per-hole on 07 (all nine letter holes kept, `dominant rank 0 < fill rank 3`), both directions pinned as tests. **Fix 3: the 3×3 opening erased whole small-type lines** (02's EST. 1974 · SUPPLY CO. died while NORTHFIELD kept the mask coarse enough to pass the guard); `_open_preserving_detail` restores components the opening erased OUTRIGHT above dust size (`SPECK_KEEP_MM2 = 0.15`) — the 2.6mm line now drops cleanly at the area filter (below embroiderable size) instead of leaving random dashes. **Also recorded as cleared, so the next loop doesn't re-chase them**: low-zoom fill banding is resize moiré + honest thread texture (star geometry painted row-complete); 07's ring text mush is the known small-lettering ceiling; 02's ~1mm sun registration crescent is named as the next target, mechanism unpainted. **Corpus:** only 02's stream changes (3,696→3,406 st, jumps 180→116, spill 3.7→1.6; band −3.1 because the graded SHAPE changed — contours no longer carry letter holes); all other fixtures byte-identical; corpus jumps 1,692→1,628; **floor 0→0, classification identical, over-limit 0, sub-0.5 9**. **pytest 170 WITH and 170 WITHOUT rembg (+5 fidelity regression tests); ruff 14 all pre-existing (LINT-VERIFY machine-checked); secrets clean; 4 new constants, all grounded; evidence strips committed (input | part13 | part14 for 02/07/10).** |
| 49 | 2026-07-29 | Claude (Fable 5) | ✨ Feature | **v2 Part 13 — competitor-driven product round, executed from the LAUNCH-READINESS plan with live-web competitor research (docs/COMPETITOR-COMPARISON.md, cited).** **Fabric profiles (12 fabrics)**: `FABRIC_PROFILES` replaces the scalar pull table — pull + fill/satin density + underlay step + edge inset per fabric; **cotton == the old globals so the all-cotton corpus is byte-neutral by construction, confirmed by bench diff**; per-object density seeding makes `rebuild_design` fabric-aware with zero changes; fleece on fixture 07 → **4,611 stitches vs 7,825 cotton**, visible in the UI. Terry set DENSER not sparser — the sources conflict and the choice + tiebreaker (protocol sew-out) are recorded at the constant. **Jump reduction: corpus 2,042 → 1,692 (−17.1%)** — 08 −41% (387→229), 07 −18% (979→807) — via `_fill_by_component` (scattered wide-mask fills per fragment, was 435 of 07's jumps) and `_order_branches` (nearest-endpoint chaining replacing SET-order emission, now deterministic). **The reorder manufactured a fresh floor violation (07 Satin 5, 0.277mm); instrumentation showed every generator returning clean — `_coalesce_short` was creating it downstream.** Fixed architecturally: `_drop_floor_reversals` now also runs as a **backstop after the last sequence-changing transform**, so no upstream reshuffle can leak a violation again. **Violations 0→0, classification identical, sub-0.5 9→9, over-limit 0.** Honest cost: 08 −0.3 interior/−0.5 band (sequence-dependent coalescing, explicitly unpainted), 05/06 +0.1; ring hole-hop (~396 jumps) named as the dominant remaining producer. **Quality in the product**: `analyze_quality` + hoop fit (blocking, −25) + max/mean stitch + jumps/1,000; **auto-run after every digitize/lettering, rendered in a new studio QualityPanel, and written as quality.json into the package ZIP**. **Research-forced correction**: the “≤5 jumps/1,000 industry target” the Part 12 brief cited **does not exist** (dedicated search; guidance is trim cost ~3-7s) — the first implementation cited it because the orchestrator told it to; message now states trim cost and the test pins that no industry target is claimed. **Export honesty**: `/api/formats` derives from pyembroidery's writer table — the falsely-advertised `vip` is gone, HUS documented import-only, JEF/EXP/VP3/XXX/PEC round-trip tested. **Lettering**: `letter_spacing_mm` (UI control) + `font_path`; default path pinned byte-identical. **Localhost verified, not instructed**: `npm run dev` actually run and driven by a scripted browser — fleece digitize of fixture 07, panel auto-scores 99/A — screenshot committed (v2-part13-ui-quality-panel.png); README gained a 60-second walkthrough. **Competitor position** (live web, July 2026): cohort = EmbroidAI/SewFlow/StitchPilot/StitchFast; after this part STITCHIQ matches or beats the cohort on fabric profiles, quality reporting, export honesty and pathing, and is **alone in measuring machine-safety**; widest gaps to paid products: lettering depth and underlay TYPE (double-zigzag/knockdown), both cited and deferred deliberately. **Standards**: pytest **165 WITH and 165 WITHOUT rembg** (+24); vitest **64** (+7); typecheck/build clean; digitizer.py **96%**, optimizer 92%, lettering 87%; new functions ≤31 lines; **LINT-VERIFY: 15 over sixteen touched files, machine-verified** (both audits pass the CI check); secrets clean. |
| 48 | 2026-07-29 | Claude (Fable 5) | 🛠 Fix | **v2 Part 12 — Part 11's five open questions closed; launch-readiness measured. Every corpus stitch stream byte-identical to Part 11; floor violations 0→0; the only bench change is the new additive `density` field.** **A1, the discrimination claim is now evidence:** `scripts/discriminator_search.py` shows **100% of violating reversals (590/590 sampled + both real fixture-07 cases) are satin-realizable AS THE SAME COORDINATES**, so no function of the triple can separate an individual case — constructive, not argued; the distributional caveat (leg-ratio 0.908 balanced accuracy on the adversarial space, collapsing on the equal-leg family real decimation emits) is measured and stated, with the balanced-accuracy correction called out. **A2, side preference:** fixed return-drop creates the LONGER merged stitch in **49.5%** of 3,552 asymmetric turnarounds — neither fixed policy wins, so **adaptive shipped** (smaller merged span, ties keep return → corpus byte-identical, confirmed by bench diff not by design intent); losing case pinned. **A3:** `_edge_walk` **wired** into the floor repair and proven by an adversarial erosion-spike sweep (raw violations at 2 of 21 spike lengths, wired zero); `_center_walk` **proven structurally unable to violate** (monotone step + isometry ⇒ pairs ≥2·step ≈ 4mm vs 0.30mm floor) and left unwired with a property test instead of dead code. **Building that case surfaced a structural blind spot: a contour-seam same-hole pair is invisible to ANY consecutive-triple metric** — first/last of the loop, adjacent in space, far apart in stream. **A5, the successor metric, catches exactly that class:** `density_metrics` counts penetrations per 0.5mm cell, order-independent; corpus max **7** (fixture 08; 6 without rembg), flag at **14 = 2× healthy max**, provisional and tied to the protocol; proven to fire on a pile-up and on the seam pair, 0 flagged corpus-wide. **A4, honestly:** no fabric was ever stitched in this project and none was pretended — `docs/FABRIC_TEST_PROTOCOL.md` committed (4 fabrics, spacing/length ladders, 2×/3×/4× pile-up patch, per-constant acceptance rules); the industry-0.5mm question is **reconciled**: that rule is stitch LENGTH = `MIN_STITCH_MM = 0.5` exactly, while 0.30mm bounds same-side spacing — the pipeline enforces the guidance PLUS an extra check, now documented at the constant. **A6:** audits now embed `LINT-VERIFY: findings=N files=…` and `scripts/verify_lint_claim.py` re-runs ruff in CI, failing on mismatch — the Parts 7–10 miscount class becomes a build failure; this part's marker (**15 over eight files**, 14 digitizer + 1 bench, both pre-existing) machine-verified before commit; research found CI previously ran NO lint at all. **Part B, seven parallel investigations with file:line evidence (docs/LAUNCH-READINESS-GAPS.md): the brief was wrong three times** — pull compensation EXISTS and is fabric-aware (`PULL_BY_FABRIC`, 12 fabrics, tested), machine export EXISTS (pyembroidery DST/PES round-trip tested, package ZIP), a composite quality score EXISTS (`/api/optimize/quality`, 0-100). Partial: fabric profiles (pull only — density/underlay globals), quality report (not persisted/auto-run), lettering (rasterize-then-digitize, no kerning, `text_mode` is DEAD code), pathing (**964/979 of fixture 07's jumps are WITHIN objects** — 831 from one scanline rule — untouched by the opt-in object reorder). Absent: real-world corpus (documented licensing reason), physical machine testing. Bugs surfaced and left as plan items per scope: `/api/formats` advertises `vip` which pyembroidery cannot write; JEF/EXP/VP3/XXX untested. **Decision points flagged, not assumed**: lettering scope, untested formats, fabric underlay type, auto path-optimize. **Standards:** digitizer 95%, measure 95%, discriminator 98%, verify_lint 93%; `_drop_floor_reversals` 51 lines (one over, reported); 8 introduced lint findings fixed pre-commit; secrets clean; 3 constants added, all commented with grounding. **pytest 141 WITH rembg and 141 WITHOUT (18 added); vitest 57.** |
| 47 | 2026-07-29 | Claude (Opus 5) | 🛠 Fix | **v2 Part 11 — the underlay double-back, closed. Penetration-floor violations 2 → 0**, zero across the ten-fixture corpus **and all three probes** — not true at any prior point in this project — and reached **without changing the metric**: its only edit swaps a literal `0.9` for an import of the same `0.9`, so the count is directly comparable to every figure since Part 5 (3,235 → 3 → 3 → 5 → 3 → 2 → **0**). **Housekeeping done properly this time:** Part 10 removed every USE of `_MITRED_POINTS` and `_mitre_key` and reported them deleted, but the definitions were still in the file; both are now gone, **confirmed by pasting the `git diff` hunk into the audit** and by grepping the repo, where the only surviving mention is the test asserting absence. **The duplicated constant is merged: the DIGITIZER owns it**, `measure_stitch_quality.py` imports it, pinned by a test asserting **identity not equality** so the two cannot drift back apart. The pipeline owns it because `scripts/` is not a package — a shipped service importing from a dev CLI would invert the dependency — and because `run_quality_bench.py` already imports `MIN_PENETRATION_MM` from the digitizer, so this is existing convention. **Geometry measured before anything changed**, and position confirmed STRUCTURALLY by instrumenting `_axis_underlay` rather than inferred from step length: both violations are medial-axis branch tips where the underlay walks out and back down the same line — 07 `Satin 1` triple@1 of a 6-point underlay (0.1828mm), 07 `Satin 13` triple@173 of a 268-point underlay (**0.0000mm, the needle in the identical hole**). **The finding worth arguing with: the metric was not wrong.** Locally a 180° running-stitch reversal and a satin column whose pitch has collapsed to zero are **the same shape** — `|ac|≈0`, `|ab|≈|bc|` large and anti-parallel — so no test on the triple alone can separate them; three candidate discriminators (anti-parallelism, equal legs, collinearity) were rejected because each re-measures the gap. The only separating signals are contextual or structural, i.e. teaching the metric the pipeline's internals, which is exactly what Part 5 built it to avoid. So the **generator** was fixed, not the metric. **The fix mirrors Part 10's principle — touch only what violates:** `_drop_floor_reversals` removes ONE point of the coincident pair, leaving the thread on the same line, with three guards — triples containing a jump are skipped (a jump breaks the run, and the flag must survive), the merged stitch must stay within `MAX_STITCH_MM` or **the point is kept and the violation reported honestly rather than traded for a long stitch**, and the pass count is bounded. **Cost corpus-wide: TWO POINTS** (07's underlays 6→5 and 268→267). **Only one field on one fixture moved**: 07 stitch_count 7,806→7,804. Interior/edge-band/spill **identical on all ten**, classification and all 96 verdicts identical, sub-0.5mm **9→9 unchanged**, 0 over the 12.7mm limit, max/mean stitch and jumps identical. **Visual cost painted, not asserted** (practice binding since Part 7): 138 differing pixels of 249,999 (**0.055%**) on 07 in one 16×23px region, **0 pixels** on 05; before/after at 8× committed. **The junction probe's last violation also closed** (`hairline_30deg` Satin 3 min 0.259→0.314mm) — independent confirmation, since that probe was built in Part 7 for an unrelated purpose and the repair was not aimed at it; curvature and letter probes byte-identical. **Option (b) argued even though unused:** the physical risk IS smaller for a running stitch (the floor exists to stop cumulative pile-up; a turnaround is one re-entry, ubiquitous practice), but excluding it was declined — the fix cost two points, the metric cannot identify underlay locally anyway, and `MIN_PENETRATION_MM = 0.30` **has still never been tested on fabric**, so relaxing an unvalidated rule on an unvalidated argument compounds two guesses. **Nothing was excluded to reach zero.** **Standards:** digitizer.py **95%** (up from 94%), measure_stitch_quality.py **95%**; `_drop_floor_reversals` 46 lines and `_axis_underlay` 33, both inside the limit; `ruff` 14 all pre-existing (one introduced, fixed before commit); secrets clean; one constant added, one renamed and shared, two dead definitions removed. **Also recorded:** Parts 7–10 reported “ruff 14 over every touched file” when 14 was digitizer.py alone. **pytest 123 WITH rembg and 123 WITHOUT; vitest 57.** |
| 46 | 2026-07-29 | Claude (Opus 5) | 🛠 Fix | **v2 Part 10 — targeted floor repair replaces Part 9's blanket protection. Floor violations 3 → 2 AND sub-0.5mm stitches 33 → 9**, both machine-safety numbers improving at once with **no coverage figure moving on any fixture**. **Housekeeping, and why it needed doing twice:** the test comment still read `apex_M 96.6 → 97.9, apex_V 98.1 → 98.7`; **Part 9's audit reported it corrected but the edit never landed** — a Python `str.replace` searched for "the opposite direction to…" where the file says "the opposite of…", and a replace that matches nothing returns the string unchanged and raises nothing, so the no-op was silent and the audit was written from intent rather than the file. Now **corrected and verified by diffing**, with the provenance recorded in the comment. **The fix:** Part 9 protected EVERY mitred endpoint from `_coalesce_short`, paying a short stitch per mitre whether or not it would ever cause a violation. Part 10 inverts the order — coalesce first, recording what was dropped after each survivor, then `_restore_for_floor` re-inserts **only** the points whose absence leaves a same-side pair under the floor, using the metric's own zigzag test so a running-stitch underlay cannot trigger a spurious restore. Any dropped point in the offending span restores A-B-A-B alternation, so **the repair is minimal by construction**. The floor is passed only for satin; tatami rows never zigzag and stay byte-identical. **Beat the target on both axes** (brief asked for violations held at 3 with short stitches meaningfully under 33). The extra violation closed is fixture 08 `Satin 16` 0.2519mm — **pre-existing and unfixed since Part 7** — because the repair does not care how a violation arose. **Both remaining violations are the running-stitch UNDERLAY** (07 Satin 1 0.1828mm, 07 Satin 13 0.0000mm); **for the first time since Part 6 there are ZERO satin-column violations corpus-wide**, leaving the underlay double-back as the only producer, now six parts unfixed. **Sub-0.5mm from actual coordinates, cross-checked against the bench JSON:** 01:0 02:0 03:0 04:2 05:0 06:0 07:4 08:2 09:1 10:0 = 9; three predate the mitre entirely, so **the mitre's true residual cost is 6 short stitches, not 27**. **Movement confined to stitch counts** on 02 (−10), 06 (−8), 07 (−11), 08 (−7) — exactly the four fixtures the blanket rule had inflated; **all three probes byte-identical**. **Deleted `_MITRED_POINTS`**, the module-level register Part 9 flagged in its own §7 as "state, and state is how bugs hide" — it became write-only once the repair moved downstream, answering Part 9 §8 item 3: the right fix needed none of the four signature changes. **Standards:** digitizer.py **94%**, measure_stitch_quality.py **95%**; `_coalesce_short` 30 lines and `_restore_for_floor` 31, both inside the limit; `ruff` 14 all pre-existing; secrets clean; two constants added, two constants and one register removed. **pytest 116 WITH rembg and 116 WITHOUT; vitest 57.** |
| 45 | 2026-07-29 | Claude (Opus 5) | 🐛 Fix | **v2 Part 9 — closed the penetration-floor regression Part 8 introduced: violations 5 → 3**, back to the pre-mitre count, **with no coverage number moving on any of the ten fixtures**. **The five located exactly** (fixture/object/underlay-or-column): 07 Satin 1 underlay 0.1828mm and 07 Satin 13 underlay 0.0000mm (pre-existing since Part 5), 07 Satin 13 **column 0.0618mm and 0.2659mm — the two the Part 8 mitre added**, 08 Satin 16 column 0.2519mm (pre-existing since Part 7). **Mechanism confirmed by experiment, not assertion**: fixture 07 run four ways gave mitre-on+coalesce-on **4**, mitre-on+coalesce-**disabled** **2**, mitre-off 2 either way — disabling `_coalesce_short` alone removes both new violations. **A first attempt is reported as failed rather than dropped**: Part 8 guarded the outgoing path step from a mitred point, so I added the incoming one on the theory the collapse was adjacent — **no change, still 5**. Coalescing *cascades*: each drop re-bases the comparison for the next point and shifts which points survive across the whole run, so the triggering short step is not adjacent to the mitre. **The fix:** `_coalesce_short` never drops a mitred endpoint nor the point immediately after one — protecting the point alone gave 4, point plus successor gave **3**. **The cost, stated plainly: sub-0.5mm stitches 6 → 33**, a *different* machine constraint (`MIN_STITCH_MM` prevents thread break and needle strike), so this trades one safety property against another; the floor wins per the brief, and the narrower fix (restore only the point that causes a violation) is named as unattempted. **Task 2 — no joined-apex vs butt-joint signal exists**: measured, a butt-jointed V and the letter probe's joined apex have **identical medial-axis topology** (branches=3, two arms plus a short apex spur, [166,169,9] vs [207,8,205]); a junction-margin gate was built against that theory, **measured inert** (apex_M band 92.1→92.2, butt joint still 97.3→95.1) and **removed rather than kept as decoration**. The real difference is the outline notch at the tip — a shape test, not cheap. **Task 3 — `MITRE_MIN_STALLED` sensitivity**: 2 → 97.41/94.06, **3 (shipped) → 97.59/94.20**, 4 → 97.54/94.05, off → 96.71/93.54 corpus interior/band; **violations are 3 at every setting now**, i.e. the floor no longer depends on this constant. **Task 4** — the test comment's "apex_M 96.6→97.9, apex_V 98.1→98.7" was wrong (numbers from an intermediate build before the MIN_STITCH_MM guard); corrected to **96.6→97.3 and 98.1→97.8** with provenance noted. **Held:** classification identical, **0** over 12.7mm, colours/segmentation/filled area identical; curvature and junction probes byte-identical, letter probe moved only apex_M band 92.2→92.1. `_mitre_one_side` hit 71 lines and `_revert_bad_mitres` was split out; `ruff` 14 all pre-existing; secrets clean; no net new constants. **Design note flagged for challenge:** `_MITRED_POINTS` is module-level state cleared per object (precedent: `_CLASSIFICATION_LOG`), with a test pinning the no-leak property. **pytest 115 WITH rembg and 115 WITHOUT; vitest 57.** |
| 44 | 2026-07-29 | Claude (Opus 5) | 🛠 Fix | **v2 Part 8 — satin mitre at sharp apexes, letter-shaped probe, committed uncovered-pixel painter.** **⚠ One mandatory item NOT met: residual penetration-floor violations 3 → 5** (4 underlay, 1 satin column); stated up front, revert is one constant. **Part 7 §4's account of the apex was wrong and the probe proved it**: it claimed the medial axis "terminates inside the shape" leaving an unreachable triangle; measured, `apex_V` has a **6-sample spur reaching within 3.0px of the tip** and 21 columns already reach the apex region. Part 7 asserted a mechanism without painting it — the practice it had just made binding. **The real conflict:** at a sharp vertex the OUTER arc needs a column every 0.4mm while every one wants its INNER end on the reflex point, packing far under the 0.30mm floor, so `_enforce_floor` drops them and the fan opens. **Option (a) the mitre was tried first**, laying inner ends along the corner's bisector (the medial axis IS that bisector). **Its failed first attempt is recorded**: firing on any sub-floor step scattered fresh gaps along every straight arm; a corner stalls a boundary for CONSECUTIVE columns, a straight stroke throws isolated ones, so `MITRE_MIN_STALLED = 3`. Three further guards each forced by measurement — axis must itself advance (past a terminal `mid` is clamped, stamping 0.000mm duplicates); mitred column ≥ `MIN_STITCH_MM` (a short column loses a point to `_coalesce_short`, breaking A-B-A-B alternation, the identical failure Part 6 measured for retraction — **took violations 7 → 5 at a cost of 2.4 interior points on 05**); and revert anything made worse. **Result:** interior **05 90.1→95.3, 06 96.7→97.7**, edge band **05 87.5→89.5, 06 90.1→92.3, 08 91.5→92.3**; apex red blocks broken up and much reduced but **partially, not fully, closed**. **Part 7 open question 2 answered explicitly: 08 recovers +0.8 of its 1.9 lost edge band (~42%); 03 recovers +0.1 of 2.7 — essentially nothing**, consistent with 03's loss being ring curvature with no apex to mitre. **A case where the mitre measures WORSE is pinned as a characterisation test rather than tuned away**: a butt-jointed V goes 97.3→95.1 interior, opposite to joined apexes. **New `tests/fixtures/letter_probe/`** — short arms, sharp apexes, bench-scale stroke, `apex_U` control and a 28.9° `apex_narrow` — reproduces the corpus defect immediately where Part 7's junction probe did not. **The uncovered-pixel painter is now committed tooling** (`measure_stitch_quality.py --paint`) since the practice is binding. **Held:** classification (stitch_types + all 96 verdicts) identical, **0** over 12.7mm, colours/segmentation/filled area identical. **Standards:** digitizer.py **94%**, measure_stitch_quality.py **95%**; `_mitre_stalled_side` hit 71 lines and was split, nothing added exceeds 50; `ruff` 14 all pre-existing; secrets clean; one new named constant. **pytest 113 WITH rembg and 113 WITHOUT; vitest 57.** |
| 43 | 2026-07-29 | Claude (Opus 5) | 🛠 Fix | **v2 Part 7 — junction-aware boundary partition.** Part 6 §4 named a Voronoi partition stalling at asymmetric junctions as the cause of the visible M/U gaps. **The mechanism is real and is now fixed; it was not the dominant cause.** **The adversarial case, built first** (`tests/fixtures/junction_probe/`, kept out of the ten-fixture corpus): a 3.75mm stem with a 0.63mm stroke leaving it at 30°. Colouring each boundary pixel by its assigned branch showed the hairline owning a tall vertical run of the **stem's** boundary — a stem boundary pixel sits 12.0px from the stem axis and 12.4px from the hairline axis, **a tie decided by noise**, and those pixels then collapse onto one station of the hairline's short axis. **Fix: subtract the local medial radius.** A boundary point lies at exactly r(a) from the axis point whose maximal inscribed disc touches it, so minimise `|p−a| − r(a)` instead of `|p−a|` — ~0 for the branch it bounds, positive for any other, **at any thickness ratio**. Pinned by a test: the hairline owns **12** stray stem-boundary points under the old rule, **1** under the new one. **The finding worth arguing with:** fixing the partition did NOT recover 05/06/08 — alone it moved 05 from 89.3 to **88.2** interior, slightly worse. Painting the misses and then measuring showed **`_enforce_floor` was dropping only 35 of 632 columns on fixture 05 (5.5%)** — the wedges were not dropped columns but columns **never generated**. Part 5 had gated the pacing loop on `min(moved_a, moved_b) >= floor_px`, requiring the SLOW side to advance a full floor; at a junction the slow arc is stalled, the minimum never reaches the floor, and the branch emits **no columns at all**. Part 6 §4's "the floor removed the overdraw" was half right — it also stopped generation, and that was the larger effect. Pacing now runs on the fast side only; the guarantee is untouched because `_enforce_floor` still applies 0.30mm to the final endpoints. **Two audits in a row attributed a coverage loss to the wrong cause and only got it right by painting the pixels.** **Result — partial recovery:** interior **08 94.1→96.9, 06 94.5→96.7, 05 89.3→90.1**, missed interior pixels on 05 **6,402→5,372 (−16%)**, fans around the M apexes and U join visibly thinned. **Not recovered:** the flat blocks at the apexes, now the dominant loss — at an M apex the medial axis terminates INSIDE the shape and the triangle above it is unreachable by any branch under any assignment; needs a junction-region treatment (mitre or tatami patch), the honest next step. **Real regression reported, not averaged away:** edge band 03 −2.7, 08 −1.9, ring probe −3 to −6 at every radius — this part **trades edge band for interior**. A variant (gate only closed loops) was measured and **removed as a no-op** — after `_extend_branch_ends` almost no annulus still closes within CLOSED_LOOP_TOL_PX. An **ablation table** is in the audit; the free-end cap distinction measures at **+0.01/+0.05, within noise**, kept as geometrically correct but flagged as arguably unearned. **Constraints held with the floor ON throughout:** classification (stitch_types + all 96 verdicts) **identical**, penetration violations **3 → 3**, **0** stitches over 12.7mm, colours/segmentation/filled area identical. **Standards:** digitizer.py **94%**, measure_stitch_quality.py **95%**, run_quality_bench.py 65% (pre-existing); `_free_ends` 25 lines; `ruff` 14 findings all pre-existing (3 introduced, fixed before commit); secrets clean. **pytest 110 passed WITH rembg and 110 WITHOUT; vitest 57.** |
| 42 | 2026-07-29 | Claude (Opus 5) | 🛠 Fix | **v2 Part 6 — penetration floor enforced.** Part 5 built the metric and left enforcement off so the decision could stand on its own evidence; this is that decision. **Corpus same-side penetration violations 3,235 → 3**, minimum penetration on the worst fixture **0.000 → 0.300mm**. On the ring probe (no terminals, so curvature is isolated) violations go to **zero at every radius**. **Nothing else moved**: `stitch_types` and all 96 per-object classification verdicts identical, `color_count` / `segmentation_method` / `filled_area_mm2` identical on all ten, jumps 2,046 → 2,045, **0 stitches over the 12.7mm limit**. **Two cleverer enforcement strategies were implemented and rejected on measurement** — deleting a whole crossing to fix one boundary looks wasteful, so both were built first: drop (shipped) 3 violations / 95.84 interior / 94.28 band · **slide** the end along its boundary 245 / 96.60 / 95.33 · **retract** the end along its column 44 / 96.61 / 95.34. Both buy ~0.5 coverage points and give up the guarantee, failing for the same reason in two forms — moving a penetration instead of removing it only relocates the crowding; retraction additionally shortens the column under the 0.5mm minimum so `_coalesce_short` eats a point and breaks the A-B-A-B alternation. A strict second pass rescued neither (59, 45). Both implementations **deleted, not parked behind a flag**. A floor sweep found **no knee** — coverage falls smoothly ~0.5 interior points per 0.05mm, and even removing only the exact duplicates costs a full point because a duplicate column still lays a full crossing — so the value rests on the safety argument alone, and **`MIN_PENETRATION_MM = 0.30` remains asserted from practice, not measured on fabric**. **The finding worth arguing with, and a VISIBLE regression:** fixture 05's wordmark now shows gaps at the M/U junctions (interior 99.8→**89.3**, band 98.3→**88.5**; 06 and 08 move the same way, the junction-free ring probe barely moves). The floor did **not** create those holes — at a junction one boundary arc stalls because the nearest-branch partition hands a branch a fillet that doubles back (the Voronoi weakness **Part 4 §9.3 named and left standing**), the columns there overlapped heavily, and **their surplus thread was painting over the gap**. So **Parts 4 and 5's junction coverage numbers were flattered by duplicate stitching** — the second time a measurement has invalidated an earlier part's comparison. The real fix is the junction-aware boundary partition, which is the next part. Corpus stitches 33,759 → 29,116 (−13.8%). **A product judgement flagged rather than made silently**: this ships a visibly worse wordmark today to buy a safety property the user cannot see; reverting is one constant. **A bug found in the instrument itself**: `measure_stitch_quality.py --floor-mm` defaulted to None and *called* `set_penetration_floor(None)`, which meant "report only" in Part 5 but silently **disabled enforcement** once the floor shipped on — caught because a `--fixture` run printed floor-off numbers; `--no-floor` is now the explicit switch, and an autouse fixture stops tests leaking the module floor into each other. **Residual 3 violations** unchanged from Part 5 (2 in the running-stitch underlay, which no floor governs; 1 in a 0.63mm column), still flagged unproven. **Standards:** digitizer.py **95%**, measure_stitch_quality.py **95%**, run_quality_bench.py 65% (pre-existing, unchanged); nothing added over 50 lines; `ruff` 15 = pre-existing count; secrets clean. **pytest 108 passed WITH rembg and 108 WITHOUT; vitest 57.** |
| 41 | 2026-07-29 | Claude (Opus 5) | 🧪 Test | **v2 Part 5 — penetration-density safety metric, committed grading methodology, written-down standards.** Measurement and documentation only: **all 10 fixtures byte-identical to v2-part4 on `stitch_types`, `stitch_count` and `jump_count`**, and every pre-existing metric field hash-identical. **(1) The metric.** Part 4 shipped boundary-paced satin and stated plainly that no metric detected its fabric-perforation risk. Now measured: a satin path alternates sides, so points i-1/i+1 are the same-side pair and the triple counts only when it ZIGZAGS — which a running-stitch underlay or a tatami row can never satisfy. Taken from the **stitch stream only**, so it stays valid however a future part generates columns. **Part 4 was wrong about its own corpus**: it guessed "no fixture is tight enough to show it"; in fact **every satin fixture** violates, 8-24% of penetrations under 0.30mm, with hundreds at *exactly* 0.000mm — the needle entering the same hole twice. Splitting by position found the **bigger producer is not the one Part 4 predicted**: its own cap handling runs the column grid past each terminal where both boundary arcs clamp to the same point, so those columns are duplicates (29-45% of terminal triples violate, vs 16-20% mid-run from curvature). **(2) The probe.** `tests/fixtures/curvature_probe/` — rings of decreasing radius, kept OUT of the bench corpus so "the corpus" keeps meaning the same ten fixtures. A first version using U-turns **failed and is recorded**: every open stroke has terminals, which pin the minimum near zero at every radius and bury the curvature signal; a ring has none. Measured p05 vs the closed form `pitch x (R-w/2)/(R+w/2)`: **R=8w 0.264mm (pred 0.353), 4w 0.185 (0.311), 2w 0.093 (0.240), 1.25w 0.009 (0.171)** — monotone as predicted and consistently tighter. **Answer to Part 4 §11.2: the concave side is already under 0.30mm at R=8w**, a curve most digitizers would call gentle — not an edge case, the normal case. **(3) The floor**, implemented at two points (pacing requires BOTH boundaries to advance past it; `_enforce_floor` then re-checks the FINAL endpoints, necessary because pull comp moves ends outward after the pacing decision — pacing alone fixed 86-91%, the second pass fixes all). Probe before→after: **violations 79/82/77/62 → 0 at every radius, min 0.000 → 0.300mm**; cost is radius-dependent — nearly free to R=4w, ~5 edge-band points at R=2w, **untenable at R=1.25w (edge band 99.6→53.7)**. **Shipped OFF by default and the reason stated**: the brief demands both a floor and byte-identical stitch counts, which cannot both hold, so the floor is implemented/tested/measured with enforcement off and the 5-10 point coverage decision surfaced rather than taken silently. Three residual violations on 07 **attributed**: 2 are in the running-stitch UNDERLAY (a second producer the floor does not govern), 1 in a 0.63mm column, likely `_coalesce_short` running after generation — **flagged as unproven rather than asserted**. **(4) Methodology committed.** `scripts/measure_stitch_quality.py` replaces the prose description every audit since Part 2.5 restated; the harness imports it rather than keeping a second copy, and both metrics now land in the per-fixture JSON. **It reproduces the historical numbers to the decimal** — including keeping a deliberate wart (`_poly` truncates rather than rounds; switching moved 07's spill 5.0→5.1 and would make the script disagree with the audits it exists to make reproducible). **(5) `docs/ENGINEERING_STANDARDS.md`** captures what has been applied since Part 3 — both rembg paths, coverage as a **percentage** with an 80% floor, ~50-line functions/~800-line files with a reporting duty, a runnable secrets scan, no magic numbers, conventional commits, and the honesty rules — each stated so it can be measured, with the command. **Standards compliance:** digitizer.py **94%**, measure_stitch_quality.py **97%** (raised from 66% by adding CLI tests rather than granting an exception on a document just written), run_quality_bench.py **65%** — under the floor at its pre-existing figure, stated not waived. All 15 added/split functions ≤50 lines; `ruff` 15 findings = exactly the pre-existing count; secrets scan clean; 2 new named constants. **pytest 107 passed WITH rembg and 107 WITHOUT; vitest 57.** |
| 40 | 2026-07-28 | Claude (Opus 5) | ✨ Feature | **v2 Part 4 — true edge-bounded satin.** The rewrite Parts 2.5 §5 and 3 §8.4 both deferred: columns are now laid between CORRESPONDING points on a stroke's two boundary contours, not between a centreline and a locally measured half-width. **Why the old way could not close the gap:** the distance transform (Part 2) and the ray-cast that replaced it (Part 2.5) both aim outward from the axis along a direction estimated from a 1px stair-stepped skeleton — wherever that tilts, the end lands off the outline. Edge-bounded satin removes the estimate: a column end *is* a boundary point. **Correspondence across junctions — the part earlier audits called blocking — is solved by refusing the global problem.** No attempt is made to split the contour into left/right arcs (at a junction no such split exists); instead every boundary pixel is assigned to its NEAREST axis branch, so the contour is partitioned by the skeleton's own topology and each branch sees only its own boundary. Within a branch a point carries `t` (arc length, refined by tangential projection) and a side; corresponding points are equal `t`, opposite side. **Caps and rings then need no special code path**: tangential refinement makes `t` keep increasing past the last axis sample, so running the grid two pitches past each terminal makes both arcs clamp and converge onto the cap; a ring (axis returns to its start) is the simpler case — `t` cyclic, arcs repeated one period each way. **5 of 293 corpus branches are closed loops.** **Measured — edge band now beats the TATAMI baseline on every satin fixture:** 02 96.8→**97.3**, 03 94.8→**97.2**, 04 99.6→**99.9**, 07 96.3→**96.9**, 08 95.8→**97.2**, 10 94.9→**95.2**, and against the Part 2.5 tatami reference **05 84.1→98.3 (+14.2)** and **06 87.5→99.8 (+12.3)** — *without* losing interior, which is at or above both prior parts everywhere except 08 (level with Part 3). Spill essentially flat and still under tatami on 03/05/06. **The finding worth arguing with:** with everything above in place edge band was STILL only ~85–92%; painting the uncovered pixels (the metrics all looked fine) showed a thin dotted rim along every stroke. `_emit_columns` was emitting both ends of each column with an alternating lead, putting two penetrations ONE PITCH apart on the same boundary back-to-back in the path — a 0.4mm stitch, under the 0.5mm minimum, so `_coalesce_short` correctly deleted it, **halving the needle penetrations on both boundaries**. Strict alternation (every path step a full crossing) is worth more than the rest of the part combined: 03 84.4→**97.2**, 05 82.0→**98.3**, 06 90.2→**99.8**. **This bug shipped in Parts 2, 2.5 and 3**, hidden because over-reaching ray-cast ends papered over it — which also explains why Part 2.5 §3's bias knob appeared to buy coverage. Those parts' comparative conclusions deserve re-reading. A half-pixel outward bias was swept and **rejected again** (buys nothing post-fix, costs spill 03 8.0→9.7, 05 12.2→15.6). **Classification untouched, structurally and by verification**: `_skeleton_satin` split so the classification widths come from a pass that feeds nothing downstream — all 10 fixtures identical `stitch_types`, all 96 per-object verdicts identical, colours/segmentation/`filled_area_mm2` unchanged, controls 01/09 bit-stable, **0 stitches over the 12.7mm limit**. **Costs:** stitches +30% corpus-wide (26,031→33,759 — the old counts were artificially low because half the penetrations were being deleted); runtime 12.8→13.2s; jumps exactly flat at 2,046. **Unmeasured risk stated:** boundary-paced pitch can put penetrations closer than the pitch on tight inner curves, which no current metric detects. **Engineering Standards:** `pytest --cov` digitizer.py **95%** (was 91%), new code ~184 stmts / 9 uncovered; all 15 added functions ≤50 lines (`_skeleton_satin` 133→53); no secrets or unnamed constants introduced (4 new named module constants); `ruff` 14 findings = exactly the pre-existing count. **pytest 95 passed WITH rembg and 95 WITHOUT; vitest 57.** |
| 39 | 2026-07-28 | Claude (Opus 5) | ✨ Feature | **v2 Part 3 — measured-width satin/tatami classification for EVERY shape.** The skeleton + local-half-width machinery Parts 2/2.5 built for `text_mode` is now *the* classifier; the global aspect-ratio/bounding-rect threshold is deleted. **Why the old rule could not work:** every term (`min(rect[1])`, `max/min` aspect) is a property of the bounding BOX, not the shape — a ring, arc or L-bend is uniformly thin and obviously satin work but has a square box, so fixture 04 (artwork made of *nothing but* thin lines) scored **0 of 11 objects** satin. **Replacement:** thin to the medial axis, measure local width along it, and require two independent things — width under the cap **and** columns swept along the axis actually *account for* the shape (`SATIN_MAX_UNCOVERED = 0.35`). The second test is what stops the obvious failure mode: a disc *has* a medial axis, but capped-width columns can't cover it, so broad fills stay fills. A distance-transform pre-gate skips thinning for obvious fills (17.5s→10.2s, classifications byte-identical under the pinned RNG). Every decision is logged per object and surfaced in the harness JSON, so every audit claim is quoted from the run. **Measured: satin 19/96 → 71/96 objects.** 04 **0→11**, 07 **0→14**, 08 **0→12**, 02 **1→12**, 03/10 0→2; **controls 01 and 09 unchanged at 0** (region widths 8.43/6.97/7.17mm → `broad_fill_pregate`). **0 stitches over the 12.7mm limit; corpus jumps 2,613 → 2,046 — below even the v1 baseline's 2,175.** Colour/background verified directly for 02/07/08/09: `color_count`, `segmentation_method`, `objects_with_holes` and `filled_area_mm2` identical **to the decimal**, and colour stops + per-object colour assignment dumped from both builds diff as IDENTICAL. (`coverage_ratio` 1.0→0.0 on 02/07/08 is *not* a colour field — `fill_row_geometry` correctly declines to report row pitch for majority-satin designs, the Part 1 guard.) **Two regressions I introduced, both recorded with intermediates:** (a) generalising satin to rings broke `_center_walk` (it walks the min-area *bounding rect* midline = a diameter straight through a ring's hole); the medial-axis replacement decimated by **list index** while axis samples are 0.4mm apart, producing **82.22mm stitches** on 6 fixtures — a first fix threading per-branch jump flags *still* left 6 over the limit and was **abandoned** rather than tuned, replaced by distance-based decimation with gap-derived jump flags. (b) fixture 04's rings shipped **visibly dashed** — caught by opening the grid, not by any metric: `_skeleton_branches` counted raw 8-neighbours, so every L-corner of a thinned staircase read as a junction (**617 'branches' from 1,288 skeleton pixels**, most 2px long); suppressing diagonals already served by a shared 4-neighbour takes it to **1 branch** (edge band 89.3→**96.7%**, jumps 192→**29**). **Honest counterweight:** re-grading all ten from stitch geometry, **edge-band coverage FELL on every fixture that flipped** (03 −7.5pts, 05 −3.3, 08 −3.5, 04 −2.9) while **spill fell on all of them too** — Part 2.5 §4's trade-off confirmed corpus-wide, with both numbers reported. **`SATIN_MAX_W_MM` 4.0→4.5**: `test_rotated_bar_becomes_satin_with_angle`'s bar is documented as "~3.6mm" but rasterises wider at 45° — measured **4.43 / 4.25 / 4.50mm** three ways (the old rule passed only because `minAreaRect` under-reads a diagonal at 3.82mm); sensitivity shows **exactly one object** separates 4.0 from 4.5 and **no broad fill changes at any cap up to 6.0mm**. `SATIN_ASPECT` + `SKELETON_MIN_WIDTH_MM` deleted (a comment claiming `SATIN_ASPECT` was still used by `rebuild_design` was wrong and is corrected); `SATIN_MIN_W_MM` kept but deliberately NOT enforced — 04's 0.28mm strokes are exactly what must be satin. **Still wrong:** 9 objects at 0.73mm return `no_medial_axis` (bean/run stitch is the craft answer, absent here); 03's annuli are the weakest satin call; runtime 7.6→12.8s for ten designs. Scope held to `digitizer.py` + `run_quality_bench.py`. **pytest 90 passed WITH rembg and 90 WITHOUT; vitest 57.** |
| 38 | 2026-07-28 | Claude (Opus 5) | 🛠 Fix | **v2 Part 2.5 — preview renderer fidelity + satin edge crispness.** Closes both open items from the Part 2 report. **(1) Renderer.** Measured the defect rather than assuming it: the preview showed only **62.2%** of fixture 05's area as inked against **87.1%** actual geometric coverage — it understated *every* fill, satin worst, which is why a reviewer concluded the strokes were "hollow". Fixed by filling the quad between consecutive satin crossings (what thread physically occupies) plus **3× supersampling**; the swath is **gated** so it only fires when crossings actually touch, so a genuinely sparse fill still renders sparse and the preview cannot flatter the stitches. Result **62.2→91.8%** (05), **74.9→93.1%** (06), **71.2→96.5%** (01, tatami). Also: white thread `#ffffff` on `(250,250,250)` paper differed by a total of **15** across channels — mathematically invisible, and the reason a reviewer thought fixture 02's 398 white stitches were missing; low-contrast thread now gets a darker backing halo (white swatch: **23.4%** of pixels now differ from paper). **(2) Satin edges.** Root cause found: the distance transform returns the distance to the *nearest* edge, not the edge *in the column's direction*, so applying it symmetrically made one column end fall short and the other overshoot on any stroke whose medial axis is off-centre — no amount of tangent smoothing could fix that. Each column end now **ray-marches to the real boundary** (~25 lines; no classification/threshold/topology change). **06's edge band now beats tatami (90.8% vs 87.5%)** with interior also up (98.5%); **05 closed 48% of its edge gap** (6.0→3.1 pts) with interior flat (95.2→94.9%). **Important nuance recorded:** the edge-band metric alone flatters tatami, which earns part of its number by overshooting — measuring *spill outside the outline* shows satin is crisper on both fixtures (**05: 10.8% vs tatami 20.0%; 06: 20.6% vs 27.5%**). A bias knob that would close 05's last 3 points was measured and **deliberately not used**, because spill rises to 18.0/32.9% — buying back the exact furriness reviewers flagged. **Verification:** all **8 non-text fixtures hash-identical** to v2-part2 (canonicalised metric JSONs, volatile fields stripped — the PNGs necessarily differ since the renderer changed on purpose, and claiming byte-identical PNGs would have been false). **pytest 90 passed WITH rembg and 90 WITHOUT; vitest 57.** |
| 37 | 2026-07-28 | Claude (Opus 5) | ✨ Feature | **v2 Part 2 — skeleton-guided satin lettering.** Glyphs are strokes, not blobs: lettering now emits satin columns ACROSS each stroke, stepping ALONG its medial axis, with column width taken from the distance transform so it tracks local stroke width (needed for script faces). **Two scope decisions flagged, not made silently:** (a) **not** using `skimage.skeletonize` — scikit-image is absent from `requirements.txt`/`-dev.txt` (it only arrives via the optional rembg extra) and `cv2.ximgproc` is missing from opencv-headless, so depending on either would rebuild exactly the environment-dependence Part 1's review caught; Zhang-Suen thinning is implemented in NumPy instead. (b) A shared `digitize_image` change **was required** — `generate_lettering` delegates to it and the bench runs 05/06 as PNGs through the same function, so a lettering-only change would have left every bench number identical; added an explicit `text_mode` flag (default off) rather than shape-driven detection, which is Part 3. **Three defects found by measuring, each fixed:** the medial axis stops half a stroke-width short of a stroke END so every terminal lost its cap (coverage **82–89%** vs tatami's 96.7/99.3% → **96.3/95.8%** after extending branch ends along their tangent); thinning sprouted hairs (letter 'S' = **82 branches from 179 skeleton px**, each starting its own run → pruned); tangent estimated from adjacent samples swung ~45° on a stair-stepped skeleton and fanned columns into a furry edge → ±3-sample window. Per-segment tatami fallback where a stroke exceeds the satin cap; the width test uses **median** because the distance transform spikes at junctions (SUMMIT stems 3.66mm median but 7.32mm p90 purely from 'M'/'U' joins). **Measured:** satin share **05 17%→100%, 06 25%→100%**; fixture 05 jumps **174→93**; **all eight non-lettering fixtures byte-identical**; 02/07/08/09 colour counts unchanged (3/3/5/2 from JSON); 0 stitches over the 12.7mm limit. **Adversarial re-grade (challenged): 05 2→3 improved, 06 2→2 mixed**, both controls confirmed untouched. A challenger claimed the strokes were "hollow — outline stitching, not satin"; **measured and refuted** — interior coverage *rose* (84.5→**95.2%**, 92.9→**98.2%**), and the hollow appearance is the *third* instance of the preview renderer misrepresenting stitches (it draws hairlines, so satin columns read as separate ticks). Two of their criticisms **were** right and are recorded: edge-band coverage fell 84.1→78.1% on 05 (the ragged edges are real, unfixed — edge-defined satin is the known remedy and is larger than Part 2's scope), and `fill_row_pitch_mm` reported a physically impossible 0.018mm for satin, now guarded to 0.0 for majority-satin designs. **pytest 90 passed WITH rembg and 90 WITHOUT; vitest 57.** |
| 36 | 2026-07-28 | Claude (Opus 5) | 🐛 Fix | **Part 1 post-review fix — anti-aliasing halo promoted to a thread colour** (reviewer-blocked). The reviewer ran the suite themselves and found `test_lettering.py::test_single_color_text_is_one_stop` failing, contradicting the report's "88 passed". **They were right, and the failure was environment-dependent**: with the optional `rembg` installed the suite passed; on the *documented* install (`requirements.txt` + `requirements-dev.txt`, which is also what CI runs) it was **87 passed / 1 failed**. Reproduced deliberately via `STITCHIQ_DISABLE_REMBG=1`. **Root cause** (the same bug already logged open in audit §6.3 for fixture 05, not previously connected to the test): rendering leaves a 1–2px anti-aliased band where two colours meet; those blend pixels were fed to k-means as ordinary samples, so a 2-colour budget on one-colour art put the second centroid on the halo. **Fixed at root, not special-cased for lettering**: (1) the palette is now learned from the foreground's **interior** (eroded mask), so only colours owning real area can seed a centroid, and every foreground pixel is then assigned to the nearest palette entry — the halo is absorbed, not promoted; (2) genuinely **ambiguous blend pixels are left unassigned**, because assigning each to its nearer centroid grew every shape ~1px per side and silently pushed a 3.6mm satin bar past the 4mm satin/tatami threshold. **A third defect surfaced while fixing it**: U2-Net emits a soft matte whose 50% level sits *outside* the true edge, so `alpha>128` fattened every shape (measured on a 3.60mm bar: 128→4.45mm, 192→4.14mm, **224→3.82mm**) — threshold raised to 224, which was mis-sizing satin classification on every rembg-segmented input. **Verified: 90 passed with rembg AND 90 passed without**, pinned by a new parametrised regression test covering both tiers so this can never again be hidden by one machine having an optional dep. **Fixture 05 now returns exactly 1 colour** (was 2); every Part-1 win intact (02→3, 07→3, 08→5, 09→2 unchanged); jumps improved **2,768→2,688**; 0 stitches over the 12.7mm limit. Audit §7's false "88 passed" claim corrected in place with the correction visible rather than silently edited. |
| 35 | 2026-07-28 | Claude (Opus 5) | ✨ Feature | **v2 Part 1 — layer preservation, real background separation, contour smoothing** — commit `a1b4109`. Diagnosis first: the audit's root causes #1 and #2 were **the same bug**. v1 decided background by **colour identity** (`_is_background`, ΔBGR<40 vs the corner average), deleting every pixel of a matching colour anywhere in the frame — fixture 02's white type sat at **Δ0.0** and was deleted whole; 08's cream muzzle at **Δ34.8** was just inside the cutoff; 09's gradient bands at **Δ51–53** were just outside it and got embroidered. No threshold fixes that, so background became a spatial **mask**: new `services/segmentation.py` = **rembg/U2-Net** → **border flood-fill** → v1 corner heuristic, all lazily imported with clean fallback (rembg is optional, in `requirements-features.txt` only — it downloads a 176MB model, so CI must not depend on it). k-means now clusters **foreground pixels only**. Garment-coloured regions are judged by **enclosure** (topological) + size, so a catchlight inside a pupil is ink but a glyph counter or a ring's interior is not. Contours get Douglas-Peucker + Chaikin; morphological opening is now **conditional** (unconditional 3×3 opening was erasing the ~2px stem that removed the **L** from HARBOR CLUB); speck threshold 4.0→2.0mm² so the mascot's 2.6mm² freckles survive; sub-0.5mm needle penetrations are coalesced. Also fixed `render_preview`'s hard-coded 2px stroke (v1 audit §3) and reworked the harness metrics to measure coverage from **stitch geometry, not the bitmap**. **Measured v1→v2:** colours matching request **2/10→5/10**; sub-0.5mm stitches **3,456→2**; fixture 09 **5,816→1,010 st** (background no longer stitched); 0 stitches over the 12.7mm limit. **Acceptance questions: 02 white text stitches ✅ (398 st, verified by plotting the stop on grey — it is invisible on the white preview), 08 cream muzzle ✅ (797 st) + catchlights ✅, 07 "HARBOR C UB"→"HARBOR CLUB" ✅, 09 background ✅, 10 "LC" ❌ not fixed.** **Honest counterweight:** the adversarial re-grade moves barely at all (bg 3.3→3.6, contour 2.5→2.4, colour 3.2→3.1; 1 improved / 7 mixed / 2 regressed) — Part 1 fixed the named defects without lifting perceived quality, because blocky tatami and illegible small text dominate the eye and belong to Parts 2–3. **Open regressions:** jumps **2,175→2,768** (7/10 fixtures — caused by stitching content v1 deleted; the fix is annular-region decomposition, a fill-pattern change reserved for Parts 2–4); fixture 05 gains a spurious light colour stop. Adversarial review also caught a regression I introduced (Chaikin shrinking adjacent layers apart, opening a bare wedge on fixture 01) — traced, measured (v1 27.5% · 2 iters 41.1% · 1 iter **27.8%**) and corrected. **pytest 88 · vitest 57 · typecheck clean.** |
| 34 | 2026-07-28 | Claude (Opus 5) | 🧪 Test | **v2 plan Part 0 — digitizer quality benchmark harness + honest v1 baseline** — commit `db7d098`. Measurement infrastructure only; **`digitizer.py` and the renderer have a zero-line diff** (the harness imports them). **Corpus:** `tests/fixtures/quality_bench/` — 10 fixtures (flat logo · fine text · soft-edge gradient · thin-line art · caps + script wordmarks · circular badge · high-detail mascot · **non-uniform background** · **low-contrast subject**), all original art generated by `_generate_fixtures.py`, so there is no licensing question and the corpus is byte-reproducible. **Harness:** `scripts/run_quality_bench.py` → per-fixture PNG+JSON, `<tag>-summary.json`, and a labeled input→output grid; renders through the app's own `package.render_preview` so the audit grades what customers receive; `--tag` + a pinned OpenCV RNG make future runs diff-able (verified: two runs, identical metrics). **Baseline (`docs/benchmarks/v1-baseline-audit.md`): mean 1.2/5, 0/10 fixtures customer-acceptable**, scored by 10 independent graders then re-scored by 10 adversarial reviewers — **the adversarial pass lowered a score on all 10**. Hard numbers: **8/10 fixtures lose a color** (in 02 and 08 the lost layer *was* the subject — white type, cream muzzle); **satin chosen for only 10 of 76 objects** (badge and mascot 100% tatami → the "blocky text" complaint, measured); badge needs **718 jumps across 13 objects** and stitches "HARBOR CLUB" as "HARBOR C UB"; fixture 09 stitches most of its background. Genuinely passing: no stitch >12.7mm, zero crashes/warnings, 2.2s for ten designs. **⚠️ Audit correction:** most graders called the fills "gappy"; measuring the geometry disproved it (row pitch 0.28–0.42mm vs 0.4mm thread, **0 rows >1mm across all 10**, density 0.59 vs 0.62 st/mm² professional). The gaps are an artifact of `render_preview` drawing fixed 2px polylines regardless of scale — a **real but different** defect, since that misleading PNG ships in the customer's package ZIP. Logged for a later part, not fixed here. **pytest 83→88** (+5 harness guards). |
| 33 | 2026-07-28 | Claude (Fable 5) | 🐛 Fix | **Make features work out of the box — 9 defects from a full e2e + in-browser sweep** — commit `0d7125b`. User reported "features run partially / results not good"; every feature was exercised over HTTP and in headless Chrome. Fixes, worst-first: **(1, fresh install broken)** the documented setup (`requirements.txt`+`-dev`) omitted **pyembroidery** → Open/Export/Convert/Package all failed on a fresh clone; moved to core requirements. **(2)** black-on-black canvas: stitches rendered on a near-black stage → dark designs (e.g. black lettering) were invisible; added a **light fabric backdrop** + stitches draw at **physical 0.4mm thread width** → fills read as embroidery. **(3)** Check/Quality/Optimize banners all rendered in one fixed corner — hid each other and **blocked clicks on Studio⇄Dashboard**; now they stack below the toolbar. **(4)** lettering accepted unsupported chars (emoji → .notdef tofu boxes → garbage stitch rectangles); per-char detection vs U+0378 rejects them with a clear 422. **(5)** lettering dropped sub-4mm² details ('i'/'j' dots at small sizes); lettering digitizes at 0.5mm² min-region ('i'@8mm keeps its dot, +2 tests). **(6)** tatami row pitch 0.6→0.45mm (full coverage, was fabric-through-fill). **(7)** page-nav Studio/Dashboard toggle reused the absolutely-positioned `.view-toggle` class → sat on top of Sign-in. **(8)** DST stops surfaced pyembroidery filler threads literally named "Random" → now "Color n (file has no color data)". **(9)** favicon 404. **pytest 81→83** (the emoji test previously FAILED on Linux fonts — now passes), vitest 57, typecheck+build clean, zero console errors/failed requests in Chrome re-verify. README refreshed (stale counts/claims). |
| 32 | 2026-07-04 | Claude (Opus 4.8) | 🛠 Fix | **Harden session code — 6 bugs from an adversarial multi-agent review** — commit `24c9aef`. A 42-agent Workflow (5 review dimensions × 3 skeptics/finding) raised 12, **confirmed 7** (deduped to 6 root causes), rejected 5. Fixes, worst-first: **(1, data loss)** `create_design` only error-checked the `designs` INSERT — a failed `design_objects`/`color_stops`/`design_versions` write was swallowed → phantom 201 that lists but 404s on open; now every child write `raise_for_status`es + compensating-DELETEs the orphan row on failure. **(2, data loss)** `setDesign` left `activeTool`/`draft` dirty → loading a file mid-draw then Finish wiped an imported .DST; now resets tool+draft + `onFinishDraw` re-guards `isImportedNotEditable` (**verified in Chrome**: Open mid-draw keeps the 87-st import). **(3)** in-memory ids derived from `len()` reused after delete → monotonic `itertools.count`. **(4)** `list_designs`/`design_stats` truncated at PostgREST's 1000-row cap → added `_get_all` pagination. **(5)** malformed `design_id` → 400→502; now uuid-validated → 404. **(6)** RUNNING_DOUBLE/TRIPLE duplicated the turnaround vertex (0-length stitch) → drop the junction point. Plus 502 bodies no longer leak the internal Supabase URL/query/uuids. **pytest 78→81** (+3 regression), vitest 57; live cloud round-trip + browser re-verify; typecheck+build clean. |
| 31 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Manual digitizing — draw Run/Satin/Fill on the canvas** — commit `7a7bdfb`. Lights up the dead toolbar tools. Backend: `rebuild_design` gains a **RUNNING branch** (`_manual_run` + `_resample_open`) so a drawn **open path** stitches ALONG it (single/double/triple pass) instead of area-filling; Fill=tatami/Satin=column reuse existing branches. Frontend: store `activeTool` + `draft` points (setTool/addDraftPoint/undoDraftPoint); **StitchCanvas draw mode** (click drops points → design-mm via the fit Group, live polyline + hoop + crosshair); Toolbar wires **Select/Run/Satin/Fill** + Finish/⌫/Cancel; `lib/manual` builds the object (contour + stitch-type spec) → commits via `/api/designs/rebuild` (new design, or appended to a digitized one; imported files blocked with a message). **Esc** cancels. **Verified in Chrome**: draw Fill quad → 536-st tatami; add Run path → running chevron (16 st); both editable objects; **Undo reverts**. **pytest 75→78** (+3), **vitest 52→57** (+5); typecheck+build clean. |
| 30 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Phase 8 v1 — optimization engine (path opt + quality)** — commit `fe2482d`. Classical/deterministic baseline (neural digitizing & text-to-design need GPU/data → future). `services/optimizer.py`: **`optimize_path`** — since `rebuild_design` already groups objects by color, the win is a **nearest-neighbour tour within each color** to cut needle travel/jumps → reassign sequence_order + rebuild; returns before/after metrics; no-op when not regenerable or no gain. **`analyze_quality`** — 0–100 score + grade + findings (over-long >12.7mm stitches, sub-0.5mm stitches, excessive color changes/jumps). New `POST /api/optimize/{path,quality}` + models (PathMetrics/OptimizeReport/OptimizeResult/QualityFinding/QualityReport, TS mirror). Toolbar **Quality** + **Optimize** buttons with report banners; Optimize uses `replaceDesign` (Undo reverts). **Verified in Chrome**: digitize 8-object logo → Quality **A·100/100** → Optimize cut travel **328.8→272.9mm (−55.9mm)**, objects renumbered, Undo works. **pytest 70→75** (+5); vitest 52; typecheck+build clean. |
| 29 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Real per-user Dashboard from cloud data + toolbar wrap** — commit `09a74ac`. New `GET /api/designs/stats` (owner-scoped aggregate: design count, summed stitches/colors, recent list) → `services/supabase_store.design_stats`. `lib/dashboard.ts` rewritten: signed-in shows **real cloud metrics** (My designs / Total stitches / Colors used), signed-out falls back to this-browser saved designs (colors = "—", no local source); refetches on login/logout. Retires the placeholder revenue/users/conversion KPIs. Also fixed: the button-heavy toolbar now **flex-wraps** to a 2nd row instead of overflowing the page (the ☁ buttons had widened it past 1280px). **Verified in Chrome**: sign up → Open sample.dst → ☁ Save → Dashboard shows *My designs 1 · Total stitches 87 · Colors used 2* from cloud + recent activity. **pytest 69→70, vitest 52** (dashboard tests rewritten for the new shape); typecheck+build clean. |
| 28 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Phase 6 — per-user auth + cloud Save/Open UI (§8)** — commit `0d96ee8`. Backend `routers/auth.py` (signup/login/me over Supabase GoTrue; signup admin-creates a confirmed user then logs in) + `deps.current_user` (verifies bearer token → 401 when Supabase on, `local-dev` sentinel when off). `designs` CRUD now **scoped to the acting user** (list/get/delete filter by owner; create attributes to the user + mirrors auth.users→public.users). Frontend `lib/auth` + `store/authStore` + `AuthBar` (sign-in popover / logged-in email + logout) in the top nav; Toolbar **☁ Save / ☁ Open** (per-user cloud) beside local Save; client attaches bearer token, surfaces `{detail}` errors, handles 204. **Verified live in Chrome**: signup→logged-in→Open sample.dst→☁ Save→☁ Open lists it→reload keeps session; **multi-user isolation** over HTTP (B can't see/GET A's designs; unauth/bad-pw→401). **pytest 65→69, vitest 47→52**; typecheck+build clean. Phase 6 now essentially complete. |
| 27 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Phase 6 — Supabase cloud persistence (§8)** — commit `d93696d`. User provided real Supabase keys + DB password; **`db/schema.sql` (10 tables) applied to the live project** (via psycopg direct connection). `services/supabase_store.py` (PostgREST + Auth-admin over the service key; get-or-create system owner user for the RLS FK chain) + `designs.py` CRUD now persist to Supabase — create writes designs/design_objects/color_stops + a full-fidelity `design_versions` snapshot; get restores stitches+contours; delete cascades; **graceful in-memory fallback** keeps app + offline pytest running keyless. Seeded `thread_database` (5 threads). **Verified end-to-end over real HTTP against live Supabase**: POST→201 (uuid+ts), list, full-fidelity GET, DELETE→204→404. **pytest 65/65** (+2 `test_designs.py`); httpx→core dep. Secrets in gitignored `apps/backend/.env` only. *(Remaining Phase 6: per-user auth/login UI + frontend "cloud save" wiring — backend attributes to one system user for now.)* |
| 26 | 2026-07-04 | Claude (Fable 5) | ✅ Verify | **In-browser render VERIFIED** (no code change). Drove the real app in system Chrome via Playwright + screenshots: shell, Open→canvas paints stitches, stop-select→highlight+Properties, TrueView 3D→lit thread tubes, Digitize PNG→correct fill + nested objects, Check→banner. Retires the long-standing "paint not eyeballed" caveat (§12). Only console msg: `/favicon.ico` 404 (cosmetic). |
| 25 | 2026-07-04 | Claude (Fable 5) | 📝 Docs | **README rewrite** — commit `e04a78a`. Replaced the stale "scaffold only, no features" README with an accurate description of the working studio (inputs, editing/rebuild, TrueView 3D, validation, export/convert/package, local save, dashboard), real stack, setup/run/test, honest gates. Points to STATUS.md. |
| 24 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Download editable master (.stiq.json)** — commit `88d00e7`. `serializeMasterDesign` + Toolbar **Master** button download; completes the master round-trip (previously the master was only inside the package zip). **vitest 47/47** (+2 serialize→parse fidelity). |
| 23 | 2026-07-04 | Claude (Opus 4.8) | ✨ Feature | **Studio dashboard page (Phase 6 groundwork)** — commit `7cc6c96`. `lib/dashboard.ts` (pure: formatters + activity derivation + `fetchDashboard`, unit-tested), `components/dashboard/Dashboard.tsx` (loading/error/empty/data states), App **Studio ⇄ Dashboard** nav toggle, dashboard/stat-card/activity CSS. Revenue/users/conversion have **no source yet** → honest "—" (Phase 6); **recent activity is real** from `lib/storage.ts` saved designs. Frontend-only, no backend/data-model change. **vitest 45/45** (+10), typecheck/lint/build clean. |
| 22 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Thread length per color on the worksheet (§4.9)** — commit `b3e977e`. `build_worksheet` computes thread consumed per color (STITCH segment sum; jumps break the run) → `WorksheetColorRow.threadLengthMm` (TS+Pydantic); PDF shows metres. **pytest 63/63** (+1). |
| 21 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Stronger pre-export validation (§4.8)** — commit `5e4f06f`. Hoop-fit is now a blocking issue (design bigger than its hoop can't be stitched); >15 color changes warns. **pytest 62/62** (+3). |
| 20 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Re-open master .stiq.json** — commit `d23c24e`. `lib/masterFile.ts` parseMasterDesign (JSON → validated Design, keeps objects/contours) + isMasterFilename; Toolbar Open routes .json → local parse, embroidery → backend. Closes the export→edit→re-open loop. **vitest 35/35** (+5); export→import contract verified vs a real package zip. |
| 19 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Local design persistence (save/load in-browser)** — commit `e7ba3d3`. `lib/storage.ts` (localStorage save/list/load/delete, injectable KV → unit-tested); store `setDesignId`; Toolbar **Save** + **Saved (n)** popover (load/delete). Closes "lose your work on refresh" without keys; cloud sync stays Phase 6. **vitest 30/30** (+5). |
| 18 | 2026-07-04 | Claude (Fable 5) | ✨ Feature | **Pre-export validation surfaced in the UI (§4.8)** — commit `9dae09e`. Toolbar **Check** button + validate-before-Export; dismissible report banner (blocking issues vs warnings vs all-clear). `/api/export/validate` existed since Phase 1 but was untested/unwired — now `test_validate.py` (empty/ok/oversize/long-stitch). **pytest 59/59** (+4); e2e via proxy. |
| 17 | 2026-07-03 | Claude (Fable 5) | ✨ Feature | **Appliqué stitch type (§4.3) + satin hardening** — commit `b1a6c35`. rebuild APPLIQUE branch = placement outline → tackdown → 2mm satin border (`_run_along`/`_satin_border`/`_resample_closed`); `_satin_zigzag` subdivides cross-width zigs so wide columns stay ≤12.7mm; PropertiesPanel stitch-type selector. **pytest 55/55** (+4); e2e: 2 objs→APPLIQUE→1220 st, max 2.0mm, exports PES. |
| 16 | 2026-07-03 | Claude (Fable 5) | ✨ Feature | **Pull compensation (§4.6)** — commit `407a327`. Wires up the dead `DesignObject.pull_compensation` field: digitizer assigns a fabric-dependent default (knit 0.4–0.5, woven 0.15–0.2mm) and dilates the top fill/satin to counter fabric pull; rebuild honors edits; PropertiesPanel pull-comp input. **pytest 51/51** (+4); e2e: pull 0→1mm widens 71.5→72.25mm. v1 = uniform dilation (directional is future). |
| 15 | 2026-07-03 | Claude (Fable 5) | ✨ Feature | **Thread nearest-match (§4.4 complete)** — commit `c5e942d`. `hex_to_lab` (pure sRGB→Lab D65) + `nearest_thread` (CIE76 ΔE); `POST /api/threads/match` implemented (was 501), 422 on bad hex; ThreadPalette "nearest catalog thread" button snaps a stop's color to an orderable thread. **pytest 47/47** (+5); e2e via proxy (black→Black, red→Flame, blue→Royal Blue). |
| 14 | 2026-07-03 | Claude (Fable 5) | ✨ Feature | **Phase 7: TrueView 3D thread preview** — commit `af37f26`. `lib/thread3d.buildThreadScene` (pure, tested) → `TrueView3D` renders TubeGeometry per color run with lighting + fabric plane; drag-rotate + scroll-zoom (no OrbitControls dep); 2D/3D toggle. **vitest 25/25** (+4). ⚠️ 3D **paint not eyeballed** (headless) — geometry math tested, render needs a human. |
| 13 | 2026-07-02 | Claude (Fable 5) | ✨ Feature | **Phase 5: production export package (ZIP) + brand map** — commit `afbe3b2`. `POST /api/export/package` → ZIP (machine file + master .STIQ JSON + worksheet PDF + color-card PDF + preview PNG + summary); `GET /api/formats` (brand→format table); Toolbar **Package** button. **pytest 42/42**; e2e 6-artifact ZIP via proxy. |
| 12 | 2026-07-02 | Claude (Fable 5) | ✨ Feature | **Phase 4 lettering + holes + 4 digitizer bug fixes** — commit `d9c8fea`. Text→stitches (PIL render → digitizer) `POST /api/lettering` + Toolbar Text button; `DesignObject.holes` (donut/counter carve via RETR_CCOMP + rebuild). Adversarial review of the diff surfaced 4 bugs (its verifier agents died on a session limit → I confirmed each by reproduction): **phantom color stops** (spurious thread change on every design), **satin rotation crop** (narrow letters half-height), **400→1200px res cap** (wide text collapsed), **empty-result 422**. **pytest 38/38** (+5 regression); e2e via proxy. |
| 11 | 2026-07-01 | Claude (Fable 5) | ✨ Feature | **Phase 5 start: convert endpoint + export picker + CI config** — commit `b3aaf13`. `POST /api/convert` (base64 any→any, color-loss warnings, 400/415 errors); Toolbar export dropdown (.DST/.PES/.JEF/.EXP/.VP3); `.github/workflows/ci.yml` (**unverified — no remote**). **pytest 28/28**; e2e dst→jef via proxy. |
| 10 | 2026-07-01 | Claude (Fable 5) | ✨ Feature | **Phase 3 complete: underlay generation (§4.6)** — commit `b212b44`. Edge-walk under fills (0.6mm inset, 2mm running stitch), center-walk under satin columns; digitize assigns `underlay_type`, rebuild honors it (toggleable per object); Properties underlay selector. **pytest 24/24**; e2e via proxy: underlay 1011 → NONE 790 stitches. |
| 9 | 2026-07-01 | Claude (Opus 4.8) | ✨ Feature | **Phase 3: object-level editing + server-side rebuild** — commit `f5ddf89`. `DesignObject.contour` (mm outline, TS⇄Pydantic); `rebuild_design` re-fills every object from its contour with current density/angle (angled tatami via rotate-scan); `POST /api/designs/rebuild`; objects listed under stops; Properties object mode (density/angle → Apply). **pytest 21/21, vitest 21/21**; e2e halve-density 885→309 stitches via proxy; imported designs → 422. |
| 8 | 2026-07-01 | Claude (Opus 4.8) | ✨ Feature | **Phase 3: satin detection + digitize params dialog** — commit `df14eb0`. minAreaRect classifier (0.8–4mm, aspect ≥2.5) → rotated-zigzag **SATIN columns** (any angle); `DigitizeDialog` (fabric/hoop/max-colors). **pytest 16/16** (+3); SATIN confirmed e2e via proxy. Threshold is physical mm → hoop-dependent (by design). |
| 7 | 2026-07-01 | Claude (Opus 4.8) | ✨ Feature | **Phase 3 core: auto-digitize v1** — commit `9eed902`. OpenCV pipeline (k-means quantize → background drop → contour regions → boustrophedon fills) → `Design` with **real objects** + darkest-first stops; `POST /api/digitize` + Toolbar **Digitize** button. cv2 4.13/numpy 2.5/pillow 12.2 work on py3.14. **pytest 13/13** (+5); e2e PNG→DST verified via proxy. |
| 6 | 2026-07-01 | Claude (Opus 4.8) | ✨ Feature | **Phase 2: reorder color stops** — `dfe8901`. Pure `reorderColorStop` + store + ▲▼ UI. vitest 18/18. |
| 5 | 2026-07-01 | Claude (Opus 4.8) | ✨ Feature | **Phase 2: on-canvas selection + undo/redo + vitest** — `05117e9`. vitest 13/13. |
| 4 | 2026-06-30 | Claude (Opus 4.8) | ✨ Feature | **Phase 1 tail + Phase 2 start** — `12a18f7`. Worksheet PDF; threads catalog; recolor/rename + ThreadPalette. pytest 8/8. |
| 3 | 2026-06-30 | Claude (Opus 4.8) | ✨ Feature | **Phase 1 core** — `d9fbc28`. pyembroidery I/O, parse/export/validate/worksheet; Konva render; StitchPlayer. pytest 5/5. |
| 2 | 2026-06-30 | Claude (Opus 4.8) | 📝 Docs | Roadmap (§14) + Phase 1 deep-dive (§15). STATUS v1 `3e34389`. |
| 1 | 2026-06-30 | Claude (Opus 4.8) | 🏗 Scaffold | Greenfield monorepo. `d6a4fd1`. |

**Type legend:** 🏗 Scaffold · ✨ Feature · 🐛 Fix · ♻️ Refactor · 📝 Docs · ⬆️ Deps · 🚀 Deploy

---

## 🧰 3. Tech Stack (exact installed versions)

**Frontend** (`apps/frontend`, **TypeScript**): react/react-dom 18.3.1 · vite 5.4.21 · typescript 5.9.3 ·
konva 9.3.22 + react-konva 18.2.16 · three 0.169 + @react-three/fiber 8.18 (stub) · zustand 5.0.14 ·
@tanstack/react-query 5.101.2 · zod 3.25.76 · eslint 8.57.1 · **vitest 2.1.9** (18 tests).

**Backend** (`apps/backend`, **Python 3.14**):
| Package | Role | Installed? |
|---|---|---|
| fastapi 0.138.2 · uvicorn 0.49.0 · pydantic 2.13.4 (+settings) · python-multipart | core | ✅ |
| **pyembroidery 1.5.1** · **reportlab 5.0.0** | embroidery I/O · worksheet PDF | ✅ used |
| **opencv-python-headless 4.13.0 · numpy 2.5.0 · pillow 12.2.0** | **auto-digitize (Phase 3)** | ✅ **used — confirmed on py3.14** |
| pytest 8.x · httpx | tests | ✅ dev |
| scipy · supabase | Phases 8/6 | ❌ not installed |

> Deps: `requirements.txt` (core) · `requirements-dev.txt` (tests + reportlab + opencv/numpy/pillow) · `requirements-features.txt` (remaining heavy libs).

---

## 📁 4. Repository Structure

```
apps/frontend/src/
  App.tsx (+ undo/redo shortcuts)  main.tsx  index.css
  types/design.ts                shared data model (TS)
  lib/stitches.ts                pure buildRuns / computeBounds / reorderColorStop (unit-tested)
  lib/units.ts                   mm↔px helpers (unit-tested)
  api/client.ts                  parse · digitize · export · worksheetPdf · listThreads · validate
  store/designStore.ts           design · selectedStop · playHead · updateColorStop · reorderStop · undo/redo
  components/
    toolbar/Toolbar.tsx          Open · Digitize · Export · Worksheet · Undo/Redo (live)
    canvas/StitchCanvas.tsx      Konva render + zoom/pan + click-to-select (live)
    panels/ColorObjectList.tsx · PropertiesPanel.tsx (recolor/rename/reorder) · ThreadPalette.tsx (live)
    player/StitchPlayer.tsx (live) · trueview/TrueView3D.tsx (stub)
  {lib,store}/*.test.ts          vitest (18 tests)
apps/backend/app/
  main.py  config.py  models/design.py            shared data model (Pydantic)
  routers/  auth(signup/login/me) · files · digitize · export(+package) · worksheet · convert · lettering · threads · designs CRUD (all live, Supabase-backed + owner-scoped)
  services/ embroidery_io · digitizer · worksheet_pdf · threads(list+nearest) · supabase_store · supabase_auth (all live) · deps.current_user (bearer gate)
  tests/ test_embroidery_io · test_worksheet · test_digitizer · test_designs · … · make_fixtures · fixtures/sample.dst,.pes
db/schema.sql (**applied to live Supabase**) · docs/ · STATUS.md · README.md · AI-Embroidery-Software-Prompt.md
```

---

## 📊 5. Feature Status Matrix

**Status:** 🔴 Stub · 🟡 In progress / partial · 🟢 Done & verified

### Backend endpoints (`/api`)
| Endpoint | Status | Behavior |
|---|---|---|
| `/health` · `/files/parse` · `/export` · `/export/validate` | 🟢 | ok · parse → Design · stream file · checks |
| `/worksheet` · `/worksheet/pdf` | 🟢 | Worksheet **JSON + PDF** |
| **`/digitize`** | 🟢 | **image → Design with objects (TATAMI fills + SATIN columns + contours)** |
| **`/designs/rebuild`** | 🟢 | **re-fill objects from contours with edited params (422 if not regenerable)** |
| **`/convert`** | 🟢 | **base64 any→any + color-loss warnings** |
| **`/lettering`** | 🟢 | **text → Design (PIL render → digitizer); 422 on unsupported glyphs** |
| **`/export/package`** · **`/formats`** | 🟢 | **production ZIP (6 artifacts)** · brand→format map |
| `/threads` (GET) | 🟢 | catalog (brand filter) |
| **`/threads/match`** | 🟢 | **nearest catalog thread (CIE Lab); 422 bad hex** |
| **`/auth/signup`·`/login`·`/me`** | 🟢 | **Supabase GoTrue proxy; bearer-token gate (`deps.current_user`)** |
| **`/designs` CRUD (POST/GET/GET id/DELETE)** | 🟢 | **owner-scoped Supabase persistence + version snapshot; keyless in-memory fallback** |
| **`/designs/stats`** | 🟢 | **per-user aggregate (count · stitches · colors · recent) for the Dashboard** |
| **`/optimize/path`** · **`/optimize/quality`** | 🟢 | **Phase 8: nearest-neighbour path opt (cut travel/jumps) · 0–100 quality score + findings** |

### Backend services
| Function | Status |
|---|---|
| `read_embroidery`/`write_embroidery` · `build_worksheet`/`render_pdf` · `list_threads` · **`nearest_thread`** (CIE Lab) · **`digitize_image`** (holes + satin/underlay, no-crop rotation) · **`rebuild_design`** · **`generate_lettering`** · **`build_package`** | 🟢 |

### Frontend components
| Component | Status | Notes |
|---|---|---|
| App shell · api client · types · lib/* | 🟢 | pure libs unit-tested |
| StitchCanvas · ColorObjectList · ThreadPalette · StitchPlayer · PropertiesPanel | 🟢 | select stops **+ objects** · recolor · rename · reorder · nearest-match · **edit stitch-type(+appliqué)/density/angle/underlay/pull-comp → rebuild** · animate |
| **TrueView3D** + 2D/3D toggle | 🟢 | thread tubes + lighting + fabric; drag-rotate/zoom (**render verified in Chrome — §12**) |
| designStore | 🟢 | + reorderStop · **selectObject/updateObject/replaceDesign** · undo/redo |
| Toolbar (Digitize/Lettering dialogs) | 🟢 | Open/Digitize/Text/Save/Saved/Check/Quality/Optimize/Export/Package/Worksheet/Undo/Redo + **manual Run/Satin/Fill draw tools** live |
| Local persistence (`lib/storage.ts`) | 🟢 | save/load/delete via localStorage (unit-tested); **cloud Save/Open now live too** |
| **Auth + cloud sync (`lib/auth`,`authStore`,`AuthBar`)** | 🟢 | sign-in popover · session persists · ☁ Save/Open per-user (verified in Chrome) |
| Master file (`lib/masterFile.ts`) | 🟢 | **Open** a `.stiq.json` → editable Design (objects/contours kept) · **Master** button downloads it (round-trip tested) |
| **Studio Dashboard** + Studio⇄Dashboard nav | 🟢 | **real per-user metrics** (My designs / stitches / colors) from cloud `/api/designs/stats` when signed in; local fallback when not |
| **Dashboard** (`lib/dashboard.ts` + `Dashboard.tsx`) | 🟢 | signed-in → **real cloud KPIs** + recent activity; signed-out → this-browser saved designs (colors "—"); refetches on login/logout; loading/error/empty states; pure logic unit-tested |

### Infrastructure
| Item | Status | Notes |
|---|---|---|
| Monorepo · shared data model | 🟢 | camelCase-on-wire verified |
| Tests | 🟡 | **pytest 88 + vitest 57**; CI config written (**unverified**) |
| **Digitizer quality bench** | 🟢 | 10-fixture corpus + `scripts/run_quality_bench.py`; **v1 baseline = 1.2/5, 0/10 acceptable** ([audit](./docs/benchmarks/v1-baseline-audit.md)) · **v2-part1 re-graded** ([audit](./docs/benchmarks/v2-part1-audit.md)) |
| **Foreground/background segmentation** | 🟢 | `services/segmentation.py` — rembg/U2-Net → flood-fill → corner heuristic, optional dep with clean fallback |
| **DB schema applied · Supabase designs CRUD** | 🟢 | 10 tables live; `services/supabase_store.py` create/list/get/delete verified over HTTP (§8) |
| **Per-user auth (signup/login) + cloud Save/Open** | 🟢 | GoTrue proxy · bearer-token gate · owner-scoped CRUD; verified in Chrome (§8) |
| Deploy · AI/ML · `.STIQ` binary | 🔴 | Phases 8 / X |

---

## 🟢 6. What's DONE (verified)

**Phases 0–2 (Updates #1–6):** monorepo + shared data model; parse/export/validate/worksheet(JSON+PDF);
Konva render + zoom/pan + click-to-select; recolor/rename/**reorder** color stops; ThreadPalette; undo/redo;
StitchPlayer; pytest + vitest suites; everything e2e-verified via the Vite proxy.

**Phase 3 core (Update #7, commit `9eed902`) — each confirmed by running it:**
1. **Auto-digitize** — PNG/JPG → k-means quantization → background drop (corner heuristic) → per-color
   contour regions → boustrophedon scanline fills (0.6mm rows, ≤6mm stitches) → machine-valid stream
   (JUMP/TRIM/COLOR_CHANGE/END) + **real `DesignObject`s** (TATAMI, density, entry/exit) + darkest-first stops.
2. **E2E via proxy** — real PNG → `/api/digitize` (885 stitches, 2 stops, 2 objects, 71.6mm) → `/api/export?format=dst` → re-read valid.
3. **Tests** — pytest **13/13**: +5 digitizer (objects produced, stream machine-valid ≤12.7mm, DST round-trip, garbage rejected, hoop fallback).
4. **py3.14 confirmed** for cv2/numpy/pillow (installed + functionally tested).

**Phase 3 tail (Update #8, commit `df14eb0`):**
5. **Satin detection** — narrow elongated regions (0.8–4mm wide, aspect ≥2.5) become rotated-zigzag
   **SATIN columns** with correct `stitch_angle`; wide shapes stay TATAMI. pytest **16/16**; SATIN
   confirmed through the real HTTP proxy path (−45° bar → `Satin 1`, 400 stitches).
6. **Digitize params dialog** — fabric/hoop/max-colors chosen before digitizing (was hardcoded defaults).

**Object-level editing (Update #9, commit `f5ddf89`):**
7. **Contours stored** — every digitized object carries its region outline in mm (`DesignObject.contour`).
8. **Server-side rebuild** — `POST /api/designs/rebuild` re-fills all objects from contours with their
   current density/angle/type (angled tatami via rotate-scan). E2e via proxy: halve density → 885→309
   stitches, bounds stable; imported (contour-less) designs correctly rejected 422.
9. **Object editing UI** — objects nested under stops in the left panel; Properties object mode
   (density/angle → Apply); undo restores the pre-rebuild design. pytest **21/21**, vitest **21/21**.

**Underlay — Phase 3 complete (Update #10, commit `b212b44`):**
10. **Edge-walk / center-walk underlay** (§4.6) — generated by digitize (fills → EDGE_WALK, satins →
    CENTER_WALK), honored + toggleable via rebuild, selectable in Properties. pytest **24/24**;
    e2e via proxy: underlay on 1011 stitches → NONE 790.

**Convert — Phase 5 start (Update #11, commit `b3aaf13`):**
11. **`POST /api/convert`** — base64 any→any through `read_embroidery`/`write_embroidery`; color-loss
    warnings for colorless formats (dst/exp/…); 400 bad base64 / 415 unknown format. pytest **28/28**;
    e2e dst→jef via proxy (valid JEF, threads preserved). Toolbar export dropdown (.DST/.PES/.JEF/.EXP/.VP3).
12. **CI config** — `.github/workflows/ci.yml` (pytest py3.12 + typecheck/vitest/build node22).
    **UNVERIFIED**: repo has no remote; the first GitHub push exercises it.

**Lettering + holes + digitizer fixes (Update #12, commit `d9c8fea`):**
13. **Lettering** (§4.10) — `POST /api/lettering`: PIL renders text → the digitizer turns it into
    contoured, editable, rebuildable objects with underlay. Toolbar **Text** + LetteringDialog. v1 = tatami fills.
14. **Holes/counters** — `DesignObject.holes`; RETR_CCOMP hierarchy carves an 'o'/'O' counter out of the
    fill (was solid), on both digitize and rebuild.
15. **4 digitizer bugs fixed** (found by adversarial review of the diff; verified by reproduction since the
    review's verifier agents hit a session limit): phantom empty color stops + dangling COLOR_CHANGE
    (every design carried a spurious thread change); satin rotation cropping (narrow letters ~half height);
    resolution cap 400→1200px (wide text collapsed); empty-result → 422. **pytest 38/38** (+5 regression).

**Production package — Phase 5 done (Update #13, commit `afbe3b2`):**
16. **Export package ZIP** (§4.8) — `POST /api/export/package` bundles machine file + master .STIQ JSON +
    worksheet PDF + thread color-card PDF + preview PNG (PIL) + summary txt. `GET /api/formats` returns the
    machine-brand format map. Toolbar **Package** button. **pytest 42/42**; e2e 6-artifact ZIP via proxy.

**TrueView 3D — Phase 7 done (Update #14, commit `af37f26`):**
17. **TrueView 3D** (§4.7) — `lib/thread3d.buildThreadScene` (pure, tested) → `TrueView3D` renders one
    TubeGeometry per color run with `MeshStandardMaterial` sheen, a fabric plane, ambient + 2 directional
    lights; drag-rotate + scroll-zoom via local state (no OrbitControls dep). 2D/3D toggle. **vitest 25/25**.
    ⚠️ The 3D **paint is not eyeballed** (headless) — geometry math tested; render/lighting/camera need a human.

**Threads + pull compensation (Updates #15–16, commits `c5e942d`, `407a327`):**
18. **Thread nearest-match** (§4.4) — `hex_to_lab` + `nearest_thread` (CIE76 ΔE); `POST /api/threads/match`;
    ThreadPalette "nearest catalog thread" snaps a color to an orderable thread. **pytest 47/47**; e2e via proxy.
19. **Pull compensation** (§4.6) — wires the previously-dead `pull_compensation` field: fabric-dependent
    default + top-layer dilation on digitize, honored/editable on rebuild, PropertiesPanel input. **pytest 51/51**;
    e2e pull 0→1mm widens 71.5→72.25mm. (v1 = uniform dilation; directional is future.)

**Appliqué + satin hardening (Update #17, commit `b1a6c35`):**
20. **Appliqué** (§4.3) — object stitch-type APPLIQUE → rebuild emits placement outline + tackdown +
    2mm satin border along the contour (`_run_along`/`_satin_border` + arc-length `_resample_closed`).
    PropertiesPanel stitch-type selector. **pytest 55/55**; e2e 2 objs→APPLIQUE→1220 st, max 2.0mm, exports PES.
21. **Satin hardening** — `_satin_zigzag` subdivides cross-width zigs by max-stitch, so a wide region
    mis-set to SATIN stays machine-valid (≤12.7mm) instead of emitting one giant stitch.

---

## 🔴 7. What's REMAINING

### A. Phase 3 — ✅ nothing remaining (complete as of Update #10)

### B. Phases 4–10 & cross-cutting
- export package ZIP + brand map (rest of 5) · Supabase persistence/auth (6) · TrueView 3D (7) ·
  AI engine + `nearest_thread` (8) · generative + assistant (9) · collab/API/mobile (10).
- Cross-cutting: **CI** (pytest + vitest + tsc on push), Dockerfiles/deploy, logging, authz/upload-limits.

---

## 🧭 8. Key Decisions & Rationale

| Decision | Why |
|---|---|
| **TypeScript** + **Python** | User request; Python mandatory for pyembroidery/OpenCV/reportlab. |
| Build **vertically**, phase by phase | User scope; full spec is multi-year. |
| **Classical CV digitizer first** (k-means + contours + scanline) | No training data/GPU needed; honest approximate baseline; neural quality is Phase 8; stays as fallback. |
| **Background = cluster near corner-average color** | Simple, no ML; fails on non-uniform backgrounds → documented risk. |
| Digitizer emits **objects** + raw stitches together | Objects unlock §4.3 property editing; stitches keep export/render trivially working. |
| Color stop = editable unit for **imported** files; objects exist for **digitized** ones | Imported stitch files genuinely have no vector data. |
| Reorder = re-sequence COLOR_CHANGE blocks · bounds-based round-trip asserts · pure libs for testability | See Updates #3–6. |
| npm workspaces · tiered py deps · single tsconfig · ESLint 8 · lazy heavy imports | See Updates #1–2. |

---

## ⚠️ 9. Environment & Gotchas

- **Python 3.14.3** — pyembroidery, reportlab, **opencv 4.13/numpy 2.5/pillow 12.2** all install & work (**confirmed**). Only `scipy`/`supabase` remain untested.
- **DST has no color** — parsed `.DST` shows filler colors; `.PES` preserves real ones.
- **Round-trip stitch count is not stable** (writer normalizes) — compare **bounds**.
- **Imported files are stitch-only** (`objects` empty); **digitized designs have objects**.
- **Digitizer assumptions:** background ≈ corner color; ≤8 colors; regions <4mm² dropped; wide shapes →
  TATAMI (0.6mm rows) + edge-walk underlay; narrow elongated (0.8–4mm, aspect ≥2.5) → SATIN + center-walk.
- **pnpm not installed** → `npm`. **venv** at `apps/backend/.venv`. **Vite proxies** `/api`+`/health` → `:8000`.
- **Port hygiene:** `lsof -ti tcp:8000 | xargs kill -9` before booting.

---

## 🎯 10. Next Steps (do these IN ORDER)

1. **Phase 6** — Supabase persistence/auth: apply `db/schema.sql`, wire `designs` CRUD + Storage + auth. **Needs the user's Supabase project URL + keys** (blocked until provided).
2. **Phase 7** — TrueView 3D preview (Three.js `TrueView3D` real impl).
3. **Push to GitHub** to exercise the CI config (currently unverified).
4. **Lettering v1.1** — per-stroke satin (skeletonize glyphs) instead of tatami fill; fix tiny-lowercase dot drop.

> After each step: re-run §11 checks and **update this file** (§2 + §5 + metadata).

---

## 🧪 11. How to Run & Verification Baseline

### Run
```bash
# Backend (apps/backend):  source .venv/bin/activate && uvicorn app.main:app --reload --port 8000
# Frontend (repo root):    npm run dev:frontend           # http://localhost:5173   ·   Both: npm run dev
```
### Fresh-clone setup
```bash
npm install
cd apps/backend && python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt -r requirements-dev.txt   # core + tests (reportlab, opencv, numpy, pillow)
python tests/make_fixtures.py
```
### Baseline (last confirmed 2026-07-01, Update #7)
| Check | Command | Expected | Result |
|---|---|---|---|
| Backend tests | `python -m pytest tests -q` | **81 passed** | ✅ |
| Frontend tests | `npm test -w apps/frontend` | **vitest 57 passed** | ✅ |
| Supabase CRUD e2e | POST/GET/DELETE `:8000/api/designs` (live project) | 201 (uuid) · full-fidelity GET · 204→404 | ✅ |
| Rebuild e2e | digitize → halve density → `:5173/api/designs/rebuild` | fewer stitches, bounds stable; imported → 422 | ✅ |
| Underlay e2e | digitize (EDGE_WALK default) → rebuild with NONE | 1011 → 790 stitches | ✅ |
| Convert e2e | dst→jef via `:5173/api/convert` | valid JEF, threads kept, warning | ✅ |
| Lettering e2e | text → `:5173/api/lettering` → export | 1 stop, objects with holes, valid file | ✅ |
| Package e2e | design → `:5173/api/export/package` | 6-artifact ZIP (machine/master/2×PDF/PNG/txt) | ✅ |
| Digitize e2e | PNG → `:5173/api/digitize` → `/api/export?format=dst` → re-read | 200 · objects>0 · valid DST | ✅ |
| Satin e2e | thin-bar PNG (100x100 hoop) → `:5173/api/digitize` | SATIN object with angle | ✅ |
| Parse / Export / Worksheet PDF / Threads | curl fixture → endpoints | 200 · `%PDF-` · 5 threads | ✅ |
| Frontend typecheck / build | `npm run typecheck` · `build -w apps/frontend` | 0 errors · builds | ✅ |

---

## 🚧 12. Known Risks / Unverified Claims

- ✅ **In-browser rendering VERIFIED (2026-07-04, Playwright + system Chrome).** Drove the real app at `:5173`
  and captured screenshots: studio shell renders; **Open sample.dst → stitches paint on the Konva canvas**
  + color list populates (87 st, 40×40mm); **click a stop → highlights + Properties editor opens**; **TrueView 3D
  → lit thread tubes on a fabric plane in perspective**; **Digitize a PNG → correct tatami-filled circle+square,
  nested object list, 935 st**; **Check → "✓ Ready to stitch" banner**. Only console msg: one `/favicon.ico` 404 (cosmetic).
  (Remaining unverified: fine-grained interactions like drag-pan/undo-keys and PDF/zip *download* file contents, though their logic is tested.)
- **Lettering v1 is tatami-filled** (not per-stroke satin — the classic look): fine for chunky text, heavier/blockier
  than pro satin lettering. Tiny lowercase (~8mm) can drop the dot on 'i'/'j'. Satin strokes = v1.1.
- **Digitizer quality is approximate** (classical CV): uniform-background assumption, no pull-comp, only
  edge/center-walk underlay — fine for bold logos, poor for photos/gradients. Phase 8 addresses this.
- **Adversarial review caveat (Update #12):** the workflow that reviewed this diff had its 14 verifier
  agents die on a session limit, so its `confirmed:[]` was meaningless. The 4 fixed bugs were confirmed
  by manual reproduction instead; other lenses (geometry, contract) never completed — a fuller re-review is worthwhile.
- **Satin threshold is physical mm** (0.8–4mm × aspect ≥2.5) — the same image can digitize as satin at a
  100x100 hoop and tatami at 130x180. By design; verified both ways via proxy.
- **Design persistence** unimplemented (needs Supabase). **supabase untested on py3.14.**
- **CI config unverified** — no GitHub remote; the workflow has never run.
- **DB schema unvalidated** against live Postgres.

---

## 🧬 13. Data Model Reference

Mirrored in [`apps/frontend/src/types/design.ts`](./apps/frontend/src/types/design.ts) ⇄
[`apps/backend/app/models/design.py`](./apps/backend/app/models/design.py). **Edit both together.**
Entities: `Stitch` · `StitchType`/`UnderlayType`/`ConnectMethod` · `Thread` · `ColorStop` · `DesignObject` ·
`Design` · `Worksheet` · `ValidationReport` · `Convert*`. camelCase alias makes JSON match TS (`width_mm` ⇄ `widthMm`).

---

## 🗺 14. Full Project Roadmap (Phases 0–10)

> **Build vertically.** Sizes: **S** hours · **M** 1–2 days · **L** ~a week · **XL** multi-week. Every phase: **implement → verify → update this file.**

| Phase | Name | Status | Size | Unlocks |
|---|---|---|---|---|
| 0 | Scaffold | 🟢 Done | — | the codebase |
| 1 | File I/O + Canvas | 🟢 Done | L | open/view/export + worksheet PDF |
| 2 | Interactive editing | 🟢 Done | L | select/recolor/rename/reorder/undo |
| 3 | Auto-digitizing v1 (OpenCV) | 🟢 **Done** | XL | TATAMI+SATIN + underlay + dialog + object-edit/rebuild |
| 4 | Lettering & monogramming | 🟢 **Done (v1)** | L | text → tatami-filled stitches ✅ · satin strokes = v1.1 |
| 5 | Production output & formats | 🟢 **Done** | M | convert + multi-format export + production package ZIP + brand map |
| 6 | Persistence & accounts (Supabase) | 🟢 **Done (v1)** | M | schema applied · **auth (signup/login)** · owner-scoped cloud CRUD + version snapshots ✅ · teams/password-reset = later |
| 7 | TrueView 3D simulation | 🟢 **Done** | L | lit thread tubes + fabric, drag/zoom (**render verified in Chrome**) |
| 8 | AI engine (+ thread match) | 🟡 **v1** | XL | ✅ thread match · ✅ **path optimization** · ✅ **quality scoring** (classical); neural digitizing = future (GPU/data) |
| 9 | Generative & assistant | ⬜ | XL | text-to-design, STITCH-GPT |
| 10 | Platform & scale | ⬜ | XL | collab, cloud API, mobile |
| X | Cross-cutting (tests/CI/deploy/security) | 🟡 Ongoing | — | ships everything safely |

### Phase 3 — Auto-Digitizing v1 🟢 DONE (size XL)
`digitize_image` (quantize → segment → TATAMI fills + **SATIN columns** + **edge/center-walk underlay**
→ Design with objects, **contours**, stops), `POST /api/digitize` + params dialog, **object editing
(density/angle/underlay) with server-side `rebuild_design`** (`POST /api/designs/rebuild`), 16 tests, all e2e verified.
Quality notes: classical CV baseline (uniform background assumed); neural digitizing + more underlay
types (double-zigzag/parallel/contour) are Phase 8.

### Phases 4–10 (summaries)
- **4 Lettering:** 🟢 v1 done — PIL render → digitizer (tatami + underlay + holes); satin strokes = v1.1. **5 Production:** 🟢 done — convert + multi-format export + production package ZIP + brand map (§4.8).
  **6 Supabase:** apply `db/schema.sql`, auth, CRUD + Storage (**needs user keys**). **7 TrueView 3D:** 🟢 done — lit thread tubes + fabric, drag/zoom (§4.7; paint unverified).
  **8 AI:** SAM/CNN/RL + quality scoring + Lab k-d thread match (§4.2/§6). **9 Generative:** diffusion + STITCH-GPT (§4.1/§4.11).
  **10 Platform:** collab/cloud API/mobile (§4.12).

### Phase X — Cross-Cutting (start now)
- Tests (pytest ✅ + vitest ✅; add **CI**) · Dockerfiles + deploy (§7) · logging/Sentry · authz, upload limits, rate limiting.

---

## 🔧 15. Phase 1 Deep-Dive — File I/O + Canvas

> **Status: 🟢 DONE & verified.** Kept as reference for how file I/O works.

### pyembroidery API cheat-sheet — CONFIRMED on v1.5.1 / Python 3.14
| Need | API | Notes |
|---|---|---|
| Read | `pe.read(filename)` | write the upload to a temp file first |
| Stitches | `pattern.stitches` → `[[x,y,cmd_int]]` | **1/10 mm** → ÷10 for mm |
| Commands | `pe.STITCH=0 JUMP=1 TRIM=2 STOP=3 END=4 COLOR_CHANGE=5` | map via `pe.*` (`embroidery_io._CMD_TO_STR`) |
| Threads | `pattern.threadlist` (**empty for DST**) | `.hex_color()`, `.description`, `.catalog_number`, `.brand` |
| Color blocks | `pattern.get_as_colorblocks()` → `(stitches, thread)` | **use for color stops** (DST + PES) |
| Extents / count | `pattern.bounds()` · `count_stitches()` | dimensions / count |
| Write | `add_thread`, `add_stitch_absolute(cmd, x×10, y×10)`, `pe.write(pattern, filename)` | mm→tenths ×10 |

Implemented in `services/embroidery_io.py`, `worksheet_pdf.py`, `threads.py`, `digitizer/` (package since Part 42); routers
`files`/`digitize`/`export`/`worksheet`/`threads`; frontend `StitchCanvas`, `lib/stitches.ts`, `Toolbar`,
`StitchPlayer`, panels, `store/designStore`, `api/client`.
Verify: `pytest -q` (63) + `npm test` (47). Manual: Open `tests/fixtures/sample.dst` **and** Digitize a PNG
(params dialog appears); click a stop or object, recolor, edit density/underlay → Apply, reorder ▲▼, undo, Export, Worksheet.

---

*End of STATUS.md — keep this file current. When in doubt, trust the code over this document, and fix the document.*
