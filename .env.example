# Root env example. Copy per-app:
#   cp apps/frontend/.env.example apps/frontend/.env
#   cp apps/backend/.env.example  apps/backend/.env
#
# Supabase (spec §8) — required only when wiring auth/persistence (not in this scaffold).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
