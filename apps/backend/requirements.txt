# Core — required to BOOT the scaffold (FastAPI app + typed models) and to run
# the primary features (open/export/convert need pyembroidery).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
python-multipart>=0.0.12
httpx>=0.27          # Supabase persistence (PostgREST + Auth admin) — services/supabase_store.py
pyembroidery>=1.5    # read/write 45+ embroidery formats — core Open/Export/Convert/Package

# Feature libraries are in requirements-features.txt (installed when implementing
# the stub endpoints). They are lazy-imported, so the app boots without them.

# Vector input (v2 Part 25): SVG -> exact raster + exact foreground mask, so
# vector logos skip the neural-matte guesswork entirely — digitizer._decode_svg.
svglib>=2.0          # SVG parsing to a ReportLab drawing
rlPyCairo>=0.4       # renderPM backend that rasterises the drawing

# Production process manager (v2 Phase B1). PROCESSES, not threads: the
# measured X1 stall (1,198ms of frozen event loop during one digitize) is GIL
# contention inside the threadpool, so only separate interpreters remove it.
# Dev still runs plain `uvicorn --reload`; the Dockerfile runs gunicorn with
# UvicornWorker.
gunicorn>=23.0

# Error reporting (v2 Phase B1). Optional at runtime: app/observability.py
# imports it only when SENTRY_DSN is set and degrades to a warning if it is
# missing, so a slim install still boots.
sentry-sdk[fastapi]>=2.20
