/** Thread palette — brand filter + swatches (spec §3, §4.4). Stub. */
export function ThreadPalette() {
  // TODO: query api.listThreads(brand), render swatches, drag onto objects to assign.
  return (
    <section className="panel">
      <h2 className="panel-title">Thread Palette</h2>
      <p className="muted">Brand filter (Madeira, Isacord…) + color swatches — stub.</p>
    </section>
  );
}
