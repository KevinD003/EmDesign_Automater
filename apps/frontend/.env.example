# Frontend env. Copy to apps/frontend/.env.
# Leave blank in dev (Vite proxies /api and /health to the backend).
# In production, set the deployed backend origin, e.g. https://api.stitchiq.app
VITE_API_BASE_URL=
